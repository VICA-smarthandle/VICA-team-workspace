# Nav2 백로그 — 진행해야 할 사항

> **이 문서의 용도**: devlog 는 날짜별 기록이라 시간이 지나면 파묻힌다. 이 문서는
> **지금 남아 있는 일**만 담는 살아있는 목록이다. 끝난 항목은 `[x]`로 바꾸고
> §12 로 옮긴다. 새 항목이 생기면 여기에 추가한다.
>
> **항목 ID 는 고정이다.** 코드 주석이나 커밋 메시지에서 `NAV2-B1` 처럼 참조한다.
> 값을 바꿀 때 그 자리 주석에 ID 를 남기면 다음 사람이 근거를 찾아올 수 있다.

기준 브랜치: `vica_ros2_ws` 의 **`dev`**. 근거 조사는
`devlog/2026-08-06-자율주행-설정-점검.md` 에 있다.

---

## 0. 한눈에

| ID | 항목 | 왜 실기가 필요한가 | 상태 |
| --- | --- | --- | --- |
| **B1** | AMCL 튜닝 4건 | 위치추정 거동이 직접 바뀐다 | [~] 구현·실기 대기 |
| **B2** | `pose_bootstrap` 를 dev 로 | 기동 절차가 바뀐다. 임계값 6종 전부 미검증 | [ ] |
| **B3** | MPPI 전환 + `inflation ≥ 0.651` | controller 교체는 주행 전체가 바뀐다 | [ ] |
| **B5** | `collision_monitor` 도입 | 새 노드가 `/cmd_vel` 경로에 들어간다 | [ ] |
| **B7** | 유턴 갇힘 — DWB 튜닝 3단계 | 회전 거동이 바뀐다 | [x] 2026-08-29 셋 다 무효 |
| **B8** | 화장실 구간 유턴 실패 (`vica_map_0630`) | 컨트롤러가 아니라 공간 문제다 | [ ] **최우선** |
| **B9** | 깊이 카메라를 costmap 에 (2D 스캔) | 새 관측원이 주행 판단에 들어간다 | [x] 2026-08-30 통과 |

**B4**(global `nvblox_layer` 재투입)와 **B6**(손잡이 지지대 각도)은 **구현하지 않는다.**
사용자 판정이며 §11 로 내렸다. 상세 조사는 §4·§6 에 근거로 남겨 둔다.

**의존 관계**: B3 는 §2 의 안전 전제를 먼저 고정해야 한다. 나머지는 서로 독립이다.
**권장 순서**: **B8** → B1 → B2 → B5.
B7 과 B3 는 2026-08-29 에 실주행으로 닫았다 — 컨트롤러 축(DWB 3단계 · RPP · MPPI)을
전부 시험했고 **문제는 컨트롤러가 아니라 공간이었다**(§7·§8).

**IMU·VSLAM pose 의 EKF 융합은 주행 파트를 닫은 뒤의 과제다**(2026-08-29 사용자
판정). 그 작업이 들어오면 카메라가 상시 필요해지므로 realsense CPU 18 % 를
주행 중 끄는 안은 함께 폐기된다.

---

## 1. NAV2-B1 — AMCL 튜닝 (구현됨·실기 대기)

### 무엇을

`src/vica_nav2/config/nav2_params.yaml` 1~41행.

| 파라미터 | 현재 | 제안 | 근거 |
| --- | --- | --- | --- |
| `update_min_d` | 0.25 | **0.10** | 자기잠금 해제 |
| `update_min_a` | 0.2 | **0.10** | 자기잠금 해제 |
| `alpha1` | 0.2 | **0.4** | 제자리 회전 직후 오차가 컸다 |
| `max_beams` | 60 | **180** | 유효 반사 414개 중 60개만 쓰고 있다 |
| `laser_max_range` | 100.0 | **`/scan` 실측값** | `likelihood_field` 가 무반사 빔을 유효로 오인한다 |

### 왜

**이 41줄에 주석이 단 한 줄도 없다.** git 이력상 마지막 변경은 좌표계 이름 한 줄
(2026-07-03)이고 나머지는 Nav2 샘플 그대로다. costmap·planner 에는 수백 줄의 실측
주석이 붙어 있는데 **위치추정만 손대지 않았다.**

그런데 이 부분이 낸 사고가 기록돼 있다.

- **실제 로봇이 AMCL 이 믿은 곳보다 26~30 cm 멀리 있었다** (`devlog/2026-07-31.md:14-48`)
- **자기잠금** (`:53-64`): 자세가 틀림 → 못 움직임 → "25 cm 이상 움직여야 갱신"
  조건 미달 → 자세가 계속 틀림. 20초 갇힘. AMCL 추정이 t+77.6~85.6 동안
  `(1.837, 3.848, 94.4°)` 로 완전히 고정돼 있었다.

내가 어디 있는지 틀리면 나머지 설정이 전부 헛돈다.

> **2026-08-11 구현됨.** `tune/amcl-b1` (`2c9eb47`) 에 네 값이 다 들어갔고 계약 시험
> `test_amcl_contract.py` 가 실측 수치를 상한으로 잠갔다. **실기 판정 전**이며 절차는
> `devlog/2026-08-11-amcl-b1-실기검증.md` 에 있다.

### 선행 조건

**`laser_max_range` 를 먼저 정해야 한다.** `/scan` 의 실제 `range_max` 가 아직
`[미확인]` 이다(`devlog/2026-07-31-home-return-implementation.md:268`).
이것이 틀리면 나머지 임계값 튜닝이 통째로 무의미해진다.

> **판단 교정(2026-08-07).** `handoff_nav2_collision_monitor_and_amcl.md` §12.5 가
> humble `amcl_node.cpp` 를 확인한 결과 `laser_max_range` 는 `min()` 으로 clamp 되므로
> 100.0 은 사실상 "드라이버 값 그대로"다. **선행 조건이 아니다** — 실측은 여전히 필요하지만
> 나머지 셋을 여기에 묶어 대기시킬 이유는 없다.

```bash
ros2 topic echo /scan --field range_max --once
```

### 검증

1. 위 명령으로 `range_max` 확인 → `laser_max_range` 에 반영
2. `max_beams` 3배는 CPU 도 3배다. 주행 후 제어주기 확인:
   `grep -c "missed its desired rate" ~/.ros/log/controller_server_*.log`
3. 자기잠금 재현 시험: 로봇을 20~30 cm 어긋난 상태로 두고 좁은 구간에 진입시켜
   `/amcl_pose` 가 고정되는지 본다. 고쳐졌다면 갱신이 계속 일어나야 한다

### 되돌릴 조건

제어주기 놓침이 늘거나, 입자가 과도하게 퍼져 위치가 튀면 `max_beams` 부터 되돌린다.

---

## 2. NAV2-B3 — MPPI 전환 (안전 전제가 붙어 있음)

> **2026-08-29 실주행 4회로 판정. 미채택.** 품질 지표는 거의 다 DWB 보다 좋았다 —
> 손 횡속도 0.055 → 0.029, 회전 반전 8.1 → 1.6 회/분, 경로 이탈 0.043 → 0.031 m,
> 제자리 회전 최장 1.8 → 4.1 s, 제어주기 놓침 566 → 95. **그런데 화장실 목표
> (3.5, 5.4) 에서 DWB 는 2/2 성공했는데 MPPI 는 4회 모두 못 갔다.** 한 번
> 금지구역에 들어가면 planner 가 경로를 거부하고, 경로가 없으면 컨트롤러는 할 일이
> 없다. 안내 로봇에서 도착은 타협할 수 없어 되돌렸다.
>
> 설정은 `nav2/mppi2` 에 보존했다(`5a6acc7`). 다시 볼 때 쓸 것:
> - **탐색 폭이 회전의 진짜 노브다.** critic 은 뽑힌 후보 중 고르는 기준일 뿐이라
>   후보에 없는 동작은 벌점을 0 으로 해도 못 고른다. `vx_std` 0.2→0.3,
>   `wz_std` 0.3→0.5 가 제자리 회전을 1.5 → 4.1 s 로 늘렸다.
> - **CPU 는 문제가 아니다.** batch 1000 이 DWB 의 2.5 배 계산량인데 제어주기
>   놓침은 오히려 6배 줄었다(점당 비용이 다르다).
> - `rotation_penalty` 는 5.0 인 채로 뒀다. 컨트롤러가 회전을 실행할 수 있으므로
>   **그 값을 함께 낮추는 조합은 아직 미시험이다.**
>
> 아래 전제 문단은 그대로 유효하다.


> B1·B2 보다 뒤에 두었으나 **전제 조건 때문에 여기 먼저 적는다.** 다른 과제를
> 하다가 controller 를 건드리게 되면 이 절을 반드시 읽을 것.

### 반드시 지켜야 할 전제

```
inflation_radius 0.55  <  외접반경 0.6506      <- 2026-08-29 해소됨
```

> **2026-08-29: 이 전제는 풀렸다.** 손잡이가 19 -> 11 cm 로 줄어 외접반경이
> **0.4727 m** 가 됐다. `inflation_radius` 0.55 가 이미 그것을 덮으므로 아래
> "0.651 이상으로 올려야 한다"는 요구와 그 대가(통로 회피 심화)는 사라졌다.
> 아래 문단은 그 전제가 살아 있던 동안의 기록으로 남긴다.

이 부등식 때문에 벽에서 **0.55~0.651 m 구간(폭 0.10 m)에서 planner 가 footprint
검사를 건너뛴다.** 지금 이것을 막는 것은 **DWB 의 `ObstacleFootprint` critic
하나뿐**이고, MPPI 로 바꾸면 그 방벽이 사라진다.

**controller 를 바꾸려면 그 전에 `inflation_radius` 를 0.651 이상으로 올려야 한다.**

- 계산 근거: `devlog/2026-08-05-inflation-외접반경-검증.md` 5절
- 이를 코드가 강제하게 하는 **계약 테스트 초안**: 같은 문서 8절
  (`test_short_inflation_requires_a_footprint_checking_controller`)
- 해당 파라미터 자리(`nav2_params.yaml` 의 `inflation_radius`)에도 경고 주석을 달아 뒀다

### 대가

`inflation` 을 0.651 로 올리면 통로 반폭 중앙값 0.70 m 에 육박해 **통로 회피가
지금보다 심해진다.** 0.55 에서 이미 이동 거리 +24 % 가 실측됐다. 즉 MPPI 전환은
"controller 만 바꾸는 일"이 아니라 **주행 성격 전체가 바뀌는 일**이다.

### 왜 MPPI 인가

DWB 로 닫힌 축이 넷이다(§4 참조). `libmppi_controller.so` 는 이미 설치돼 있다.

### 검증

전환 후 `No valid trajectories` 와 `Control loop missed` 를 먼저 센다. MPPI 는
`batch_size × time_steps` 를 20 Hz 로 돌리므로 Jetson CPU 여유가 관건이다.

---

## 3. NAV2-B2 — `pose_bootstrap` 를 dev 로

### 무엇을

`feat/home-return` 브랜치의 4개 파일을 dev 로 가져온다.

```
src/vica_localization/vica_localization/pose_bootstrap.py       (295줄, 순수 로직)
src/vica_localization/vica_localization/pose_bootstrap_node.py
src/vica_localization/launch/pose_bootstrap.launch.py
src/vica_localization/test/test_pose_bootstrap.py
```

### 왜

운영 매뉴얼 `docs/vica_robot_bringup_manual.md:490-517` 은 이미 `⑨-1 위치 자동
초기화` 단계를 요구하는데 **코드가 dev 에 없다.** 문서와 코드가 어긋나 있고, dev
로 운용하면 초기 pose 는 여전히 RViz 수동 조작이다. B1 의 사고와 직결된다.

### 검증 — 성공이 아니라 **실패해야 할 때 실패하는지**를 본다

임계값 6종이 전부 `[미검증]` 이다.

| 검사 | 기본값 |
| --- | --- |
| 위치 불확실성 | `cov_xy_max 0.0225` = (15 cm)² |
| 각도 불확실성 | `cov_yaw_max 0.0149` = (7°)² |
| 위치 이동 | `pose_shift_max_m 0.30` |
| 각도 이동 | `yaw_shift_max_deg 10.0` |
| 스캔 일치도 | `scan_match_min 0.70` (§4.1 로 신뢰도 낮음) |

**시험 절차**: 로봇을 일부러 **2 m 어긋난 곳**에 두고 검증이 제대로 실패하는지
먼저 확인한다. 너무 헐거우면 틀린 위치를 통과시키는데, 그쪽이 훨씬 위험하다.

### 주의

이 노드는 `/request_nomotion_update` 를 5회 호출한다. B1 에서 `update_min_d/a` 를
0.10 으로 낮추면 **정지 상태에서도 AMCL 이 스캔을 반영하기 시작하므로** 이 호출의
필요성과 횟수가 달라질 수 있다. **B1 을 먼저 하고 B2 를 조정하는 편이 낫다.**

---

## 4. NAV2-B4 — global `nvblox_layer` 재투입

> **구현하지 않는다**(사용자 판정, 2026-08-06). §11 로 내렸다. 아래는 다시 검토할
> 일이 생겼을 때를 위한 조사 기록이다.

### 현재 상태

```yaml
local_costmap  plugins: ["voxel_layer", "nvblox_layer", "inflation_layer"]   # 있음
global_costmap plugins: ["static_layer", "obstacle_layer", "inflation_layer"] # 없음
```

**설정 블록은 global 에도 그대로 남아 있다**(`nav2_params.yaml:932`). Nav2 는
`plugins` 목록에 이름이 있는 것만 로드하므로, 목록에 `"nvblox_layer"` 한 단어를
넣으면 되살아난다.

### 왜

라이다는 바닥에서 **38 cm 한 평면**만 본다. nvblox 는 카메라로 3D 를 본다.
지금은 **운전할 때(local)는 3D 가 보이는데 길 찾을 때(global)는 안 보인다.**
즉 planner 가 사선 책상다리 위를 지나는 경로를 그릴 수 있다.

> **2026-08-30 갱신**: 이 문단이 말하는 "라이다가 38 cm 한 평면만 본다"는 문제는
> **B9 로 다른 방법으로 풀렸다** — 깊이를 2D 스캔으로 눌러 local 에 넣었다.
> nvblox 를 되살릴 이유가 그만큼 줄었다. 다만 **global 은 여전히 라이다뿐**이므로
> planner 가 책상 위로 경로를 그리는 것은 그대로다. 그 축을 열 때는 nvblox 가
> 아니라 B9 의 스캔을 global 에 넣는 안부터 검토한다(유령이 훨씬 짧다).

### A/B 실측 (`nav2_params.yaml:901-908`) — 대가도 함께 본다

| 회차 | nvblox | 사람 | 완주 | lethal space | DWB 무효 | 유령 p95 |
| --- | --- | --- | --- | --- | --- | --- |
| ③ | 없음 | 없음 | 2/3 | 2 | **62** | 9.0 s |
| ④ | **있음** | 없음 | **3/3** | 1 | **4** | **46.0 s** |
| ⑧ | **있음** | 있음 | 2/3 | 16 | **1** | **45.0 s** |
| ⑨ | 없음 | 있음 | **1/3** | 29 | 9 | 8.0 s |

같은 조건끼리 비교하면 **넣는 쪽이 모두 낫다**(사람 없음 3/3 vs 2/3, 사람 있음
2/3 vs 1/3). DWB 무효 궤적은 62 → 4 로 15배 차이가 난다.

**대가는 유령 장애물이다** — p95 가 8~9 s 에서 45~46 s 로 5배 늘어난다(D3).
즉 B4 는 D3 와 묶여 있다. 재투입하려면 유령 대책
(`exclude_last_view_from_decay: false` 등)이 실제로 듣는지 함께 확인해야 한다.

그럼에도 뺀 이유는 `:919` — **"2026-07-30 재제거: 사용자 지시"**. 그 실패의 성격이
이후 "nvblox 유무가 아니라 지도-실제 불일치"로 규명됐다고 적혀 있다.

### 선행 조건

**사용자 판단 재확인.** 실측은 넣는 쪽이 나았으나 뺀 것은 지시였다.

계약 테스트가 이것을 "열린 실험"으로 기록 중이다
(`test_nvblox_layer_contract.py:80` `test_global_nvblox_presence_is_recorded_as_an_open_experiment`).
재투입하면 이 테스트도 함께 갱신한다.

---

## 5. NAV2-B5 — `collision_monitor` 도입

### 왜

지금 저지선이 둘뿐이다.

1. DWB 의 `ObstacleFootprint` critic — **§2 의 사각지대도 이것 하나가 막고 있다**
2. `vica_safety` 의 E-stop 래치·속도 clamp

costmap/DWB 바깥의 **독립 정지 계층이 없다.** 시각장애인이 뒤따르는 로봇에는
최후 방벽이 하나 더 있어야 한다는 판단이다.

### 배선 함정 — 이것 때문에 설계가 먼저다

```
controller → velocity_smoother → /cmd_vel_req → Safety → motor
behavior_server ─────────────────↗   (복구 동작은 smoother 를 건너뛴다)
```

**`behavior_server` 가 `/cmd_vel_req` 로 직접 발행한다.** velocity_smoother 뒤에만
Collision Monitor 를 끼우면 **Spin·Wait 같은 복구 동작이 감시를 통째로 우회한다.**
이 주의는 `devlog/2026-07-29.md:214-217` 에 이미 적혀 있다.

### 과거 기각 근거 (조건이 달라졌음을 명시할 것)

2026-07-29 검토 후 기각했다(`devlog/2026-07-29.md:206-217`). 근거는 "지원 동작이
`stop`/`slowdown`/`approach` 셋뿐이고 **모두 속도 크기만 바꾼다**"였고, 당시 목표는
"장애물에서 떨어져 가기"였다. 지금 필요한 것은 **최후 정지**라 조건이 다르다.

**후방 Collision Monitor 는 별도로 폐기됐다** — "사용자가 핸들을 잡고 뒤에 서므로
상시 발동한다"(`devlog/2026-07-30.md:152` "다시 제안하지 말 것" 표). 도입하더라도
**전방 전용**이어야 한다.

---

## 6. NAV2-B6 — 손잡이 지지대 각도 (하드웨어)

> **구현하지 않는다**(사용자 판정, 2026-08-06). §11 로 내렸다. 아래는 조사 기록이다.
>
> 이 항목을 접으면 **좁은 통로 제자리 회전(1.31 m 필요 vs 통로 1.0 m)은 미해결로
> 남는다.** 아래 표대로 소프트웨어 축 넷이 모두 양쪽 실패이므로, 다른 해법이
> 필요하면 §10 로 올려야 한다.

### 무엇을

뒤로 뻗은 손잡이 지지대를 **59.5 cm → 30.5 cm** 로 세운다.

### 왜 — 소프트웨어 축이 전부 닫혔기 때문

| 축 | 한쪽 | 다른 쪽 |
| --- | --- | --- |
| `ObstacleFootprint.scale` | 0.15 벽에 파고듦 | 0.6 궤적 전멸 152회 |
| `inflation_radius` | 0.45 벽 붙음 | 0.55 통로 회피(+24 %) |
| lattice 회전반경 | 0.5 여유 **1 cm** | 1 m 진입 불가 |
| `footprint_padding` | 0.05 planner 차단 | 0.04 DWB 차단 |

**넷 다 양쪽이 실패다.** 지금 유일하게 열린 축이 하드웨어다.

### 효과

```
핸들 스윕            0.645 → 0.42 m  (−35 %)
제자리 회전 필요폭   1.31 → 0.84 m   → 1.0 m 통로에서 회전 가능
```

STL 상 뒤로 나온 것은 높이 0.6 m 위 지지대 하나뿐이고 그 아래는 앞뒤 대칭이다.
**소프트웨어로 못 푼 두 문제(유턴 반경·좁은 통로)가 동시에 풀린다**
(`devlog/2026-08-02-주행테스트.md:382`).

---

## 7. NAV2-B7 — 유턴 갇힘 (DWB 튜닝 3단계) — [x] 2026-08-29 판정 완료

> **셋 다 유턴을 못 고쳤다.** 사용자 선택으로 3 → 2 → 1 순으로 실행했다.
>
> | 단계 | 결과 |
> | --- | --- |
> | 3 `max_vel_theta` 0.4→0.5 | **유지.** 상한에 눌린 시간 17.4→13.1 %, 손 횡속도 변화 없음. 유턴은 무효 — 각속도는 '얼마나 빨리 도느냐'라 원의 크기를 못 바꾼다 |
> | 2 `PathAlign` 32→48 / `GoalAlign` 24→36 | **유지하되 역효과.** 제자리 회전 최장 3.4→**1.8 s**. '살짝 돌며 전진'하는 궤적이 방향 점수까지 받아 이겼다 |
> | 1 Spin 되살리기 | **실주행 전 철회.** 사용자 판정 "위험해요". 근거 ② 가 사람에게 향하는 위험이라 그것만으로 기각 |
>
> **DWB 에 '회전 각도' 값은 없다.** 각도는 점수 경쟁의 결과이지 입력이 아니다.
> 제자리 회전을 늘리려면 전진 점수를 깎아야 하는데(`sim_time` 축소,
> `PathDist`/`GoalDist` 축소 — 둘 다 미시험) 안내 로봇에서 '덜 가는' 쪽으로
> 기울이는 것은 위험한 거래다. **이 축은 닫는다.**
>
> 아래는 그때의 계획과 근거다.


> **2026-08-29 신설.** 그날 실주행에서 **유일하게 남은 손실**이다. 근거 전체는
> `devlog/2026-08-29-유턴폭-rotation-penalty-rpp-판정.md` 에 있다.

### 증상

유턴하려고 **반경 0.89 m 호를 그리다 lethal 에 진입**하고, 로봇을 움직이는 복구가
없어 **60 초를 서 있었다**(전체 주행 270 초의 22 %). bag `run1241` 실측이다.
진입 직전 6 초 동안 호를 그린 비율이 100 % 였다.

기하는 이렇다. 제자리로 돌면 **0.945 m** 면 되는데 호로 돌면 **1.705 m** 가
필요하다. 유턴 폭을 정하는 것은 손잡이가 아니라 **차체 앞뒤 바깥 모서리**이므로
(꼭짓점을 직접 찍어 확인) 손잡이를 8 cm 줄여도 유턴 폭은 안 줄었다.

### 기준선 (2026-08-29 `run1241`, DWB · `rotation_penalty` 5.0)

| 지표 | 값 | 판정 |
| --- | --- | --- |
| Goal 성공 / 실패 | 5 / 0 | 좋음 |
| 회전 반전 | 7.9 회/분 | 사람이 편한 수준 |
| 손 횡속도 중앙 | 0.047 m/s | 기준값 |
| 제어주기 150 ms 초과 | 0 % | 문제 없음 |
| **갇힌 시간** | **60 초 (22 %)** | **이것만 남았다** |

### 1단계 — Spin 복구를 한 방향으로 되살린다

`RecoveryActions` 에 `<Spin spin_dist="1.57"/>`(90°) **하나**를 넣는다. 좌·우 쌍이
아니다. 제거 근거(BT 주석) 4가지 중 둘이 무효가 됐다.

| 제거 근거 (2026-08-12) | 지금 |
| --- | --- |
| ① 시간을 가장 많이 먹는다(27.6 s) | 유효 |
| ② 253 에서 회전 시 후방이 **0.651 m** 를 쓴다 | **0.4727 m** (18 cm 감소, 위험은 잔존) |
| ③ 좌·우 번갈아 나와 순 회전 +17.2° 뿐 | **구조 문제. 한 방향으로 고치면 해소** |
| ④ 회전에 **1.31 m** 필요한데 통로 1.0 m | **0.945 m.** 통로 1.3 m 에서 성립 |

②가 남는다 — Spin 의 충돌 검사는 254 만 거부하고 253 은 통과시키며 임계값을
파라미터로 못 바꾼다. `RoundRobin` 순서상 costmap clear 가 먼저 실행되는 것이
유일한 완화책이다.

**판정**: 갇힌 60 초가 줄어드는가. **벽·의자에 닿으면 즉시 되돌린다.**

### 2단계 — DWB 가 제자리 회전을 고르게 만든다 (예방)

`PathAlign.scale` 32 → 48, `GoalAlign.scale` 24 → 36. 비율을 유지하며 방향 정렬
배점만 올린다.

근거: 제자리 회전 궤적은 위치가 안 변해 `PathDist`(32)·`GoalDist`(24)에서 0 점을
받는다. **112 점 중 56 점만 경쟁에 쓸 수 있어 구조적으로 진다.** Align 배점을
올리면 크게 틀어진 자리에서 이길 수 있다.

**판정**: 주행 중 제자리 회전 최장이 3 초(70°) 이상 나오는가.
**되돌릴 신호**: 회전 반전이 7.9 → 12 회/분을 넘으면 실패다(직진 구간 진동).

### 3단계 — `max_vel_theta` 0.4 → 0.5

8/13 에 직진을 0.26 → 0.5 로 올리며 "회전은 따로 재겠다"고 미뤄둔 뒤 한 번도 안
건드린 축이다.

**상한이 실제 병목임을 실측으로 확인했다**(`run1241`):

    이동 중 105.7 s,  |w| 중앙 0.160   p90 0.400 = 상한
    상한(0.38 이상)에 붙어 있던 시간  18.4 s = 이동 시간의 17.4 %

열 번 중 한 번은 상한에 눌려 있다. 5 % 수준이었다면 올려도 효과가 없었을 것이다.

0.55 가 아니라 0.5 를 쓰는 이유는 **변화 폭**이다. 25 % 증가가 37.5 % 보다 원인을
읽기 쉽고, 효과가 확인되면 다음 회차에 0.6 으로 한 번 더 올리면 된다. 위험으로는
둘 다 안전하다 — 손 횡속도가 0.047 → 0.059(0.5) / 0.065(0.55) 인데 RPP 탈락선이
0.115 m/s 다. 모터 한계도 여유가 크다(knob 52 % 에서 1.04 rad/s).

**주의**: 유턴 폭과 무관하다(각도가 아니라 속도). 코너 속도가 0.20 → 0.25 m/s 가
되고 손 흔들림이 25 % 늘어나므로 사람 판정이 필요하다.

### 진행 순서 — **3 → 2 → 1** (2026-08-29 사용자 선택)

번호는 위험도 순으로 매겼지만 **실행은 역순으로 한다.** 3단계가 가장 안전하고
(값 하나, 손 흔들림이 탈락선의 절반), 1단계(Spin)가 가장 위험하다(253 밴드에서
회전하면 벽에 닿을 수 있다). 안전한 것부터 열어 효과를 보고, 남는 만큼만 위험을
쓴다. 3단계에서 코너가 빨라지면 갇히는 자리 자체가 줄 수도 있다.

### 진행 규칙

한 번에 하나. 매 단계 실주행 1 회 + bag 분석. 재는 지표는 위 기준선 표와 같다.
실패한 값은 되돌리고 근거를 그 자리 주석에 남긴다.

2·3단계가 모두 실패하면 다음 후보는 **B3(MPPI)** 다. RPP 와 달리 궤적 수천 개의
가중평균이라 '떨림 직결' 문제가 구조적으로 덜하다. 다만 CPU 가 관건이다.

---

## 8. NAV2-B8 — 화장실 구간 유턴 실패 (최우선)

> **2026-08-29 신설.** 그날 컨트롤러 축을 전부 시험하고 남은 유일한 문제다.

### 증상

사용자 보고: **"6월 30일 지도에서 화장실 구간에서 유턴을 할 때는 항상 실패한다."**
지도 `vica_map_0630`, 목표 **(3.5, 5.4)**.

### 왜 컨트롤러 문제가 아닌가

같은 날 세 컨트롤러와 다섯 축을 시험했고 전부 같은 벽에 부딪혔다.

    ① 좁은 곳에서 유턴하려 함
    ② 호를 그리다 금지구역 진입          <- 진짜 문제
    ③ planner 가 "Starting point in lethal space" 로 경로 거부
    ④ 로봇 정지. 사람이 손으로 꺼내야 함

**③ 에 걸리면 앞의 개선이 전부 무의미해진다.** 경로가 없으면 컨트롤러는 할 일이
없다. MPPI 는 제자리 회전을 4.1 s 까지 할 수 있게 됐는데도 돌라고 시킬 경로가
없어 못 갔다. ④ 를 푸는 유일한 수단이던 Spin 은 사람에게 위험해서 닫혔다(§11).

### 기하 (2026-08-29 계산)

| 회전 방식 | 필요 통로 폭 |
| --- | --- |
| 제자리 회전 | **0.945 m** |
| 호 (격자 R 0.5) | **1.705 m** |

사용자가 도는 자리는 약 1.3 m 다. **제자리로 돌면 되고 호로는 안 된다.**
그런데 planner 가 호를 그리므로 들어가서 갇힌다.

### 해야 할 일 (순서)

1. **`vica_map_0630` 에서 (3.5, 5.4) 주변 실제 통로 폭을 잰다.**
   `scripts/vica_corridor_measure.py` 가 free 셀마다 EDT 로 가장 가까운 벽까지의
   거리를 낸다. 0.945 m 가 나오는지 확인한다.
2. 폭이 되면 **유턴 위치를 미션에서 바꾼다** — 목적지·경유지를 넓은 쪽으로 옮기거나
   되돌 필요가 없는 순회 경로를 쓴다.
3. 폭이 안 되면 **그 자리에서 유턴하지 않는 것이 유일한 답이다.**

### 하지 말 것

`inflation_radius` 를 올려 진입을 막는 안은 여유가 10 cm 뿐이고(0.55 → 상한
0.65), 좁은 통로에서는 비용 0 인 중앙선이 사라져 오히려 갇힌다. §11 참고.

---

## 8-2. NAV2-B9 — 깊이 카메라를 costmap 에 (2D 스캔) — [x] 2026-08-30 통과

### 무엇을

D455 깊이의 **0.30~1.05 m 높이 띠**를 `pointcloud_to_laserscan` 으로 2D 스캔으로
눌러 `local_costmap` 의 두 번째 관측원으로 넣었다. 라이다(0.382 m 한 층)가 못 보는
책상 상판을 잡는 것이 목적이다.

### 왜 3D 가 아니라 2D 인가 — 이것이 이 항목의 핵심

처음에는 포인트클라우드를 그대로 `voxel_layer` 에 넣었다. 책상 다리는 잘 봤지만
**찍은 칸을 지우지 못했다.**

"지운다"는 것은 원점에서 관측점까지 선을 긋고 그 선이 지나는 칸을 비우는 일이다.
카메라(지면 1.025 m)보다 낮은 칸을 지우려면 그 선이 계속 내려가 **훨씬 먼 바닥**에
닿아야 한다.

```
필요한 광선 길이 R = d / (1 - h / 1.025)
  d 1.8 m · h 0.60 m -> 4.3 m       d 1.8 m · h 0.75 m -> 6.7 m
  d 1.8 m · h 0.90 m -> 9.8 m       (카메라 높이에 가까울수록 발산한다)
```

실내는 3~4 m 앞에 벽이 있어 그만한 바닥이 안 보인다. `raytrace_max_range` 를
2.5 -> 8.0 m 로 늘려도 볼 바닥이 없으면 소용이 없었다. **라이다가 잘 지워지는
이유는 자기와 같은 높이를 보기 때문이다.** 그래서 깊이도 한 평면으로 눌렀다.

### 실주행 판정 (`run0833` vs `run2017`)

| 지표 | 3D | **2D** | 목표 |
| --- | --- | --- | --- |
| 유령 수명 중앙값 | 16.2초 | **0.0초** | 2초 이내 |
| 10초 넘게 산 칸 | 56.6 % | **6.0 %** | 10 % 이하 |
| 30초 넘게 산 칸 | 25.3 % | **2.2 %** | — |
| 최대 | 191초 | 80.4초 | — |

사용자 보고: "책상 다리를 좀 더 잘 피하게 되어 부딪히거나 갇히는 현상이 많이 줄었다."

### 선행 조건 — 마스트 기울기 실측

URDF 가 수평이라고 믿는 동안 바닥점이 20~40 cm 떠서 계산돼 로봇 앞이 통째로
LETHAL 이 됐다. 마스트는 실제로 **위로 3.0도** 들려 있었다(`camera_pitch -0.0524`).

**마스트를 손대면 반드시 다시 재서 URDF 를 고친다.** 재는 법은 깊이 포인트클라우드
에서 바닥 평면을 RANSAC 으로 찾아 법선의 앞뒤 성분을 읽는 것이다(평면 두께 9 mm,
점 9,000개, 4회 3.04 +- 0.14도). 앞에 물건이 있어도 견딘다.
부호 주의: 측정기의 양수는 "URDF 보다 위로 들렸다"는 뜻이라 URDF 에는 뒤집어 넣는다.

### 되돌리는 법

```bash
ros2 param set /local_costmap/local_costmap voxel_layer.depth_scan.marking false
ros2 service call /local_costmap/clear_entirely_local_costmap nav2_msgs/srv/ClearEntireCostmap
```

재기동이 필요 없다. 카메라를 `run_d455.sh`(포인트클라우드 없음)로 띄워도 같다 —
스캔 토픽이 없으면 그 소스는 조용히 아무 일도 하지 않는다.

### 남은 약점

**앞만 본다(±43도).** 로봇이 회전하면 앞에 찍었던 것이 옆으로 밀려나고, 라이다는
층이 달라 대신 지워주지 못한다. local costmap(6 x 6 m) 밖으로 밀려날 때까지
남는다(0.5 m/s 로 약 6초). 8/29 bag 에서 문제는 앞쪽이었으므로(정면 24~26초 vs
뒤쪽 0.6초) 병목은 아니었다.

### 보류한 보완 (사용자 판정 2026-08-30)

한꺼번에 바꾸면 무엇이 효과였는지 못 가린다. 필요할 때 하나씩 연다.

- **해상도** — 지금 4배로 솎아 써서 3 m 거리의 4 cm 다리에 점이 1.4개뿐이다.
  3배면 1.9개(점 1.8배), 2배면 2.8개(점 4배). `run_d455_cloud.sh` 의
  `filter_magnitude`. 얇은 것(의자 다리)에 직접 듣는다.
- **낮은 띠 (10~30 cm)** — 라이다도 지금 카메라도 못 보는 사각지대(문턱·신발).
  스캔 노드를 하나 더 띄우고 **감지 거리를 1.6 m 로 묶어야 한다** — 바닥이
  2.1 m 부터 화면에 들어오기 때문이다.
- **local costmap 6 m -> 3 m** — 지나친 뒤 옆·뒤 자국이 6초 -> 3초.
- **`transform_tolerance` 0.05 -> 0.1** — 카메라 12~14 Hz 인데 스캔이 11.2 Hz 로
  조금씩 흘린다. TF 가 늦으면 그 프레임을 통째로 버린다.

### 계약 시험

`vica_nav2/test/test_depth_voxel_contract.py` 13개. 특히 `data_type` 이 LaserScan
인지(PointCloud2 로 되돌리면 유령이 재발한다), launch 와 costmap 의 값이 맞물리는지,
URDF 의 `camera_pitch` 가 계산과 일치하는지를 본다.

---

## 9. 부수 과제 — 근거를 채워야 할 값들

2026-08-06 점검에서 **근거 주석이 전혀 없음**을 확인하고 `[근거 없음]` 표식만 달아
둔 값들이다. 값은 그대로다. 실주행에서 관련 증상이 나오면 여기서 후보를 찾는다.

| ID | 값 | 메모 |
| --- | --- | --- |
| C1 | `local_costmap 6×6` (정석 3×3) | 셀 수가 정석의 4배. CPU 여유를 볼 때 후보 |
| C2 | 센서 범위 4종 (`obstacle/raytrace × min/max`) | `raytrace > obstacle` 관계만 지키면 된다 |
| C3 | `mark_threshold: 1` (정석 0) | 라이다가 단일 평면이라 올리면 표시가 사라질 수 있다 |
| C4 | `expected_update_rate: 0.2`, `inf_is_valid: true` | stale 시 Nav2 가 멈추는지 자체가 `[미검증]` |
| C5 | ~~`rotation_penalty`~~ | **2026-08-29 시험 완료. 무효였다(§11).** 5.0/2.0/1.0 어느 값도 거동을 바꾸지 못했다 |
| C6 | `cost_scaling_factor: 3.5` | 이 값을 전제로 계산한 비용표가 여러 곳에 있다. 바꾸면 함께 재계산 |
| C7 | `planner tolerance: 0.2` (정석 0.5) | 줄이면 "경로 없음"이 늘고 키우면 목표에서 먼 곳에 도착 |
| C8 | Safety `max_linear_mps 1.0` / `max_angular_radps 2.0` | 실주행 상한(0.26/0.4)의 3.8~5배. 폭주만 막고 일상 제한은 못 함 |

## 10. 원인이 규명되지 않은 것

| ID | 항목 | 출처 |
| --- | --- | --- |
| D1 | **253/254 비대칭** — planner 는 253에서 충돌, DWB 는 254에서만. "fork 만이 근본 해결" | `devlog/2026-07-30.md:246` |
| D2 | **추종 오차 12 cm** — CAN·드라이버 300 ms 지연. 파라미터로 접근 불가 | `docs/nav2_tuning_log_2026-07-28.md:59-72` |
| D3 | **유령 장애물 p95 43~46 s** — 대책은 넣었으나 실기 시험 미실시 | `devlog/2026-07-30-nvblox-ghost-obstacle.md` |
| D4 | **정지 상태 AMCL 점프** — 정지 중 15 cm / 10.8° 재정렬. 빈도·크기 미측정 | `devlog/2026-07-30.md:255-257` |
| D5 | **nvblox 진행 방향 미관측** — 전체 68~89 % 는 정상이나 진행 방향 ±30° 부채꼴은 미측정 | `devlog/2026-08-02-주행테스트.md:452` |
| D6 | **실물 각속도 배율 미측정** — 명령 대비 실측 배율을 잰 적이 없다. 선속도만 검증됨 | `devlog/2026-08-06-시뮬-회전결손-근거.md` §4-1 |
| D7 | `nav2_constrained_smoother` (곡률 제약 smoother) — 설치돼 있으나 **검토 기록조차 없다** | 2026-08-06 점검 |

## 11. 하지 말 것 — 닫힌 축

다시 제안하지 않는다. 이유가 실측으로 남아 있다.

| | 이유 |
| --- | --- |
| `SmoothPath` 추가 | 두 번 실패(07-28, 08-02). 곡률 제약이 없어 경로를 벽 쪽으로 당긴다 |
| `RotationShim` | 두 번 실패. 제자리 회전에 1.31 m 필요한데 통로가 1.0 m라 **전제가 성립하지 않는다** |
| `padding` 0.05 미만 | 0.04 는 DWB 궤적 전멸, 0.03 은 계산 폭증 후 충돌 |
| lattice 회전반경 1 m | 진입 여유 부족·직진에서도 흔들림 |
| `inflation` 0.50 이하 | 유턴·벽 근처에서 이용자 위험(사용자 판정) |
| 후방 Collision Monitor | 사용자가 핸들을 잡고 뒤에 서므로 상시 발동한다 |
| `BackUp` 복구 동작 | 뒤에 사람이 있다. 2026-07-30 에 실제로 30 cm 후진했다 |
| `Spin` 복구 동작 | 2026-08-12 제거. 253 밴드에서 후방 꼭짓점이 0.651 m 를 쓸어 실제로 핸들이 의자에 부딪혔고, 시뮬 12패스에서 27.6 s 를 쓰면서 순 회전은 +17.2도뿐이었다 |
| 좁은 틈을 `inflation`·nvblox 조정으로 통과 | 통과 여부는 inscribed(footprint 유래 **0.555 m**)가 정하며 `inflation_radius` 와 무관하다. 통로 1.0 m 에 사람이 서면 남는 0.55 m 로는 어떤 설정으로도 못 지나간다. 2026-08-12 시뮬 12패스 실측 |
| planner `rotation_penalty` 조정 | 2026-08-29 5.0/2.0/1.0 실주행. 회전을 정하는 것은 planner 가 아니다 — DWB 가 자기 점수표로 다시 고른다. 내릴수록 처방만 늘어 '갈 길은 있는데 멈춘 시간'이 15.8 -> 67.1 s 로 3배가 됐다 |
| **RPP 컨트롤러** | 2026-08-29 실주행 2회. 로봇 성적은 압도적(금지구역 206 -> 4)이나 **손잡이가 받는 횡속도가 2.4배**(0.047 -> 0.115 m/s). 사용자 판정 "부상을 입을 정도". 설정·시험은 `nav2/rpp3` 에 보존 |
| **B4** global `nvblox_layer` 재투입 | 구현하지 않는다(사용자 판정). 조사는 §4 에 남아 있다 |
| **B6** 손잡이 지지대 각도 (하드웨어) | 구현하지 않는다(사용자 판정). 조사는 §6 에 남아 있다 |

## 12. 완료

| 날짜 | 항목 |
| --- | --- |
| 2026-08-06 | 죽은 `ekf_config/ekf.yaml` 삭제 (미참조 + 옛 버그값 `sensor_timeout 0.1`) |
| 2026-08-06 | 주석-코드 어긋남 6건 수정 (planner 헤더 뒤바뀜, inflation 0.50, 외접반경 수치, BT 복구 각도 등) |
| 2026-08-06 | 근거 없는 값 8종에 `[근거 없음]` 표식 (§9) |
