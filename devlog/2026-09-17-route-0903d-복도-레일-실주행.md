# 0903_d 긴 복도 레일 실주행 — 관찰·측정 항목 (2026-09-17)

`vica_map_0903_d`(67 m 일자 복도)에 나무형 레일을 깔고 레일 7판(`0410a56`)으로 처음 달린다.
0630 보고서(`devlog/2026-09-17-route-server-레일주행-1~4판.md`, 아티팩트 "VICA 레일 주행 보고서")
§6-7 "다음 실주행은 0903 지도. 첫 회차는 표본기·bag 을 걸고 짧게"의 실행 기록이다.
결과는 맨 아래 §5 에 실주행 뒤 채운다.

## 1. 이번 회차의 구성 (0630 과 다른 것)

| 항목 | 0630 (run13) | 0903_d (이번) |
| --- | --- | --- |
| 레일 모양 | 고리(진입점 각도순, 74노드) | **나무**(등뼈 407호↔테스트1 42.7 m + 화장실 곁가지 6.1 m, 60노드·무방향 엣지 60) |
| 접점 | 없음(고리) | 화장실 곁가지 1곳, 등뼈 양쪽 호 두 개로 **Y 자**(작은 고리 1개가 정상) |
| 목적지와 레일 | 전부 레일 위(0.03 m 안) | 테스트2·3·홈은 **등뼈 옆 0.5~0.75 m, 곁가지 없음**(BT 인계 2 m 안이라 토막이 쓸모 없음). 마지막은 planner |
| 자 | 회색 잡티를 벽으로 셈 | 검은 칸 없는 ≤40칸 회색 섬은 자유(Nav2 도 미지 칸을 안 부풀림). 0903_d 128개 |
| 금지구역 | 무효(전 구역 128) | **유효** — x 30.5~38.0, y −8.5~−4.0(테스트3~홈 남쪽 방). 레일은 밖 |
| 벽 여유 | 최소 0.5 m | 검은 벽 기준 최소 0.90 m(화장실 곁가지) |
| 목적지 | 8곳 | 6곳(407호·화장실·테스트1·2·3·홈). **테스트_끝은 12:50 에 파일에서 사라짐** |

생성기 `scripts/vica_route_graph.py --tree`(미커밋). 레일 파일 `maps/vica_map_0903_d_route.{geojson,png}`(미추적).
BT·params 는 7판 커밋본과 설치본이 같다(14:45 빌드).

## 2. 기동 절차 (지도가 갈리는 곳이 세 군데다)

1. `python3 scripts/vica_map_pick.py --set vica_map_0903_d --yes` — CURRENT_MAP.
2. ⑨ nav2 칸·미션 칸 Ctrl+C → `ps` 로 `nav2_map_test|mission_manager|ekf_node|encoder_feedback` 이 사라졌는지.
   남아 있으면 오도메트리가 이중으로 뜬다(EKF·encoder 정상 = 각 1개).
3. 두 칸에서 `export VICA_MAP_ID=vica_map_0903_d VICA_MAP_YAML=$HOME/VICA-smarthandle/vica_ros2_ws/maps/vica_map_0903_d.yaml`
   (터미네이터 rc 가 잡아둔 0630 을 덮어쓴다. 안 하면 mission·initpose·goto 가 옛 지도를 본다).
4. nav2: `ros2 launch vica_nav2 nav2_map_test.launch.py map:=$VICA_MAP_YAML use_route:=true` — 칸 기본 명령엔 `use_route` 가 없다.
5. mission: `ros2 launch vica_mission_manager mission_manager.launch.py map_id:=$VICA_MAP_ID map_yaml:=$VICA_MAP_YAML`
6. 초기 위치 `bash scripts/vica_set_initial_pose.sh 테스트1`(또는 x y yaw). 목적지는 `bash scripts/vica_goto.sh` 번호.

## 3. 기동 전·후 확인

- [ ] `git -C vica_ros2_ws status --short` — params/BT 가 되돌려지지 않았는지(09-16 21:13 사고).
- [ ] **OS 표본기를 nav2 보다 먼저** 켠다(아티팩트 §6-3). 5 % 이상 프로세스 전수, 2 s.
- [ ] nav2 로그 `[route] 레일을 적용한다: …vica_map_0903_d_route.geojson` / `[keepout] 마스크를 적용한다: …0903_d_keepout.yaml`.
      "레일 파일이 없다"면 레일 없이 조용히 돈다. `ros2 param get /route_server graph_filepath` 로 재확인.
- [ ] Foxglove·RViz `/route_graph` 마커: 복도 한가운데 한 줄 + 화장실 곁가지. 두 겹이면 옛 파일.
- [ ] AMCL 초기 위치 뒤 라이다가 벽에 붙는지. 안 찍으면 global_costmap activating [13] 정지(고장 아님).
- [ ] E-stop: 재기동으로 통신 원인 래치가 걸리면 정지 중엔 자동 복구. 안 풀리면 앱 reset.
- [ ] teleop 칸 꺼짐, 모터 knob 0 아님.
- [ ] **출발 전 CPU 50 % 아래**(run12·13 기동 후 3~16분 고부하). 그 사이 `ros2 node list`·`/dev/shm` 목록·tailscale0 을 남긴다.
- [ ] bag: `bash scripts/vica_drive_record.sh route0917_run15`(사전 점검 포함). 기록 중엔 bag 을 읽지 않는다.

## 4. 볼 것

### 4.1 주행 중 (사람 눈)

1. **긴 직선(테스트1↔407호 42 m)**: 1 m 노드에서 멈칫·감속 없이 고른 속도인가. 당근이 3 m 앞으로 매초 옮겨지는 구조라 직선에서 가장 잘 드러난다.
2. **곁가지 없는 목적지(테스트2·3·홈)**: 2 m 앞에서 레일을 버리고 목적지 자체에 서는가. 레일 노드에 서서 도착 처리되면 안 된다.
3. **화장실 Y 접점**: 들어갈 때·나올 때 호를 타고 부드러운가, 손잡이 좌우 왕복이 없는가. 6판 사고처럼 등뼈 반대편 호로 잘못 드는 일이 없는가.
4. **복도 한복판 재출발**: 세웠다가 다음 목적지를 주면 뒤로 가거나 서지 않는가(1판 "1 nodes" 사고의 재발 여부).
5. **레일 위 사람**: 돌아 나와 레일로 복귀하는가(5판 당근의 목적). 막힌 복도에서 틈으로 파고드는가(§6-2 대기 정책 미구현).
6. **등뼈 끝 U턴(테스트1·407호)**: 한 번에 도는가, 좌/우 임의 선택으로 휩쓸리는가(run12 화장실 출발과 같은 현상).
7. **도착 뒤 한 바퀴 더**(run10 31 s) 재발 여부, **도착 방향**(6판 사고 +79~96° 재발 여부 — 7판 첫 새 지도 검증).
8. **금지구역 경계(테스트3~홈 남쪽)**: 침범 0, 그 근처에서 이유 없는 감속·정지 없음.

### 4.2 bag 에서 잴 것 (아티팩트 §3·§4 의 잣대 그대로 + 나무형 항목)

| 잣대 | 0630 run13 기준 | 어디서 |
| --- | --- | --- |
| 완주 / 손 개입 | 15/15 · 0 | `/rosout` 미션 흐름, `/navigate_to_pose/_action/status` |
| `Received plan with zero length` | 0 | `/rosout` |
| NaN 경로 / `Aborting handle` | 0 / 38 | `/plan`, `/rosout` |
| `Starting point in lethal` | 27 | `/rosout` |
| 당근 planner 실패(`no valid path`) → 레일 후퇴 횟수 | (run12 에서 처음 셈) | `/rosout` |
| 0.25 m 안 재계획 횟수 | 6~12 | `/plan` vs 목적지 거리 |
| 도착 xy / yaw (odom + map→odom 합성, /tf 끊김 대비) | 0.07~0.23 m / ≤12° | `/odom`, `/tf` |
| 도착 정렬 시간 | 1.2~7.7 s | 도착 반경 진입 → 성공 |
| 구간 이동 시간 | 화장실→안내소 36 s | 미션 로그 |
| 벽여유 <0.45 m 비율(0.2 s 표본, 정적 지도) | 11.2 % | `/tf` + 지도 |
| 레일 이탈 1.5 m 초과 틱(0630 의 "휴게실 경유"에 해당) | 0 | `/plan` 첫 점 vs 레일 노드 0.06 m |
| CPU: cpu_percent ekf·imu_adapter·controller, load | 정상 ekf 4 %·controller 17 % / 고부하 33 % | `/diagnostics_agg`, 표본기 |
| `/tf` 끊김(>0.4 s)·액션 ack 타임아웃·controller 주기 미달 | 정상 회차 12~19 | bag 시각차, `/rosout` |
| route 계산 시간(60노드) | 0.4~1.1 ms(42~74노드) | route_server 로그 시각차 |
| **직선 속도 균일성**: 테스트1↔407호에서 `/cmd_vel_safe` v 의 표준편차, 1 m 노드 통과마다 감속 유무 | (신규) | `/cmd_vel_safe`, `/tf` |
| **곁가지 없는 목적지**: 인계(거름망 near-goal 거부) 시각 → 도착까지, 레일 노드 정지 0 | (신규) | `/rosout`, `/plan` |
| **Y 접점** 통과 시 w 표준편차·부호 반전 횟수 | 0630 코너 0.18 | `/cmd_vel_safe` |
| **U턴** 소요·방향, `/speed_limit` 사다리 정상 작동 | — | `/cmd_vel_safe`, `/speed_limit` |
| 금지구역 안 발자국 표본 | 0 | `/tf` + keepout 마스크 |

## 5. 결과 — run15 (16:46~16:53, bag `route0917_run15`, 7판 `0410a56`, 나무형 레일 60노드)

분석 산출물: `~/vica_data/analysis/run15/` (스크립트 2개·그림 2장·중간 데이터). 표본기는 nav2 기동 전
16:36 부터. goal 1 은 nav2 기동 45 s 뒤에 출발해 기동 후 고부하 구간을 탔다(§5.4).

### 5.1 개요

| 잣대 | run15 (0903_d) | 0630 run13 | 판정 |
| --- | --- | --- | --- |
| 완주 / 손 개입 | **9/9 · 0** | 15/15 · 0 | 통과 |
| 도착 xy / yaw | 0.06~0.26 m / ≤8.2° | 0.07~0.23 / ≤12° | 통과 |
| 도착 정렬 | 1.7~10.9 s (홈 10.9·테스트1 10.1 = 180° U턴 도착) | 1.2~7.7 s | 보통 |
| zero length / NaN / abort | 0 / 0 / 6 (= planner 출발점 치명 6) | 0 / 0 / 38 | 통과 |
| Starting point in lethal | 6 (전부 사람 옆에 붙었을 때) | 27 | 통과 |
| Control loop missed | 53 (**전부 goal 1**, 기동 후 CPU 60~98 %) · goal 2~9 는 0 | 12~19 | 기동 시점 문제 |
| 벽 여유 <0.45 m 비율 | **0.1 %** (평균 1.29 m) | 11.2 % | 복도가 넓어 비교 무의미 |
| 레일 이탈 | 평균 0.19 m · >0.5 m 9.9 % · >1.5 m 0.4 % | — | 접점·회피에서만 |
| "1 nodes" 응답 | 64회, **전부 목적지 2 m 안** | — | 설계대로(마지막 구간) |
| 이동 중 v | 평균 0.44 · 최대 0.58 m/s | — | 0.58 > DWB 상한 0.5 [미검증] |
| 사람 회피 | 정지 2 s·8 s, 사람이 비켜야 진행 | run12 30 s 정지 | **실패** |
| 긴 직선 42 m | 흔들림 없음(w 평균 0.10) | — | 통과 |
| 금지구역 침범 | 0 | — | 통과 |

### 5.2 문제 1 — 코너를 끊어 돈다 (사용자 체감 1·3)

**증거.** 출발 U턴(180°)을 뺀 코너 창 5개(46~117°, 8~11 s)에서 w 명령이 **4~9번 켜졌다 꺼지고**, 사이에
0.2 s 넘는 직선 틈이 4~5번, **부호 반전 3~9번**. 그동안 v 는 0.50 그대로다 — "서서 도는" 게 아니라
"달리면서 조향을 펄스로 준다". 새 plan(1.05 s 간격) 이 발행되는 순간마다 w 가 바뀐다(그림 `run15_corner_w.png`).
**정정(09-17 저녁)**: 처음엔 "plan 첫 방향이 ±20~40° 로 번갈아 튄다"고 썼는데, `/plan` 에 route_server 의
레일 경로(245개, 첫 점 = 가까운 노드)와 planner 경로(346개, 첫 점 = 로봇)가 섞여 있어 그 교대가 나온
것이었다. planner 경로만 고르면 코너에서 첫 방향은 로봇 방향에서 **한쪽으로 10~30° 처진다**(직선 구간은
±2°, 코너 창 부호 교대 0~1회). 즉 매초 오는 새 경로가 "지금 방향에서 R 0.10 급으로 꺾었다가 직진" 모양이라
DWB 가 허용 최대 w 로 한 번 꺾고 직진하고, 다음 plan 이 오면 다시 꺾는다. 그림의 세로선-펄스 동기는
그대로 유효하다. 접점(화장실 Y) 통과 때 레일 이탈 최대 0.81~0.99 m — 레일에 굽어 둔 호(R 0.48)는
쓰이지 않는다. FollowPath 가 받은 경로("Passing new path" 298회)는 planner 경로 수(346)와 맞고, 레일
경로는 blackboard 에만 간다 → **0903_d 에서도 코너는 자유주행 planner 가 그렸다**(사용자 질문에 답).

**원인 후보.**
- A1 (구조, 유력) 5판 당근 방식: 매초 lattice planner 가 **로봇 현재 방향을 16칸(22.5°)으로 양자화**한
  시작 자세에서 3 m 앞 당근까지 새 경로를 그린다. 로봇이 두 칸 경계 근처를 달리면 시작 방향이 매초
  다른 칸으로 잡혀 경로 첫 구간이 좌·우로 번갈아 꺾이고, DWB(PathAlign 48·forward_point 0.325) 가
  그 새 경로에 맞추느라 w 를 껐다 켠다. 공식 문서: 기본 격자는 "16 bin, 0.5 m turning radius".
- A2 격자·벌점: 자작 격자 R 0.10·프리미티브 ≤0.4 m, `change_penalty` 기본 0.05("motion is changing
  directions (L to R)")·`non_straight_penalty` 기본 1.05 → 좌우를 바꾸는 값이 거의 공짜라 "꺾고-직진-반대로
  꺾고" 모양이 가장 싸다. `smooth_path` 는 문서대로 "simple and quick smoothing" 이라 22.5° 꺾임을 못 지운다.
- A3 DWB: PathAlign 48·GoalAlign 36(기본 32·24), forward_point 0.325(기본 0.1) — 경로 방향에 아주 강하게
  붙는다. 경로가 매초 바뀌니 그대로 펄스가 된다.
- A4 레일 호를 안 탄다: 당근 방식에서는 레일 기하(fillet·Y 호)가 당근 위치에만 쓰이고 controller 는
  planner 경로를 따른다. 3판(fillet)의 효과가 5판부터 사라진 셈.

**해결안(우선순위).**
1. **앞 3 m 레일 조각이 비어 있으면 레일 경로를 그대로 따르고, 막혔을 때만 당근 planner**(README 1번+2번
   혼합, 아티팩트 §6-1 제안과 동일). 4판(레일 직접)은 코너 w 표준편차 0.18·파고들기 0 이었다. 레일의 호가
   되살아나고 1 Hz 재계획이 사라진다. README 1번은 "Smoother Server 나 spline 을 route 와 controller 사이에"
   권한다 — simple_smoother 가 설정만 있고 BT 에 없다.
2. 격자 벌점 한 번에 하나: `change_penalty` 0.05→0.5, 다음 `non_straight_penalty` 1.05→1.2. 좌우 교대 억제.
   (A1 의 양자화 자체는 못 없앤다.)
3. [차후, 사용자 결정 09-17] controller 를 RPP(`nav2/rpp2`)로: "exact path following … without any dynamic
   obstacle avoidance" 라 회피를 planner(당근)가 맡는 구조와 짝이 맞는다. 지금 회차에는 넣지 않는다.
4. 32칸 격자 재생성은 CPU·계획 시간 재측정이 먼저(지금 route 0.4 ms, planner 최대 1.0 s 제한).

**"해결안 1 은 4판으로 되돌리기인가?"(사용자 질문) — 아니다.** 4판 = 깨끗하면 레일 직접(좋은 반쪽) + 막히면
최종 목적지까지 자유주행(배회를 만든 반쪽). 5판 = 항상 당근. 제안 = 깨끗하면 4판 반쪽, 막히면 5판 반쪽.
배회의 원인(최종 목적지까지 자유주행)은 돌아오지 않는다. 4판에서 유령 띠가 3 m 조각 검사를 매초 떨어뜨린
사건은 이 구조에서 레일↔당근이 갈리는 것으로 바뀌고 둘 다 같은 3 m 앞 점으로 가므로 멀리 새지 않는다.

### 5.3 문제 2 — 사람 반응이 늦고, 너무 가까이서 서고, 못 빠져나간다 (사용자 체감 2)

**증거**(그림 `run15_person.png`). 사람 사건 4건(goal 8 x≈26.8·17, goal 9 x≈6.4·11).
- 전방 ±20° 안에 사람이 처음 잡힌 거리 **1.97 / 1.66 m**(옆에서 들어옴). plan 이 레일에서 0.4 m 넘게
  벗어난 첫 시각은 그로부터 **+3.0 / +3.5 s, 그때 전방 1.25 / 1.16 m**. 벗어난 폭도 0.5~0.75 m.
- collision_monitor 40 % 감속 at 1.04 / 0.88 m → **정지 폴리곤 at 0.59~0.73 m** → 정지 2 s / 8 s. 사람이
  비킨 뒤("continue normal") 에야 진행. planner `no valid path` **0회** — 길은 늘 있었고, planner 는 사람
  옆 0.3 m 로 지나가는 경로를 그렸다. 사람 옆에 붙자 `Starting point in lethal` 6회, `No valid
  trajectories` 3회.
- 재출발 뒤 좌우 스윙: 레일 이탈 0.57 m, plan 첫 방향 ±40° 교대(문제 1 과 같은 기작).

**원인 후보.**
- B1 (유력) 감지 지평: global obstacle_layer `obstacle_max_range` **3.0 m**(문서 기본 2.5), 갱신 1 Hz, plan
  1 Hz → 사람은 3 m 안에서만 costmap 에 적히고 최대 2 s 뒤에 경로가 바뀐다. 0.5 m/s 면 1 m 를 더 간 뒤다.
  **측정된 유효 반응 거리 ≈ 1.2 m.** 1 m 옆으로 비키려면 2~3 m 의 직진 거리가 필요하므로 이미 늦다.
- B2 사람 = 다리 2점: inflation 0.55 라 치명 반경은 내접 0.2 뿐. planner 는 `cost_penalty` 4.0 이어도 사람
  옆 0.3 m 로 지나간다(회피가 아니라 스침). 실제 제동은 collision_monitor 폴리곤이다.
- B3 DWB 저울: ObstacleFootprint 0.15 vs PathAlign 48 — 경로를 벗어나느니 감속·정지. 문서(tuning guide)는
  DWB 가 "diverge from the path if there are dynamic obstacles present" 하도록 critics 를 맞추라 한다.
- B4 당근이 사람 뒤/위에 놓인다: 3 m 앞 당근이 사람 자리와 겹치면 경로는 사람을 지나야 한다 → 사람이
  비켜야 진행. README 2번의 "leave when blocked … proactively when it sees blocked in immediate future" 가
  지금 트리에는 없다.
- B5 이번 회차 nvblox 미기동(라이다만). 사람 상반신을 못 본다.

**해결안(우선순위, 한 번에 하나).**
1. **global `obstacle_max_range` 3.0→6.0, `raytrace_max_range` 3.5→8**(8 은 이미 근거 조사·승인된 축).
   반응 거리가 2배 이상 늘어난다. 1 Hz 갱신 비용은 720빔이라 미미.
2. **당근을 장애물 너머로**: 앞 3 m 조각에 치명·부풀림 칸이 있으면 당근을 그 너머 첫 빈 레일 점으로
   옮기거나 `ComputePathThroughPoses` 로 다음 노드 2개를 지나게 한다. 사람을 "지나가야 할 점"이 아니라
   "돌아갈 물체"로 만든다.
3. ~~DWB `ObstacleFootprint.scale` 상향~~ — **닫힌 축.** 07-29 0.3/0.4/0.5(좁은 곳 갇힘), 07-30 0.25(lethal
   28회)·0.5(DWB 무효 133회), 08-02 0.6(궤적 전멸 152회·성공 3/실패 2). params 주석: "파고들기(0.15)와
   멈춰서기(0.6) 사이에 앉힐 값이 없다". 게다가 DWB 지평은 시간(sim_time 2.0×0.5+0.355 ≈ 1.4 m)이라 3 m
   앞 사람은 어차피 planner(당근) 몫이다(08-02 devlog). B3 는 원인으로 남기되 해결안에서 뺀다.
4. 그래도 막히면 **기다리기 정책**(아티팩트 §6-2). 사람이 비키는 데 2~8 s 걸렸다.
5. global costmap `update_frequency` 1→2 Hz 는 CPU 를 재고 나서.

**감지 범위 확대의 이력(사용자 질문에 답, 09-17 확인).** 둘 다 **실주행 적용 0회**. 07-31 devlog §6.1 이
3.0→5.0(+raytrace 5.5)의 비용만 계산했다: 마킹 점 1.46배·레이트레이스 셀 1.19배·갱신 영역 2.78배,
"inflation 0.35→0.50 에서 셀당 작업이 2배 늘어도 주기가 안 변했으므로 주기를 못 움직일 가능성이 높다".
규칙 둘: raytrace > obstacle(안 지키면 유령 마크) → 6/8 만족. params 주석 "global 네 값은 local 과 같아야
한다(다르면 planner 가 통과 가능하다고 본 경로를 controller 가 거부)" 는 global 이 local 보다 **덜** 볼 때의
위험이다. local 은 6×6 창이라 3 m 가 물리 한계이고 global 만 넓히는 것은 planner 가 더 보는 쪽이라 그
위험이 없다 — 대신 주석을 함께 고친다. 08-02 "DWB 는 시간을 내다보므로 obstacle_max_range 단독은 효과
없음" 은 맞고, 이번엔 DWB 가 아니라 당근 planner(global costmap 을 보는 쪽)가 회피 경로를 그리므로 효과가
나는 쪽이다. 새 부작용 후보: 6 m 밖 유리창 반사 잡점이 멀리서 찍힐 수 있다(0903_d 지도에 광선 잡티 많음)
→ raytrace 8 이 다음 스캔에서 지운다. 적용 후 `Starting point in lethal`·레일 이탈·주기 미달·CPU 로 확인.

### 5.4 그 밖에 본 것

- **기동 후 고부하 중 출발**: goal 1 은 CPU 60 %(최대 98 %)·주기 미달 53회. goal 2~9 는 46~52 %·0회.
  아티팩트 §6-3 "50 % 아래 확인 후 출발" 이 맞았다. 이번 표본기 파일 `usage_20260917_163637.jsonl` 에
  기동 전후가 다 있다(상위 12 프로세스).
- **pose_bootstrap_node 가 13 % CPU 로 goal 7 까지 살아 있었다.** 초기 위치를 CLI 로 넣으면 앱 commit 이
  없어 스캔 구독을 안 끊는다. CLI 로 찍은 날은 노드를 끄거나, 노드가 AMCL 수렴을 보고 스스로 해제하게.
- bag 기록기 21~25 % (시험용 비용, 아티팩트와 같음).
- 도착 정렬 10 s 는 목적지 yaw 가 접근 방향과 반대(홈·테스트1 yaw 0, 동쪽에서 접근)인 곳. 등록 yaw 를
  접근 방향으로 두거나 감수.
- `_route.png` 가 maps/ 에 있어 keepout 노드가 `vica_map_0903_d_route.yaml` 을 찾다 실패 5회(0630 과 같음).
- 명령 최대 0.58 m/s > DWB max_vel_x 0.5 — 어디서 넘는지 [미검증].
- 출발 U턴(180°)은 1~2 버스트·|w| 0.42~0.47·8 s 로 부드럽다(제자리 회전이라 plan 양자화 영향이 없다).

### 5.5 다음 순서(§6 에서 갱신)

1. 5.2-1(레일 직접 + 막힐 때만 당근) 을 다른 세션의 §6-1 과 합쳐 8판으로. 판정 잣대: 코너 창 w 버스트 ≤2,
   부호 반전 0, 접점 레일 이탈 ≤0.3 m.
2. 5.3-1(감지 지평 6 m) 하나만 바꿔 같은 사람 시험. 잣대: plan 이탈 시작 거리 ≥3 m, 정지 폴리곤 0회.
3. 기동 후 CPU 50 % 아래 확인 후 출발(표본기 값), pose_bootstrap 정리.

## 6. run16(레일 7판·표시 6 m) · run17(기존 planner·표시 6 m) — 세 회차 비교

바꾼 것은 하나, global scan `obstacle_max_range` 3.0→6.0 · `raytrace_max_range` 3.5→8.0(run16 부터). run17 은
같은 값에서 `use_route` 없이 기존 planner(Lattice 가 최종 목적지까지, 표준 BT 1 Hz 재계획)로 같은 코스.
전체 표는 `~/vica_data/analysis/compare0917/compare.md`, 그림 `compare_person.png`·`compare_corner.png`,
분석기 `analyze_run.py`(회차 공용)·`compare_runs.py`.

| 잣대 | run15 레일·3 m | run16 레일·6 m | run17 planner·6 m |
| --- | --- | --- | --- |
| 완주 | 9/9 | 5/5 | 6/6 |
| 도착 xy / yaw 최대 | 0.26 m / 8.2° | 0.23 / 9.5° | 0.22 / 11.4° |
| Control loop missed | 53(goal 1) | 59(goal 1, 기동 113 s 뒤 출발) | 0 |
| Starting point in lethal / planner 실패 | 6 / 6 | 2 / 5 | **12 / 18** |
| Wait 복구 / costmap 클리어 | 4 / 5 | 4 / 5 | **53 / 19** |
| 벽 여유 <0.45 m | 0.1 % | 0.0 % | 0.8 % |
| 레일선 이탈 >0.5 m | 9.9 % | 12.0 % | **51.7 %** |
| 코너 w 버스트 / 부호 반전 / 직선 틈 (평균, U턴 제외) | 6.0 / 6.3 / 4.5 | 4.0 / 5.0 / 4.0 | 3.2 / 3.0 / 3.0 |
| 코너 레일이탈 최대 | 0.74 m | 0.95 | **1.33** |
| 사람 사건(지도에 없는 전방 물체) | 7 | 8 | 7 |
| 첫 포착 거리 평균 | 1.84 m | 1.50 | 1.77 |
| **경로 이탈 시작 거리 평균** | 1.07 m | **1.68** | **1.77** |
| 최근접 거리 평균 | 1.23 m | 0.88 | 0.75 |
| **횡 이탈 최대 평균** | 0.33 m | 0.27 | **0.62** |
| 정지 시간 합 | 14.7 s | 17.0 | 17.2 |
| 사건당 손실 시간 | 3.3 s | 3.2 | 2.7 |

### 6.1 카메라(사용자 질문)
`/camera/depth_scan` 이 세 회차 모두 13~15 Hz 로 담겼고 realsense 노드는 1시간 30분째 살아 있다(CPU 10 %).
local costmap 의 depth_scan 소스는 정상. 없던 것은 nvblox 3D 층이며 세 회차 공통이라 비교는 공정하다.

### 6.2 코너 — 레일 탓이 아니다
표시 범위만 바꿨는데 코너 버스트 6.0→4.0·반전 6.3→5.0(사용자 체감 "완화"와 일치). 후보 원인은 raytrace 8 이
옛 마크를 지워 costmap 잡음이 줄고 planner 경로가 안정된 것이나 표본 3개라 확정은 못 한다. **기존 planner
도 3.2/3.0/3.0 으로 같은 끊김**이 난다 — 끊어 돌기는 lattice(16칸·R 0.10)+1 Hz 재계획+DWB 조합의 성질이지
레일의 성질이 아니다. 대신 기존 planner 는 코너에서 복도선을 1.33 m 벗어나 넓게 돈다. 레일 모드에서
재계획을 없앨 수 있는 것은 8판(앞 3 m 레일이 비면 레일 직접)뿐이다.

### 6.3 사람 — 당근 지평이 회피 폭을 묶는다
- **시험 방식의 한계**: 첫 포착이 세 회차 모두 1.5~1.8 m. 사람이 옆에서 2 m 안으로 들어왔다. 0.4 m/s 로 옆으로
  1 m 비키려면 2~3 m 의 직진이 필요하므로 어떤 설정도 이 조건에선 선다. 6 m 로 넓힌 효과는 "보였을 때"만
  나온다: 3 m 밖에서 보인 2건(run16 17:54:01 2.9 m, run17 18:02:08 3.1 m)은 **정지 없이 통과**했고, 레일의
  경로 이탈 시작 거리는 1.07→1.68 m 로 늘었다.
- **레일(당근) vs 기존 planner**: 횡 이탈 0.27 vs **0.62 m**. 당근이 "3 m 앞 레일 위 고정점"이라 planner 는
  사람을 비켜도 곧 그 점으로 되돌아오는 좁은 S 를 그리고, DWB 가 그 좁은 틈을 못 지나 collision_monitor
  가 세운다. 기존 planner 는 목적지가 멀어 복도 폭을 다 써 넓게 돌아간다 — 사용자 체감 "planner 가 좀 더
  피한다"와 일치. 단 정지 합은 같고(17 s), 기존 planner 는 벽·사람 옆에서 출발점 치명 12·planner 실패 18·
  Wait 53 으로 복구 루틴에 자주 빠졌으며 복도선을 절반 넘게 벗어났다(안내 로봇으로선 사람이 따라오기 나쁨).
- **결론**: 레일 모드의 회피 한계는 감지 지평이 아니라 **당근이 레일 위 3 m 고정점**인 것이다. 5.3-2(3 m 조각에
  치명·부풀림 칸이 있으면 당근을 장애물 너머 첫 빈 점으로, 또는 `ComputePathThroughPoses` 로 다음 노드 2개)가
  핵심 수리다. 표시 6 m 는 유지(효과 확인·부작용 없음: 주기 미달은 goal 1 기동 고부하뿐, CPU 45~51 %).

### 6.4 다음 순서(갱신)
1. **당근을 장애물 너머로**(5.3-2) — 레일 모드 회피 폭을 기존 planner 수준으로. 판정: 횡 이탈 ≥0.6 m·정지 폴리곤 0.
2. **8판 레일 직접 follow**(5.2-1) — 코너 끊김. 판정: 버스트 ≤2·반전 0.
3. 사람 시험을 둘로 나눈다: (a) 4~5 m 앞에서 등장 → "보이면 피하는가", (b) 2 m 안에서 튀어나옴 → "서는가·얼마나
   빨리 재출발하는가". (b)는 정책(기다리기) 문제다.
4. 기동 후 CPU 50 % 아래 확인 후 출발(run16 도 113 s 뒤 출발해 goal 1 주기 미달 59).

## 7. 8판 적용 (2026-09-17 저녁, ros2 커밋 참조) — 한 번에 넣은 두 수리와 구분법

§6.4 의 1·2번을 **한 회차에 함께** 넣었다. 둘은 서로 다른 상황에서만 작동하고, bag 에서 어느 모드였는지
틱마다 갈리므로 효과를 따로 셀 수 있다.

| 수리 | 작동 상황 | bag 에서 구분 | 판정 잣대 |
| --- | --- | --- | --- |
| 레일 직접(앞 3 m 조각이 비면 레일을 그대로 FollowPath) | 코너·직선·접점 등 평상시 | `{path}` 첫 점 = 가까운 레일 노드(로봇에서 0.06~1 m) | 코너 w 버스트 ≤2·부호 반전 0·접점 레일 이탈 ≤0.3 m |
| 당근 6 m(3 m 조각이 막히면) | 사람·유령 자국이 조각 위에 있을 때만 | `{path}` 첫 점 = 로봇 위치, 끝점이 레일 위 6 m 앞 | 횡 이탈 ≥0.6 m·정지 폴리곤 0 |

함께 넣지 **않은** 것: `change_penalty`(당근 경로 모양이 바뀌어 사람 회피 비교를 흐림), 기다리기 정책,
RPP, DWB 저울(닫힌 축). 표시 6 m·소거 8 m 는 run16·17 과 같아 대조가 유지된다.

BT 구조: `ComputeRoute → 거름망 → TruncatePathLocal(3 m) → Fallback[ RailDirect(IsPathValid 조각 → ComputeRoute→{path}) |
CarrotBeyond(TruncatePathLocal 6 m → GetPoseFromPath → Fallback[ComputePathToPose(당근), ComputeRoute]) ]`.
IsPathValid 는 Humble 에서 치명·내접 칸만 거르므로 레일 옆 0.2~0.5 m 에 비껴 선 사람은 레일 모드가 못
거른다(DWB 몫) — 이번 회차에서 그 경우가 나오면 다음 판 과제로 올린다. 두 모드가 유령 자국으로 1초마다
갈릴 수 있다는 것도 볼 것(소거 8 m 로 줄었을 것).

실주행 절차는 §2 와 같다(nav2 를 `use_route:=true` 로 재기동, 초기 위치, CPU 50 % 아래 확인). bag `route0917_run18`.

## 8. run18 — 8판(레일 직접 + 6 m 당근) 첫 실주행 (19:30~19:45, bag `route0917_run18`)

10/10 완주, 도착 xy ≤0.24 m·yaw ≤6.1°(역대 최소). 표는 `~/vica_data/analysis/compare0917/compare.md`(네 회차).

| 잣대 | run16 레일 7판 | run18 레일 8판 | 판정 |
| --- | --- | --- | --- |
| 당근 모드 비율(planner 경로 초/주행 초) | 90 % | **54 %** | 레일 직접이 절반 가까이 작동(나머지엔 마지막 2 m·목적지 근처 포함) |
| 코너 w 버스트 / 부호 반전 / 직선 틈 | 4.0 / 5.0 / 4.0 | **3.4 / 2.7 / 2.2** | 개선(사용자 "어색함 없음") |
| 코너에서 레일 이탈 최대 | 0.95 m | **0.55** | 레일의 호를 탄다 |
| 직진 중 w 표준편차 | 0.103 | **0.146(악화)** | 407호→화장실 두 구간이 0.23·0.22(§8.1) |
| U턴 제자리 비율 / 레일 이탈 / 벽 여유 최소 | 42 % / 0.46 m / 0.74 m | 32 % / 0.75 / **0.36** | 호를 그리며 나감(§8.1) |
| 사람: 정지 합 / 사건당 손실 / 최근접 평균 | 17.0 s / 3.2 s / 0.88 m | **9.3 / 1.4 / 1.24** | 개선(사용자 "4 m 면 자연스럽게, 2~3 m 면 멈칫") |
| 사람: 횡 이탈 최대 평균 | 0.27 m | 0.25 | 6 m 당근으로도 폭은 안 늘었다 |
| 거름망 "far"(레일에서 1.5 m 초과) | 0 | **3** | U턴 호가 1.39 m 까지 나가 한계에 닿음 |
| Control loop missed | 59 | 30(goal 1 = 9) | 기동 후 고부하는 여전 |

### 8.1 407호→화장실 직진 흔들림과 U턴 호 — 뿌리가 하나다

시간열(goal 3·5, 0.5 s): 출발 순간 **뒤 0.4 m 에 사람(손잡이를 잡고 따라오는 사용자)** 이 서 있다. 407호에서
화장실로 가는 레일은 뒤쪽(서쪽)으로 나가므로 앞 3 m 조각이 그 사람을 지난다 → IsPathValid 실패 → 당근 모드
(당근 5.5 m). lattice 는 사람을 피해 **넓은 호로 U턴**을 그린다(제자리 3 s 뒤 호, 레일 이탈 1.39 m 까지,
복도 남쪽 벽 쪽). 호가 끝나면 조각이 비어 **레일 모드**로 넘어가는데, 레일 경로는 가장 가까운 노드에서 시작하는
선이라 로봇 옆 1.3 m 에 놓인다. DWB 가 옆에 놓인 선에 붙으려고 w ±0.5 로 좌우를 번갈아 치며 8 s 동안 1.39→0.15 m
로 다가붙는다 — 이게 "직진에서 비틀거림"이다. run16(늘 당근 3 m)은 U턴이 제자리(42 %)라 이탈이 0.46 m 에 그쳤고
복귀도 planner 가 매끄러운 대각선으로 그렸다.

즉 **U턴 호 = 6 m 당근 + 뒤에 선 사람**, **직진 흔들림 = 호 끝에서 레일 모드가 멀리서 켜져 DWB 가 옆 선에 붙는
지그재그**. 둘 다 8판 두 수리의 상호작용이다(7판엔 없던 현상).

### 8.2 그 밖에
- 순수 레일 모드 코너(19:34:42 +113°, planner 경로 0)도 버스트 5 — 레일 Y 호가 R 0.48 이라 0.5 m/s 에서 w 1.0 rad/s
  가 필요한데 상한이 0.5. 접점처럼 넓은 곳은 FILLET_R 1.0 이 맞다(생성기 후보).
- 사람 사건 8건 중 복도 한복판 4건: 정지 ≤0.5 s, 최근접 0.59~1.95 m. 4 s 정지 2건은 테스트1 도착 지점(x≈4)에서
  목적지 옆에 선 사람 — 회피가 아니라 도착 대기.
- 테스트3 도착 정렬 14.7 s(역대 최장). 180° 도착 U턴 + 레일 모드 조합인지 [미확인].
- 카메라 depth_scan 정상, 감지 범위 6/8 그대로, CPU 45~53 %.

### 8.3 다음 후보(한 번에 하나)
1. **레일 모드 진입 조건에 "레일에서 0.5 m 안"** 을 추가: RailDirect 앞에 `IsRoutePathUsable(max_dist_from_path=0.5)`.
   멀리 있으면 당근 모드가 매끄러운 복귀선을 그린다. 기성 거름망 포트라 XML 만 바꾼다. 직진 흔들림 표적.
2. U턴 호 억제: 당근 6→4.5 m(회피 폭은 6 m 에서도 안 늘었으므로 잃는 게 적다) 또는 "당근이 로봇 뒤면 3 m 조각" —
   후자는 플러그인. 벽 여유 0.36 m·far 3 회가 근거.
3. 생성기 FILLET_R 0.5→1.0(접점·넓은 코너).

## 9. run19 — 8판-1(레일 0.5 m 안에서만 레일 모드) 실주행 (20:13~20:25, bag `route0917_run19`)

8/8 완주, xy ≤0.19 m·yaw ≤8.2°. bag 시작 전(20:09:35 기동 인스턴스)에 **controller_server 가 SIGSEGV 로 죽어**(20:10:47,
기동 72 s 뒤·첫 goal 26 s 뒤, 마지막 "Passing new path" 3.4 s 뒤, 오류 로그 없음) 미션이 취소됐고(bt_navigator "Goal
canceled" 20:10:50) 사용자가 nav2 를 재기동·초기위치 재설정 뒤 정상. 코어 덤프 없음(ulimit -c 0). 원인 [미확정].
64e28af(TF 공백 초음파 레이어 수리)는 이 브랜치에 포함돼 있고 그 사고는 예외(-6)였는데 이번은 -11 이다. 기동 후
고부하 구간(CPU 80~90 %)에서 첫 goal 을 낸 세 번째 회차라는 공통점만 있다.

| 잣대 | run18 8판 | run19 8판-1 | 판정 |
| --- | --- | --- | --- |
| 407호→화장실 직진 w 표준편차 | 0.23 / 0.22 | **0.06 / 0.06 / 0.07** | 목표(≤0.12) 달성 — 지그재그 사라짐 |
| 화장실→407호 직진 w 표준편차 | 0.14 / 0.18 | **0.17 / 0.25** | 악화(사용자 체감과 일치) |
| 당근 모드 비율 | 54 % | **81 %** | 문이 너무 좁아 레일 모드가 거의 사라짐 |
| 안쪽 문 "far" 실패 | — | **96회** | 0.5 m 밖 = 정상 주행의 13 %(run18 기준)까지 걸림 |
| 코너 버스트 / 반전 / 레일 이탈 | 3.4 / 2.7 / 0.55 m | 4.5 / 4.3 / **1.17 m** | 코너가 당근 모드로 돌아감(붙어 감·펄스) |
| 레일선 이탈 >0.5 m(이동·목적지 2 m 밖) | 12.9 % | **23.5 %** | 당근 모드가 레일선 밖으로 몰고 감 |
| U턴 벽 여유 최소 | 0.36 m | 0.35 m | 그대로(6 m 당근) |
| 사람: 정지 합 / 사건당 손실 / 최근접 | 9.3 s / 1.4 s / 1.24 m | 9.9 / 2.6 / 1.34 | 비슷(사용자 "양호") |

### 9.1 원인 — 문턱 0.5 m 는 과교정
레일선에서 0.5 m 넘게 떨어진 시간은 run16 13 %·run18 13 %(정상 주행 포함)라 문이 그 시간 내내 닫혔고, 닫힌 동안 당근
모드(6 m)가 로봇을 레일선에서 더 밀어내(23.5 %) 자기 강화됐다. 0.8 m 밖은 3~6 %, 1.0 m 밖은 0~4 %(run18 의 U턴 뒤
복귀 구간뿐)라 **문턱은 0.8~1.0 m** 가 맞다. 407호→화장실은 넓은 복도에서 당근 planner 가 접점 안쪽을 가로질러
("붙어서 감", 이탈 1.3 m) 흔들림 없이 갔고, 화장실→407호는 좁은 곁가지(1.8 m)에서 U턴 뒤 당근 planner 가 Y 를 22.5°
눈금으로 그려 7판식 펄스(버스트 7~8·반전 6)가 났다.

### 9.2 개선안(다음 판, 하나의 XML 변경으로 세 갈래)
1. 안쪽 문 0.5→**0.8 m**(정상 주행은 레일 모드 유지, U턴 뒤 복귀만 당근).
2. **복귀용 짧은 당근**: 레일 조각이 비어 있는데 0.8 m 밖이면 당근 3 m(빠르게 레일선으로 수렴, 완만한 대각선). 막혔을 때만 6 m.
   → 모드 셋: 레일(≤0.8·조각 비음) / 복귀 당근 3 m(>0.8·조각 비음) / 회피 당근 6 m(조각 막힘). bag 에선 경로 끝 거리로 갈린다.
3. 생성기 FILLET_R 0.5→1.0(Y 접점 R 0.48 은 w 상한 0.5 에 0.5 m/s 로 못 맞춘다) — 생성기 변경이라 별도 회차.
4. 기동 후 CPU 50 % 아래 확인 후 첫 goal. controller 크래시 재발 대비 nav2 칸에서 `ulimit -c unlimited` 뒤 기동.

## 10. 8판-2 적용 (ros2 커밋 참조) — 모드 셋, run20 준비

§9.2 의 1·2 를 한 XML 변경으로 넣었다. 세 모드는 서로 배타적인 상황에서만 켜지고 bag 에서 갈린다.

| 모드 | 조건 | bag 에서 구분 | 기대 효과 |
| --- | --- | --- | --- |
| 레일 직접 | 레일선 0.8 m 안 · 앞 3 m 비어 있음 | `{path}` 첫 점 = 노드 | run18 수준 코너(버스트 ~3·이탈 ~0.5)·직진 안정·지나가는 사람 무시 |
| 복귀 당근 3 m | 0.8 m 밖 · 앞 3 m 비어 있음(U턴 호 끝) | planner 경로, 끝 거리 ≈3 m | run18 지그재그(0.23)·run19 붙어 감(1.3 m) 둘 다 소멸, 곧 레일 모드 복귀 |
| 회피 당근 6 m | 앞 3 m 막힘(사람) | planner 경로, 끝 거리 ≈6 m | run18 회피 수준 유지(정지 합 ≤10 s) |

남는 것: U턴 호 자체(6 m 당근 + 뒤에 선 사람)와 그때 벽 여유 0.35 m, 접점 호 R 0.48 의 작은 펄스(FILLET_R 1.0 후보).
판정: 407호→화장실 직진 w 표준편차 ≤0.12 **그리고** 화장실→407호 ≤0.15, 코너 레일 이탈 ≤0.7, 당근 모드 ≤60 %, 사람 정지 합 ≤10 s.
기동: nav2 칸에서 `ulimit -c unlimited` 뒤 재기동, CPU 50 % 아래 확인 후 첫 goal. bag `route0917_run20`.

## 11. run20 — 8판-2(모드 셋) 실주행 (20:43~20:57, bag `route0917_run20`) 와 Y 접점 기하 검토

10/10, xy ≤0.26·yaw ≤6.8°, **주기 미달 0**(CPU 44~49 %, 사용자가 기동 뒤 기다림). 사람: 정지 합 **6.4 s**·사건당 손실
**0.9 s**·횡 이탈 0.37 m — 역대 최선. bag 전 116건의 "Timed out waiting for transform … map" 은 전부 초기위치 전(20:42:00~53)이라 무해.

| 잣대 | run18 | run19 | run20 | 판정 |
| --- | --- | --- | --- | --- |
| 407호→화장실 직진 w 표준편차 | 0.23 | 0.06 | 0.15~0.18 | 절반은 회복(판정 ≤0.12 미달) |
| 화장실→407호 직진 w 표준편차 | 0.14~0.18 | 0.17~0.25 | 0.10~0.20 | 소폭 개선 |
| 코너 버스트 / 반전 / 이탈 | 3.4 / 2.7 / 0.55 | 4.5 / 4.3 / 1.17 | 4.1 / 5.1 / 0.70 | run18 과 run19 사이 |
| 당근 모드 비율(복귀 3 m 포함) | 54 % | 81 % | 74 % | 복귀 15 %·회피 40 %·근처 19 % |
| U턴 벽 여유 최소 | 0.36 | 0.35 | **0.57** | 개선 |

### 11.1 남은 흔들림의 정체(모드 시간열)
- **모드가 1초마다 갈린다.** U턴 뒤 12 s 창에서 RAIL↔복귀(3 m)↔회피(6 m)가 1~2 s 마다 바뀌는 구간이 9개 U턴 중 6개.
  회피(6 m) 가 goal 의 40 % 를 차지하는데 사람 시험은 그중 일부다 — **뒤에 붙어 걷는 사용자가 U턴·복귀 중 앞 3 m 조각에
  닿아** 조각을 계속 무효로 만든다(화장실→407호 goal 4·6 은 33 s 중 23 s 가 회피 모드). 모드가 바뀌면 경로 모양이 바뀌고
  DWB 가 그때마다 w 를 고친다. 이게 "줄었지만 남은" 흔들림이다.
- **"U턴 뒤 한 번 더 꺾음"**: 복귀(3 m)에서 레일로 넘어가는 순간의 이탈은 0.2~0.4 m(9건 중 7건), 전환 뒤 3 s |w| 최대
  0.06~0.39·반전 0~2. 8판-1 의 지그재그(1.3 m 에서 전환)보다 훨씬 작지만 남는다. 0.78 m 에서 전환한 1건(20:46:27)은 |w| 0.50.
- **Y 접점 호 R 0.48 은 물리적으로 못 따라간다.** 순수 레일 모드로 호를 돌 때 w 가 0.45~0.50 에 붙고(상한 0.5) 안쪽으로
  0.3~0.4 m 잘라 들어간다. 0.5 m/s 에서 필요한 w 는 v/R = 1.04 rad/s. 상한 0.5 를 지키려면 **R ≥ 1.0 m**.

### 11.2 사용자 제안 검토
1. **코너 넓히기(FILLET_R 0.5→1.0) — 가능하고 맞다.** 임시 폴더에서 생성해 보니 세 접점이 (38.69,−0.83)·(39.67,−1.81)·
   (40.61,−0.82) 로 벌어지며 호 반지름 0.97~1.0, 노드 59, 최대 엣지 1.00, 뾰족 0, Y 주변 벽 여유 최소 1.20 m(지금과 같음).
   T 자 공간이 넓어 호가 벽에 붙지 않는다(그림 `analysis/compare0917/fillet_r10_dryrun.png`). 효과: 레일 모드에서 w 상한
   안에서 돌 수 있어 안쪽 자르기·펄스가 사라진다. 등뼈의 완만한 굽이는 15° 미만이라 영향 없다. maps/ 에는 아직 안 넣었다.
2. **들어오는 경로·나가는 경로 분리 — 여기엔 안 맞는다.** 레일 엣지는 방향이 있어 "동쪽 호로 들어가고 서쪽 호로 나오는"
   회전교차로는 만들 수 있지만, 화장실은 막다른 곁가지라 어느 방향으로 들어가든 **그 자리에서 180° 돌아 같은 곁가지로
   나와야** 한다. 곁가지 폭 1.8 m 는 제자리 회전 폭 1.25 m 는 되지만 돌아 나오는 고리(지름 ≥1.9 m)는 못 들인다.
   접점에서만 분리해도 U턴 뒤의 복귀 흔들림은 그대로다(흔들림의 자리가 곁가지 안·접점 앞이므로).
3. **U턴 자체를 없애는 지도 쪽 손질(코드 0)**: 화장실 등록 yaw 를 270(안쪽)→90(바깥쪽), 407호는 0→180 으로 바꾸면
   U턴이 **출발이 아니라 도착 정렬**로 옮겨간다. 도착 정렬은 목적지 0.25 m 안에서 RotateToGoal 제자리 회전이라(6판 게이트)
   뒤에 선 사람이 조각을 막을 일이 없고, 출발은 직진이 된다. 대가: 도착 방향이 문을 등진다(안내 로봇 의미상 확인 필요),
   도착 정렬 시간 8~11 s(홈·테스트1 실측)가 붙는다.
4. **뒤에 붙은 사용자가 조각을 무효로 만드는 문제**는 기하가 아니라 검사의 문제다. IsPathValid 가 조각의 **첫 0.5 m** 를 빼고
   보게 하거나(TruncatePathLocal 의 backward 는 이미 0 — 앞쪽 시작점을 0.5 m 뒤로 미는 노드는 없음 → 플러그인 필요),
   거름망 플러그인에 "로봇 반경 안의 치명 칸은 무시" 를 넣는 것이 정공법. 이것이 회피 모드 40 % 의 절반 이상을 지울 것이다.

### 11.3 권고 순서
1. FILLET_R 1.0 로 레일 재생성(생성기 상수 하나, 계약 시험 `corners_are_rounded` 는 0630 도 통과해야 함 — 0630 은 좁아
   호가 못 들어가는 코너는 그대로 남는다(fillet 이 자동으로 포기)).
2. 화장실·407호 yaw 반전은 사용자가 앱에서 결정(코드 0). 하면 U턴 관련 흔들림 대부분이 사라진다.
3. 거름망에 "로봇 반경 안 치명 칸 무시" — 플러그인(C++) 변경, 다른 세션 패키지라 조율 뒤.

## 12. run21(8판-2 + 접점 호 1.0) vs run22(8판 + 접점 호 1.0) — 같은 코스(407호↔화장실 ×3, 테스트3), 사람 시험 없음

BT 는 run22 뒤 8판-2 로 복원(ros2 7c64654). 사용자 판정: run21 "별로 안 나아짐", run22 "너무 경로로 가려 해 못 씀".

| 잣대 | run20 8판-2·R0.48 | run21 8판-2·R1.0 | run22 8판·R1.0 |
| --- | --- | --- | --- |
| 완주 / 도착 yaw 최대 / 정렬 최대 | 10/10 / 6.8° / 10.5 s | 6/6 / 8.8° / 8.0 s | 8/8 / 5.4° / **16.5 s** |
| planner 실패 / 출발점 치명 | 2 / 1 | 1 / 0 | **4 / 1** |
| 벽 여유 <0.45 m 비율 | 0.0 % | 0.0 % | **2.1 %** |
| 코너 버스트 / 반전 / 레일 이탈 | 4.1 / 5.1 / 0.70 | 7.0 / 4.5 / 0.53 | 4.6 / 3.6 / 0.53 |
| 직진 w 표준편차 (407호→화장실 / 화장실→407호) | 0.15~0.18 / 0.10~0.20 | 0.11·0.16 / 0.13·0.12 | 0.12~0.15 / 0.11~0.16 |
| 당근 모드 비율 | 74 % | 67 % | 57 % |
| U턴 제자리 비율 / 레일 이탈 / 벽 여유 최소 | 40 % / 0.75 / 0.57 | 44 % / 0.62 / 0.50 | 34 % / **0.87** / 0.55 |
| 거름망 "far"(1.5 m 초과 → 목적지까지 자유주행) | — | 17(안쪽 문 포함) | 1 |

### 12.1 차이와 원인
- **두 회차의 지표 차이는 작다.** 접점 호를 0.48→1.0 으로 키운 효과가 안 보이는 이유는 Y 를 지나는 동안 절반 가까이가
  당근 모드(코너 창의 planner 경로 3~4개/8~10 s)라 **레일의 호를 실제로 따르는 시간이 짧기** 때문이다. 당근 모드로
  넘어가는 원인은 여전히 뒤에 붙어 걷는 사용자(U턴·출발 때 3 m 조각을 막음)와 U턴 뒤 복귀다.
- **run22 의 "경로로 가려는 성질"**: 문이 없어 U턴 호 끝(레일에서 0.87~1.38 m)에서 곧바로 레일 모드가 켜지고, 레일
  경로는 가까운 노드에서 시작하는 선이라 DWB 가 옆 선으로 강하게 붙는다(run18 기작). 흔적 = 벽 여유 <0.45 m 2.1 %,
  U턴 이탈 0.87, planner 실패 4, 테스트3 정렬 16.5 s. run21 은 문(0.8)+복귀 당근이라 그 공격성은 없지만, 모드가 1~2 s
  마다 갈리는 흔들림(코너 창 버스트 7.0)이 남는다.
- 결론: **호 반지름은 필요조건이지 충분조건이 아니다.** 호를 타게 하려면 Y 근처에서 레일 모드가 유지돼야 한다.

### 12.2 수정 방안(우선순위)
1. **거름망에 "로봇 반경(≈0.6 m) 안의 치명 칸은 막힘으로 세지 않기"** — 뒤에 붙은 사용자가 조각을 무효로 만드는 것을
   끊는다. 회피 당근 40 %의 대부분이 사라지고 Y 를 레일 모드로 지나게 된다. C++ 플러그인(vica_nav2_bt_plugins) 변경.
2. **모드 최소 유지 시간(2~3 s)**: 레일↔복귀↔회피가 1 Hz 로 갈리는 것을 막는다. 상태가 필요해 같은 플러그인에 넣는다.
3. (A/B 가능, XML 한 값) 회피 당근 6→3 m: 6 m 의 회피 폭 이득은 실측에서 안 나타났고(횡 이탈 0.25~0.37 vs 3 m 때 0.27),
   U턴 호(레일 이탈 0.9~1.4 m·far 거부)는 6 m 의 비용이다. 사람 시험을 포함해 run16(3 m) 과 비교해야 판정된다.
4. 접점 호 1.0 은 유지(1·2 가 되면 효과가 드러난다).
