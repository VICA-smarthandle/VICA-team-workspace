# YOLO 비전 도입 검토 + 동적 장애물 정지 증상 — 2026-08-12

> **이 파일 용도**: 두 가지를 담는다. (1) YOLO 객체인식 도입 사전 조사, (2) 같은 날
> 보고된 동적 장애물 회피 실패 2건의 파라미터 관여 지도. 두 주제가 한 문서에 있는
> 이유는 조사 도중 "nvblox는 인식기가 아니다"라는 사실이 드러나면서 두 주제가
> 같은 축(nvblox mapping_type)에서 만났기 때문이다.
>
> **선행 문서**: `devlog/2026-07-30-nvblox-ghost-obstacle.md` 를 먼저 읽는다.
> 이 문서의 §6 증상 2는 그 문서가 다룬 증상의 연속이다.

---

## 0. 한 줄 요약

**조사만 했고 코드·설정은 하나도 바꾸지 않았다. 실기 시험도 실행하지 않았다.**

- nvblox는 **객체 인식을 하지 않는다.** static / dynamic / people 세 모드의 차이는
  "두 지도를 무엇을 기준으로 나누느냐"이며, 이름과 달리 dynamic 모드에도 DNN이 없다.
- VICA 가 `mapping_type: dynamic` 에서 3회 실패한 기전이 **nvblox 소스 주석으로 설명된다.**
  같은 약점이 people 모드에는 **구조적으로 없다.** people 모드는 아직 한 번도 시험되지 않았다.
- 동적 장애물 회피 실패는 **3건**이다. ① 원거리 주춤거림 ② 지나간 뒤 20초 이상 정지
  ③ **우회 능력 저하**. 관여 파라미터를 전수로 짚었으나 **원인은 확정하지 않았다.**
  원인을 가르는 관찰은 §7, 조정 가능한 축 전체는 §10 에 있다.
- **2026-08-11 실기는 knob 0 % 라 무효다(§0.1).** 그 회차 로그를 근거로 값을 바꾸지 않는다.
- **작업 순서는 사용자가 확정했다(2026-08-12): 동적 장애물 회피·우회를 파라미터로 먼저
  잡고, 그 뒤에 YOLO 기반 객체 인지를 얹는다.** §2~§5 는 두 번째 단계의 사전 조사다.
- 하드웨어(세로 90 → 81 cm)는 추후 진행. 계산은 §10.4 에 있다 — **81 cm 로도 1.0 m
  통로에서는 제자리 회전이 13 cm 부족하다.**

---

## 0.1 ⚠️ 2026-08-11 회차는 무효다 — 이 문서에서 가장 먼저 읽을 것

**그날 실기는 드라이버 가변저항(knob)이 0 % 상태였다.** `speed_ratio = knob_pct/100` 이므로
Nav2 가 무엇을 보내든 `clamp` 로 0 이 된다. **명령은 나갔고 바퀴는 돌지 않았다.**

그 세션 로그만 보면 전형적인 costmap 교착으로 읽힌다. **전부 2차 증상이다.**

| 지표 | 2026-08-11 (knob 0) | 2026-08-02 확정본 |
| --- | --- | --- |
| `No valid trajectories out of 419` | 954회 | 2회 |
| `Starting point in lethal space` | 16회 | 0회 |
| `Controller patience exceeded` | 60회 | — |
| `Passing new path` | 627회 | — |
| Spin 시도 | 30회 (29회가 `Collision Ahead` 로 중단) | — |

연쇄는 이렇다.

```text
바퀴가 안 돎
 ├─ 장애물에서 못 벗어남           → ObstacleFootprint 1.00 (419개 궤적 전부 충돌)
 ├─ oscillation_reset_dist 못 채움 → Oscillation critic 이 탈출 방향을 계속 거부
 ├─ 목표에 못 감                    → 재계획 폭증 (Passing new path 627회)
 └─ Spin 명령만 나가고 안 돎        → Collision Ahead - Exiting Spin 29회
```

**규칙 두 개.**

1. **이 회차 숫자를 근거로 파라미터를 바꾸지 않는다.**
2. **주행 실패를 Nav2·nvblox 설정 문제로 진단하기 전에 그 회차의 knob 부터 확인한다.**
   로그·영상만으로는 "명령을 못 만든 것"과 "명령은 냈는데 바퀴가 안 된 것"이 구분되지
   않는다. 구분하려면 `/cmd_vel_safe`(명령)와 `/wheel/odom`(실제 이동)을 함께 봐야 하므로
   bag 에 둘 다 넣는다. 확인 절차는 §7.0.

2026-08-10 회차와의 비교도 성립하지 않는다. 그 세션은 `Received a goal` 이 0건이라 주행
목표를 준 적이 없다(매핑용으로 보인다).

---

## 1. 환경 / 장비

- 이 조사는 **개발 노트북(x86_64)** 에서 문서·소스 읽기만으로 수행했다.
- 실기 확인은 **Jetson Orin NX 16GB(JetPack 6.x)** 에서만 가능하다. `uname -m` 으로 확인할 것.
- nvblox 소스는 VICA 4개 저장소 밖에 있다. 이번 조사는 GitHub 원격 소스를 읽었다.
  - `nvidia-isaac/nvblox` — `nvblox/include/nvblox/mapper/multi_mapper.h`
  - `NVIDIA-ISAAC-ROS/isaac_ros_nvblox` — `nvblox_ros/src/lib/mapper_initialization.cpp`
- **읽은 것은 nvblox 최신 public 소스다.** VICA 가 쓰는 Isaac ROS `release-3.2` 와
  다를 수 있다. 아래에서 3.2 확인이 필요한 항목은 `[3.2 미확인]` 으로 표시했다.

---

## 2. nvblox 세 모드 — 소스 수준 확정

### 2.1 공통 구조: 지도는 항상 두 개다

`MultiMapper` 가 mapper 두 개로 구성된다 (`multi_mapper.h`).

```
background mapper : 정적 물체 (TSDF 또는 occupancy)
foreground mapper : 동적 물체 또는 사람 (occupancy)
```

`ImageMasker` 가 들어온 depth 이미지를 마스크로 쪼개 두 mapper 에 나눠 넣는다.
**모드의 차이는 이 마스크를 누가 만드느냐 하나다.**

### 2.2 `MappingType` 전체 (소스 원문)

```cpp
enum class MappingType {
  kStaticTsdf,               /// only static tsdf
  kStaticOccupancy,          /// only static occupancy
  kDynamic,                  /// static tsdf (incl. freespace) and dynamic occupancy
  kHumanWithStaticTsdf,      /// static tsdf and human occupancy
  kHumanWithStaticOccupancy  /// static occupancy and human occupancy
};
```

ROS 파라미터 문자열 (`mapper_initialization.cpp`):
`static_tsdf` / `static_occupancy` / `dynamic` / `human_with_static_tsdf` /
`human_with_static_occupancy`

### 2.3 세 모드 비교

| | `static_tsdf` | `dynamic` | `human_with_static_tsdf` |
| --- | --- | --- | --- |
| 마스크 생성자 | 없음 | nvblox 자신(`DynamicsDetection`) | 외부 DNN(PeopleSemSegNet) |
| **DNN** | **없음** | **없음** | **필요** |
| background | TSDF | TSDF + **freespace layer** | TSDF |
| foreground | **`kNone` — 꺼짐** | occupancy | occupancy |
| 실행 노드 | `nvblox_node` | `nvblox_node` | **`nvblox_human_node`** |
| 망각 노브 | 1개 | 2개 | 2개 |
| slice 토픽 | `static_map_slice` | `static`+`dynamic`+`combined` | `static`+`combined` |

`findForegroundLayerType(kStaticTsdf)` 가 `ProjectiveLayerType::kNone` 을 돌려준다.
**현재 VICA 는 `static_tsdf` 이므로 `vica_nvblox_overrides.yaml` 의 `dynamic_mapper:`
블록(`occupied_region_decay_probability` 등)은 아무 효과가 없다.** 되돌리기용으로
남겨둔 값이며, 지금 그 값을 만져도 거동은 바뀌지 않는다.

### 2.4 dynamic 모드가 VICA 에서 3회 실패한 기전

`multi_mapper.h` 개발자 주석 원문 요지:

> dynamic mapping 에서는 **전체 depth 이미지가 background mapper 에 통째로 들어간다
> (마스킹 없음).** 그러지 않으면 freespace 를 리셋할 수 없기 때문이다 — freespace 에
> 떨어지는 depth 측정값은 DynamicsDetection 이 항상 dynamic 으로 마스킹해버린다.
> 그 대신 background mapper 의 esdf site 중 freespace 에 떨어지는 것을 무시한다.

**즉 dynamic 모드는 사람을 static 지도에서 빼주지 않는다.** 사람이 foreground 에도
들어가고 background 에도 들어간다. 이것이 `vica_nvblox_overrides.yaml` 주석에 기록된
실측을 설명한다.

| | `static_tsdf` | `dynamic` |
| --- | --- | --- |
| local costmap lethal | 10.7 % | **21.2 %** |
| inscribed | 26.4 % | **46.4 %** |
| 4구간 완주 | **3/3** (145.6 s) | 1/3 |

팀이 실측으로 도달한 결론("static 과 dynamic 이 같은 물체를 각자 잡아 합쳐진다")과
소스가 일치한다.

### 2.5 people 모드는 이 약점이 구조적으로 없다

`integrateDepth(depth_frame, mask, ...)` 오버로드가 `ImageMasker` 로 depth 를 쪼개
각 mapper 에 **따로** 넣는다. 사람은 static 지도에 처음부터 들어가지 않는다.

**people 모드는 VICA 에서 한 번도 시험된 적이 없다.** dynamic 3회 실패를 근거로
people 을 배제하면 안 된다. 실패 기전이 다르다.

단, 도입 시 새로 지는 비용:
- `isaac_ros_unet` + PeopleSemSegNet 이 **주행 중 상시** GPU 를 쓴다. Jetson 은 경량
  `PeopleSemSegNet_ShuffleSeg` 권장.
- launch 의 `executable="nvblox_node"` 를 `nvblox_human_node` 로 바꿔야 한다
  (`vica_nvblox.launch.py:63`).
- **핸들을 잡은 이용자 본인이 상시 검출된다.** `docs/nav2_backlog.md` §9 의
  "후방 Collision Monitor 상시 발동"과 같은 함정이다.

---

## 3. YOLO 도입 경로 3안

### A. Isaac ROS — 현 스택과 같은 컨테이너

`isaac_ros_object_detection` 이 `isaac_ros_yolov8`, `isaac_ros_rtdetr`,
`isaac_ros_detectnet`, `isaac_ros_grounding_dino` 를 제공한다.
파이프라인은 `encoder → TensorRT → decoder` 3단, 입력은 `/image` + `/camera_info`.

release-3.2 README 의 Orin NX 실측:

| 모델 | 입력 | AGX Orin | **Orin NX** |
| --- | --- | --- | --- |
| RT-DETR | 720p | 56.5 fps | **33.3 fps** |
| DetectNet | 544p | 55.4 fps | **25.7 fps** |

(YOLOv8 수치는 NVIDIA 가 싣지 않았다.)

장점: NITROS zero-copy, 이미 상시 가동 중인 컨테이너 안. 라이선스 부담 없음.

### B. Ultralytics + TensorRT — Hiwonder ROSOrin 방식

Hiwonder ROSOrin 은 **YOLOv11 + MediaPipe + OpenCV** 조합으로 광고한다.
(제품 페이지가 동적 렌더링이라 원문 사양표는 확인하지 못했다. RobotShop 은 403.)

Ultralytics 공식 Jetson 가이드의 **Orin NX 16GB** 벤치마크:

| 포맷 | 추론 시간 | 추정 FPS |
| --- | --- | --- |
| PyTorch | 13.90 ms | ~72 |
| TensorRT FP32 | 7.01 ms | ~143 |
| **TensorRT FP16** | **4.13 ms** | **~242** |
| TensorRT INT8 | 3.49 ms | ~286 (mAP 0.4789→0.4489) |

Orin NX 는 **DLA 코어 2개(614 MHz)** 가 있어 `device="dla:0"` 로 GPU 를 비켜갈 수 있다.
GPU 가 이미 붐비는 VICA 에서 실질적인 카드다.

ROS 2 연동은 `mgonzs13/yolo_ros` 가 가장 활발하고, **depth 로 2D bbox 를 잘라
3D bbox 를 만들어 `/yolo/detections_3d` 로 발행한다.** A 안에는 없는 기능이다.

⚠ **라이선스**: Ultralytics YOLOv8/v11 은 **AGPL-3.0**. 상용 배포 시 소스 공개 의무
또는 상용 라이선스 구매가 필요하다. 사용자 판단은 현재 "아직 모름"이다.

### C. nvblox people segmentation — 목적이 회피라면

§2.5 참고. 이미 완성된 nvblox → Nav2 costmap 배선을 그대로 쓴다.

---

## 4. YOLO bbox 를 nvblox 에 되먹이는 경로 `[3.2 미확인]`

`MultiMapper` 에 세그멘테이션 마스크 대신 **bounding box 를 받는 오버로드**가 있다.

```cpp
void integrateDepth(const DepthImage& depth_frame,
                    const std::vector<ImageBoundingBox>& detection_boxes,
                    const Transform& T_L_CD, const Transform& T_CM_CD,
                    const Camera& depth_camera,
                    const Camera& detection_boxes_camera);
```

`nvblox/semantics/mask_from_detections.h` 가 bbox 를 마스크로 바꾼다. 즉 YOLO 검출
결과를 foreground mapper 에 직접 먹일 수 있고, 사람만이 아니라 YOLO 가 아는 클래스라면
무엇이든 동적 취급이 가능해진다.

단서 두 개:
- **컬러 쪽 오버로드에는 "현재 detection box 는 무시되고 컬러 프레임 전체가 static map 에
  들어간다"는 주석이 있다.** 유효한 것은 depth 경로다.
- **이 API 가 release-3.2 에 있는지 Jetson 에서 확인해야 한다.**

---

## 5. 도입 전 검토 항목

| # | 항목 | 근거 |
| --- | --- | --- |
| 1 | **GPU 경합** — YOLO 는 nvblox·STT·TTS 에 이은 4번째 상시 소비자 | `devlog/2026-07-30-gpu-nvblox-stt-contention.md`. 발화 중 GPU 9.8→28.7 %(+19.0 %p). 단 그 실측은 **Nav2·AMCL 미실행** 상태였다 |
| 2 | **조용한 저하 재발** — 검출 토픽 age/Hz 감시를 처음부터 설계할 것 | 같은 devlog 의 nvblox slice 공백과 동일 기전 |
| 3 | **안전 체인 우회 금지** — 인식 결과가 `/cmd_vel` 경로에 직접 들어가면 안 된다 | `CLAUDE.md` 안전 기준 |
| 4 | **이용자 오검출** — 핸들을 잡은 사람이 상시 검출된다 | `docs/nav2_backlog.md` §9 |
| 5 | **통로 1.0 m** — 사람을 발견해도 회피 기동 공간이 없다. 출력은 회피가 아니라 감속·정지·안내여야 할 수 있다 | 같은 §9 (제자리 회전에 1.31 m 필요) |
| 6 | **컨테이너 ↔ Host 동기화** — 정본/복사본 수동 복사 문제가 그대로 재현된다 | `vica_nvblox_bringup/README.md` §0·§8. §177 에 마운트 전환이 별도 과제로 적혀 있다 |
| 7 | **D455 컬러 스트림** — 현재 depth 위주. USB 대역폭·해상도 재조정 필요 | `run_d455.sh` |
| 8 | **버전 고정** — NVIDIA 최신 Isaac ROS 문서는 **Jazzy + JetPack 7.1(Thor)** 기준으로 이동했다. 그대로 따르면 깨진다 | `guideline/official_reference_urls.md:17` |
| 9 | **AGPL-3.0** | §3-B |

### 참고 문서 (release-3.2 고정)

| 문서 | URL |
| --- | --- |
| Isaac ROS Object Detection | <https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_object_detection/index.html> |
| isaac_ros_yolov8 Quickstart | <https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_object_detection/isaac_ros_yolov8/index.html> |
| isaac_ros_rtdetr | <https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_object_detection/isaac_ros_rtdetr/index.html> |
| Isaac ROS DNN Inference | <https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_dnn_inference/index.html> |
| isaac_ros_unet (people seg) | <https://nvidia-isaac-ros.github.io/repositories_and_packages/isaac_ros_image_segmentation/isaac_ros_unet/index.html> |
| Ultralytics Jetson 가이드 | <https://docs.ultralytics.com/guides/nvidia-jetson/> |
| yolo_ros (3D detection) | <https://github.com/mgonzs13/yolo_ros> |
| ros_deep_learning | <https://github.com/dusty-nv/ros_deep_learning> |

---

## 6. 2026-08-12 주행 증상 2건 — 파라미터 관여 지도

### 6.1 증상 (사용자 보고)

1. **원거리(약 1.5~2 m)** 에서 사람을 보면 **경로가 매끄럽게 생성되지 못하고 주춤거린다.**
2. 사람이 앞을 지나간 뒤에도 **20초 이상** 제자리에 멈춰 있다.
3. **우회 능력이 떨어진다 — 장애물을 돌아가지 못한다.**

증상 3은 2026-08-12 에 추가로 보고됐다. **decay 로는 설명되지 않고 §6.7 로 설명된다.**

### 6.1.1 영상 판독 (5건, 총 211초)

`~/Downloads/20260811_21*.mp4`. **세로 촬영(1080×1920)이라 프레임 추출 시 9:16 을 유지해야
형태가 왜곡되지 않는다.**

| 파일 | 길이 | 로봇 |
| --- | --- | --- |
| 212203 | 46.5 s | 전 구간 주행 |
| 212301 | 30.5 s | 전 구간 주행 |
| 212349 | 27.9 s | 23초까지 주행 → 앉은 사람 앞 정지 (영상 끝) |
| 212943 | 48.7 s | 0~17초 정지(앞이 비어 있음) → 사람이 손으로 조작 → 주행 → 40~48초 정지 |
| **213045** | 57.4 s | 17초 정지 → **영상이 끝나는 57초까지 정지** |

213045 를 로그와 초 단위로 정합했다(영상 0초 = epoch 1786451445).

```text
 14~16 s  사람이 로봇 앞을 가로질러 걸어감
 17    s  로봇이 사람 발 앞 20~30 cm 에서 정지     ← 1.5~2 m 가 아니다
 26~29 s  사람이 로봇 앞을 떠남
 30~57 s  계속 정지. 영상이 끝날 때까지 재출발 없음
```

**정지 중 회전은 관측되지 않았다**(0.25초 간격 12프레임에서 로봇과 바닥 기준선의 관계가
동일). 다만 로그에는 Spin 좌·우 각 1회가 `completed successfully` 로 남아 있다. 좌 17.2°
→ 우 17.2° 라 순 회전이 0 이고 카메라도 함께 움직여 **영상만으로는 실제 회전 여부를 확정할
수 없다.** knob 0 을 감안하면 명령만 나가고 바퀴는 돌지 않았을 가능성이 높다.

**남는 것은 "로봇이 오래 서 있었다"는 관측뿐이고, 그 원인이 costmap 인지 구동 불능인지는
이 영상으로 가려지지 않는다(§0.1).**

### 6.2 범위를 좁히는 사실

`nav2_params.yaml:415` 주석의 계산:

> DWB 가 실제로 검사하는 거리 = `sim_time` 2.5 × `max_vel_x` 0.26 + footprint 앞단 0.355
> = **1.01 m**

**DWB 의 시야는 약 1 m다. 1.5~2 m 밖에서 멈춘다면 DWB(지역 컨트롤러)가 아니라
global planner / costmap / BT 쪽이다.**

### 6.3 증상 1 관여 파라미터

**nvblox — 무엇을 장애물로 올리나**

| 파라미터 | 현재값 |
| --- | --- |
| `mapping_type` | `static_tsdf` (사람이 정적 지도에 쌓인다) |
| `esdf_slice_min_height` | 0.05 |
| `esdf_slice_max_height` | 0.9 (사람 전신이 밴드에 들어온다) |

**costmap — 얼마나 넓게 막나** ← 1.5~2 m 를 만드는 유력 지점

| 파라미터 | 현재값 | 위치 |
| --- | --- | --- |
| `nvblox_layer.convert_to_binary_costmap` | **True** | `nav2_params.yaml:662` |
| `inflation_radius` | **0.55** | `:907`(local), `:1076`(global) |
| `cost_scaling_factor` | 3.5 | `:795`, `:1039` |
| `footprint` | 0.9 × 0.455 m (외접반경 ≈ 0.637) | `:487` |
| `footprint_padding` | 0.05 | `:621` |
| global 의 `nvblox_layer` | 있음 (2026-07-30 투입) | `:927` |

거리 합산:

```
사람 LETHAL 셀
  + inflation_radius            0.55
  + 로봇 외접반경 0.637 + padding 0.05
  ≈ 1.24 m   (로봇 중심 기준 진입 불가 반경)
```

**통로 폭 1.0 m** 와 겹치면 planner 가 물리적으로 경로를 만들 수 없다.

**BT — 경로 실패 후 무엇이 도나**
`RateController hz="1.0"`, `RecoveryNode number_of_retries="6"`

### 6.4 증상 2 관여 파라미터 — 두 개가 겹친다

**A. BT 복구 시퀀스 ← 가장 유력**

현재 launch 가 쓰는 트리는 `vica_navigate_to_pose_no_backup.xml`
(`nav2_map_test.launch.py:49`). RoundRobin 이 자식 4개를 순환한다.

```
ClearingActions → SpinLeft(+0.30) → SpinRight(-0.30) → Wait(5초)
```

- `<Wait wait_duration="5"/>` — `no_backup.xml:98`. **5초 통째로 정지**
- `Spin spin_dist="0.30"` — 각 0.75초 (`max_rotational_vel: 0.4`)
- 6회 순환 시 Spin 3회 + Wait 1회

같은 저장소 `vica_navigate_to_pose_clearing_only.xml` 주석의 **실측**:

> 최장정지 **19.4 s** — Spin 3회가 만든 시간이다
> `<Wait/>` 실패를 시간으로 덮는다. **종전 복구 시간의 79 %가 이것이었다.**

**장애물이 사라진 것과 무관하게 Wait 는 5초를 채운다.**

**B. nvblox 망각 — 겹쳐서 작용**

| 파라미터 | 현재값 |
| --- | --- |
| `decay_tsdf_rate_hz` | 2.5 |
| `tsdf_decay_factor` | 0.95 |
| `esdf_integrator_min_weight` | 0.1 |
| `exclude_last_view_from_decay` | false (2026-07-30 수정 완료) |

```
5.0 × 0.95ⁿ ≤ 0.1  →  n = 76 tick  →  76 / 2.5 Hz = 30.6 초
```

LiDAR 는 raytrace 로 즉시 지우지만 **nvblox slice 는 최대 30.6초를 기다린다.**

**C. costmap 갱신**: local `update_frequency: 5.0`, `publish_frequency: 2.0` (최대 0.5초 추가)

### 6.5 선행 문서와의 연결 — 중요

`devlog/2026-07-30-nvblox-ghost-obstacle.md` §4.1 의 기하 계산이 증상 1 에 직접 걸린다.

```
카메라 높이 0.320 m, 틸트 없음(rpy "0 0 0")
D455 수직 FOV 58° → 반각 29°, tan = 0.554
거리 d 에서 보는 높이 = 0.320 ± 0.554 × d
```

**가까이 갈수록 사람의 상체가 시야에서 빠진다.** 그 문서는 `inflation_radius` 0.45 로
계산했다. 현재 값 0.55 로 다시 계산하면:

```
정지 시 로봇 중심~장애물 = footprint 전방 0.305 + inflation 0.55 = 0.855 m
카메라는 중심보다 0.287 m 앞  →  카메라~장애물 ≈ 0.568 m
0.568 m 에서 관측 가능 높이 = 0.320 ± 0.315 = 0.005 ~ 0.635 m
→ slice 밴드 0.05~0.9 중 0.635~0.9 (약 30 %)가 사각지대
```

즉 **로봇이 멈춘 위치에서는 사람 상체 높이를 카메라로 볼 수 없어, 이미 쌓인 것을
관측으로 덮어쓸 수 없다.** 지우는 길은 decay(30.6초)만 남는다.

`exclude_last_view_from_decay: false` 수정 이후에도 증상이 보고됐다는 점이
이 문서의 새 정보다. 그 수정은 "시야 안 유령이 영구히 남는 것"을 30.6초로 바꾼
것이지 없앤 것이 아니다.

---

### 6.6 두 costmap 의 레이어 구성이 다르다 — global 에는 nvblox 가 없다

정본 `vica_ros2_ws/src/vica_nav2/config/nav2_params.yaml`.

```text
global_costmap (map frame, 1.0 Hz)                                  plugins :1107
├── static_layer      ← SLAM 지도.  지우기 없음 (영구)
├── obstacle_layer    ← LiDAR /scan. observation_persistence 0.5 s        :1138
└── inflation_layer   ← radius 0.55 (:1197), cost_scaling_factor 3.5
    ※ nvblox_layer 블록(:1108)은 있으나 plugins 목록에 없다 → 로드되지 않는다

local_costmap (odom frame, 5.0 Hz, rolling 6×6 m)                   plugins :747
├── voxel_layer       ← LiDAR /scan. observation_persistence 0.5 s         :831
├── nvblox_layer      ← 카메라 esdf slice                                  :748
└── inflation_layer   ← radius 0.55 (:1028), cost_scaling_factor 3.5
```

**따라서 `Starting point in lethal space` 는 nvblox 로 설명되지 않는다.** 그쪽은 static
지도·LiDAR·위치 추정 오차다. 반대로 nvblox decay 는 local 에만 영향을 주므로 **경로 계획을
망가뜨리지 않는다** — decay 를 빠르게 하는 변경의 위험이 그만큼 작다(§10.2).

### 6.7 planner 와 controller 가 253 밴드를 다르게 본다 — 우회 실패의 구조적 원인

`nav2_params.yaml:425-434` 주석이 두 구현의 디스어셈블 결과를 기록해 두었다.

```text
dwb_critics  ObstacleFootprintCritic::pointCost
    cmp w1, #0xfe (254 LETHAL) / #0xff (255 UNKNOWN) → 예외
    0xfd(253 INSCRIBED) 비교가 없다               → 253 을 통과시킨다

nav2_smac_planner  GridCollisionChecker::inCollision
    cmp w1, #0xfc (252 초과 = 253 이상)           → 충돌 (footprint 전체)
```

**253 밴드는 controller 에게 합법이고 planner 에게 금지다.**

```text
DWB 가 footprint 를 253 밴드에 넣으며 정상 주행
  → 그 자리에서 planner 가 "Starting point in lethal space"
  → 부딪히지도 않았고 나갈 공간이 있는데 굳는다
```

활성 planner 는 `GridBased`(:1396) = `nav2_smac_planner/SmacPlannerLattice`(:1397)다.
`GridBasedAlt`(= Hybrid, :1289)는 `planner_plugins` 목록에 없어 로드되지 않는다.
Lattice 격자는 설치본에 **0.5 m 와 1 m 두 가지뿐**이고 이미 작은 쪽을 쓰고 있다
(`0.5m_turning_radius/diff/output.json`, primitive 112개 중 길이 0 인 **제자리 회전 32개
포함**).

**이것은 knob 결함과 무관한 코드 수준 사실이므로 2026-08-11 회차가 무효여도 그대로 남는다.**
2026-07-30 실측(bins36_people)도 있다 — 정지 지점의 로봇 중심 셀 값이 74~79(경사 안, 합법)
였고 중심이 99 이상인 샘플은 36개 중 0개였다.

**[미검증]** 이 비대칭이 이번 증상에서 몇 번 발동하는지는 기준선 측정의 `lethal space`
횟수로 센다.

---

## 7. 원인을 가르는 관찰 — 젯슨에서 할 것

### 7.0 주행 전 30초 — knob 확인 (필수, 2026-08-12 추가)

**이 단계를 건너뛰면 그날 데이터가 통째로 무효가 될 수 있다. 2026-08-11 이 그랬다(§0.1).**

knob 은 토픽으로 나오지 않는다. motor node 가 발행하는 토픽은 `/motor/can_ok` 하나뿐이고,
knob 값은 콘솔 로그로만 보인다(`mdrobot_can_keyboard_knob_node.py:556`).

```bash
# ① motor node 를 띄운 터미널의 로그. 48 % 근처여야 정상이다
#    knob1= 48%  limit=(0.26m/s,0.40rad/s)  cmd=(+0.00,+0.00)  out=(+0.00,+0.00)
#    knob1=  0%  limit=(0.00m/s,0.00rad/s)  ← 이 상태면 그날 데이터는 못 쓴다

# ② CAN 에서 직접: 701 프레임의 d[6] 이 knob 값
candump can1 | grep -m3 " 701 "
#   can1  701  [8]  F1 00 58 18 05 04 00 00
#                                    ↑ d[6]=0x00 → knob 0 %
```

주행 중에도 한 번 더 가른다.

| 관찰 | 가리키는 곳 |
| --- | --- |
| `/cmd_vel_safe` 가 0 이 아닌데 로봇이 안 움직임 | **구동계.** knob·CAN·드라이버 전원 |
| `/cmd_vel_safe` 가 계속 0 | Nav2 가 명령을 못 만듦 → 7.1~7.3 으로 |

### 7.1 눈으로 30초

| 정지 중 로봇의 행동 | 원인 | 다음에 볼 곳 |
| --- | --- | --- |
| **±17° 좌우로 조금씩 회전한다** | BT 복구가 도는 중 | `Wait` / `Spin` |
| 미동 없이 서 있다 | costmap 이 계속 막혀 있음 | `inflation` / nvblox decay |

### 7.2 로그

```bash
ros2 topic echo /behavior_tree_log | grep -iE "Wait|Spin|Clearing|RoundRobin"
```

Nav2 터미널에서 찾을 문구:
- `Starting point in lethal space` — 로봇이 이미 LETHAL 안
- `Failed to create plan` / `No valid path` — planner 가 경로 자체를 못 만듦
- `Collision Ahead - Exiting Spin` — Spin 이 충돌 판정으로 중단

**"planner 실패 없이 Wait 만 도는 것"과 "planner 가 계속 실패하는 것"은 대책이 다르다.**

### 7.3 bag

**`/cmd_vel_safe`(명령)와 `/wheel/odom`(실제 이동)을 반드시 함께 넣는다.** 이 둘이 있어야
"명령을 못 만든 것"과 "바퀴가 안 된 것"이 구분된다(§0.1).

```bash
ros2 bag record -o dyn_obstacle_$(date +%H%M) \
  /cmd_vel_req /cmd_vel_safe /wheel/odom /odom \
  /local_costmap/costmap /global_costmap/costmap \
  /nvblox_node/static_map_slice \
  /plan /behavior_tree_log /scan /tf /tf_static
```

```bash
# 사람이 떠난 뒤 slice 에서 실제로 지워지는 데 걸리는 시간
ros2 topic hz /nvblox_node/static_map_slice
```

계산상 최대 30.6초다. **실측이 몇 초인지가 증상 2 의 원인이 nvblox 인지 BT 인지를 가른다.**

### 7.4 시작 전

```bash
nvpmodel -q ; jetson_clocks --show ; uname -m
```

---

## 8. ⚠️ 하지 말 것

- **원인 확정 전에 값을 바꾸지 말 것.** `mapping_type` 을 4회 왕복한 이력이 있고, 그때
  원인이 아니었던 값을 바꾸느라 시간을 썼다. 진짜 원인은
  `exclude_last_view_from_decay` 였다.
- **한 번에 하나만 바꿀 것.** 두 개를 같이 바꾸면 무엇이 들었는지 가릴 수 없다.
- `inflation_radius` 를 **0.50 이하로 내리지 말 것** — `nav2_backlog.md` §9, 사용자 판정.
- **baseline 을 잃지 말 것** — 유일하게 3/3 완주한 조합은
  `static_tsdf` + 두 costmap 모두 `static_map_slice` + scale 0.15 + decay 2.5 (145.6 s).
- dynamic 3회 실패를 근거로 **people 모드를 배제하지 말 것** (§2.5).
- **2026-08-11 회차 로그를 근거로 값을 바꾸지 말 것** — knob 0 이다(§0.1).
- **기준선 없이 값을 바꾸지 말 것** — 개선 여부를 측정할 수 없다.
- `ObstacleFootprint.scale` **재시도 금지** — 0.15 부딪힘 / 0.6 궤적 전멸 152회 /
  0.25 갇힘 최다. **두 planner(Hybrid·Lattice)에서 확인됐다.**
- `convert_to_binary_costmap` **을 False 로 바꾸지 말 것** — 2026-08-12 소스 확인:
  True 는 "전부 LETHAL"이 아니라 **장애물 내부만 LETHAL·나머지 FREE** 다. False 로 하면
  `inflation_distance`(기본 0.5 m) 이내가 **253** 이 되어 planner 가 더 막힌다.
- Lattice 격자를 더 작은 것으로 바꾸려 하지 말 것 — 설치본에 **0.5 m 와 1 m 뿐**이고
  이미 작은 쪽이다.

원인이 갈린 뒤의 첫 후보만 적어 둔다. **전체 목록과 순서는 §10 에 있다.**

| 확정된 원인 | 첫 후보 | 파일 |
| --- | --- | --- |
| BT Wait/Spin 이 시간을 만듦 | `wait_duration="5"` | `behavior_trees/vica_navigate_to_pose_no_backup.xml:98` |
| planner 가 경로를 못 만듦 | §6.7 의 253 비대칭을 먼저 볼 것 | `nav2_params.yaml:425-434` |
| 사라진 사람이 안 지워짐 | `decay_tsdf_rate_hz: 2.5` | `vica_nvblox_overrides.yaml` |

`inflation_radius` 는 `nav2_params.yaml:1028`(local)·`:1197`(global)이다.
**0.50 이하는 닫혔다.**

---

## 9. 미결 — 다음 세션이 판단할 것

1. **증상 1·2 의 원인** — §7 관찰 결과 대기. 미확정.
2. **people 모드를 시험할 것인가** — 미시험. 구조적으로 dynamic 과 다르다(§2.5).
   비용은 unet 상시 GPU + 이용자 오검출.
3. **YOLO 도입 경로 A/B/C** — 라이선스 판단("아직 모름")이 A/B 를 가른다.
4. **release-3.2 에 bbox 오버로드가 있는가** (§4) — Jetson 에서 확인.
5. **E-stop 후 상황 설명 기능** — 브레인스토밍 §범위까지만 합의됐다.
   "설명 계층만 설계하고 근접 긴급정지는 후속 방향으로만 남긴다"가 사용자 결정.
   발화 내용(원인 포함 여부 / 거리 포함 여부)은 미결. 설계 미착수.
   - 확인된 배선: `/estop_state`(Bool, 구독 전용 패턴 확립),
     `/vica/tts_request`(String, `"{priority}:{text}"`, emergency/response/narration)
   - 공백: `EmergencyLatch.active_sources` 가 원인을 구분하지만 밖으로 나가는 것은
     `/emergency_stop`·`/estop_state` **Bool 둘뿐**이다. 원인을 말하려면 인터페이스가 필요하다.
   - **근접 사람 감지로 인한 긴급정지는 현재 존재하지 않는다.** `vica_safety` 에 거리·
     `scan` 판정 코드가 없다. E-stop 원인은 물리 버튼·앱·STT 긴급어·motor CAN 넷뿐이다.
     도입하려면 `NAV2-B5`(collision_monitor)와 §9 의 이용자 상시 발동 문제를 함께 풀어야 한다.
6. **AMCL·지도 품질** — **다른 테스트 브랜치 담당이다.** 이 브랜치에서 손대지 않는다.
   장소가 바뀌어 `vica_map_0810` 을 새로 만들었으므로 `Starting point in lethal space` 의
   일부는 지도-실제 불일치일 수 있다.

---

## 10. 하드웨어 없이 지금 조정할 수 있는 축 (2026-08-12 전수 조사)

하드웨어(손잡이 지지대)는 추후 진행이 확정됐으므로, **파라미터로 열려 있는 축만 정리한다.**
`--symlink-install` 이라 **config·launch·파이썬은 재빌드 없이 반영된다.**

### 10.1 증상 3 (우회 실패) — 가장 유망

| # | 축 | 현재 | 후보 | 근거 | 위험 |
| --- | --- | --- | --- | --- | --- |
| **R1** | `max_vel_theta` | **0.4** | 0.6 | **회전 속도 관문 4개 중 유일한 병목.** smoother 1.0 / Safety 2.0 / knob 0.96 은 안 걸린다(`2026-08-01-bt-recovery-and-spin-speed.md` §6). `sim_time 2.5 × 0.4 = 1.0 rad = 57°` — **2.5초 궤적에서 57도밖에 못 돈다. 90도 코너를 한 궤적으로 계획할 수 없다** | 중간. 안내 로봇이라 급회전은 이용자 위험 |
| **R2** | `PathDist.scale` | **32.0** | 24 → 16 | **한 번도 조정된 적 없는 Nav2 기본값.** "회피가 27배 손해"의 **분모**다. `ObstacleFootprint.scale`↑ 는 궤적을 *탈락*시켜 실패했지만, 이쪽은 *순위*만 바꾼다 — 탈락 판정은 footprint 가 254 를 밟는지로 정해지므로 **궤적 전멸이 늘지 않는다** | 중간. 경로 이탈 증가 |
| R3 | `acc_lim_theta` | 2.0 | 2.5 | smoother 가 3.2 까지 허용. R1 을 올려도 가속이 낮으면 실제로 못 돈다 | 낮음 |
| R4 | `vtheta_samples` | 20 | 30 | 회전 궤적 해상도. 좁은 곳에서 해를 찾을 확률 | 낮음(계산량↑) |

회피 판단은 두 값의 비교다.

```text
장애물 비용 79 × ObstacleFootprint.scale 0.15 =  11.8
경로 이탈 10셀 × PathDist.scale 32            = 320.0   → 회피가 27배 손해
        ↑ 올리는 쪽은 실패 확정                  ↑ 내리는 쪽은 미시도
```

**슬로우스탑 주의**: `/speed_limit` 40 % 단계에서 `max_vel_theta` 가 0.16 rad/s(9°/s)까지
떨어져 2.5초에 **23도**밖에 못 돈다. 목적지 근처에서는 우회가 사실상 불가능하다.

### 10.2 증상 2 (지나간 뒤 20초 정지)

| # | 축 | 현재 | 후보 | 효과 | 위험 |
| --- | --- | --- | --- | --- | --- |
| **D1** | `decay_tsdf_rate_hz` | 2.5 | 5.0 | 소멸 30.5초 → **15.3초** | **낮음.** nvblox 는 local costmap 에만 있다(§6.6). 벽 기억은 static 지도·LiDAR 담당 |
| D2 | `tsdf_decay_factor` | 0.95 | 0.90 | 30.5초 → 약 15초 | D1 과 효과가 같다. **택1** |
| D3 | `esdf_slice_max_height` | 0.9 | 하향 | 사람 상체를 안 담으면 잔상 감소 | 높음. 사람을 놓칠 수 있다. **후순위** |

```text
5.0 × 0.95ⁿ ≤ 0.1  →  n = 76.3 tick  →  76.3 ÷ 2.5 Hz ≈ 30.5초
  projective_integrator_max_weight 5.0  (nvblox_base.yaml:78)
  esdf_integrator_min_weight       0.1  (nvblox_base.yaml:95)
```

실질 소멸을 정하는 것은 `tsdf_decayed_weight_threshold`(0.001)가 아니라
`esdf_integrator_min_weight`(0.1)다. LiDAR 는 `observation_persistence: 0.5` 로 **0.5초**에
지운다 — **60배 차이**다. costmap 은 `updateWithMax` 로 합치므로 큰 쪽이 이긴다.

### 10.3 증상 1 (원거리 주춤거림) · 정지 시간 자체

| # | 축 | 현재 | 후보 | 비고 |
| --- | --- | --- | --- | --- |
| S1 | `sim_time` | 2.5 | 조정 | 검사 도달 1.0 m. 계산량 1.5배, 좁은 곳 불리 이력 |
| S2 | Oscillation `reset_dist` | **미설정**(기본 0.05) | 명시·하향 | 조금씩 움직일 때 reset 이 안 되면 탈출 방향이 봉쇄된다 |
| W1 | BT `<Wait wait_duration>` | **5** | 2~3 | 복구 1순환당 5초를 통째로 서 있다 (`:98`) |
| W2 | `RecoveryNode number_of_retries` | 6 | — | 6회 순환 시 Spin 3회 + Wait 1회 |

### 10.4 하드웨어 — 세로 90 → 81 cm 는 얼마짜리인가 (참고)

현재 footprint 에서 역산하면 핸들 좌우 반폭은
`√(0.645² − 0.595²) = 0.249 m` 다. 앞(0.305)을 두고 뒤만 줄이는 경우:

| 세로 길이 | 뒤 돌출 | 스윕 반경 | 제자리 회전 필요폭 | 1.0 m 통로 |
| --- | --- | --- | --- | --- |
| **0.900 (현재)** | 0.595 | 0.645 | **1.29 m** | ❌ 29 cm 부족 |
| **0.81 (예정)** | 0.505 | 0.563 | **1.13 m** | ❌ **13 cm 부족** |
| 0.73 | 0.425 | 0.492 | 0.98 m | ⭕ 겨우 |
| 0.61 | 0.305 | 0.394 | 0.79 m | ✅ 21 cm 여유 |

**81 cm 로도 1.0 m 통로에서는 제자리 회전이 안 된다.** 다만 회전 가능한 최소 통로 폭이
1.29 → 1.13 m 로 내려간다. 직진 폭은 세로 길이와 무관하고 좌우가 정한다 —
내접반경 `0.2275 + padding 0.05 = 0.2775`, 양옆이므로 **최소 0.555 m**.

**[미검증]** 위 판정은 통로 1.0 m 가정이다. 장소가 바뀌었으므로(`vica_map_0810`)
**실제로 막히는 지점의 폭을 재야 목표 치수가 확정된다.**

---

## 11. 다음 회차 계획

### 11.1 기준선 (아무것도 바꾸지 않는다)

증상이 셋이므로 시나리오도 나눈다. **우회 능력은 통과 시나리오로 측정되지 않는다.**

| # | 시나리오 | 재는 증상 | 판정 |
| --- | --- | --- | --- |
| S1 | 사람이 앞 1.5 m 를 가로질러 지나가고 **비켜선다** | 증상 2 | **떠난 뒤 재출발까지 몇 초** |
| S2 | 사람이 앞 1.5 m 에서 멈춰 **계속 서 있다** | 증상 3 | 돌아가는가 / 굳는가 |
| S3 | 사람이 **3 m 밖에서 서성인다** | 증상 1 | 속도 끊김 횟수, `Passing new path` 빈도 |

각 3회. 기록은 §7.3 bag(`/cmd_vel_safe` + `/wheel/odom` 필수).

### 11.2 변경 순서 — 한 번에 하나씩

| 순 | 변경 | 성공 신호 | 되돌리는 신호 |
| --- | --- | --- | --- |
| 1 | **D1** `decay_tsdf_rate_hz` 2.5 → 5.0 | S1 재출발 시간 뚜렷이 감소 | 책상다리·청소기를 다시 놓침 |
| 2 | **R1** `max_vel_theta` 0.4 → 0.6 | S2 우회 성공 | **사람이 못 따라옴**(최우선), `No valid` 급증 |
| 3 | **R2** `PathDist.scale` 32 → 24 | S2 우회, S3 끊김 감소 | 경로 이탈 과다, `No valid` 급증 |
| 4 | R3 / R4 → S1 / S2 → W1 | 각 증상별 | — |

**D1 은 주행 없이 미리 잴 수 있다.** 로봇을 세운 채 카메라 앞으로 사람이 지나가게 하고
slice 에서 지워지는 시각을 재면 된다(§7.3). 계산값 30.5초와 크게 다르면 D1 을 후순위로
내린다.

### 11.3 판정 지표

```bash
grep -c "No valid trajectories"   ~/.ros/log/controller_server_*.log
grep -c "lethal space"            ~/.ros/log/planner_server_*.log
grep -c "missed its desired rate" ~/.ros/log/controller_server_*.log
grep -c "Collision Ahead"         ~/.ros/log/behavior_server_*.log
```

| 지표 | 2026-08-02 확정본 | 2026-08-11 (무효) | 기준선 |
| --- | --- | --- | --- |
| No valid trajectories | 2 | 954 | **미측정** |
| lethal space | 0 | 16 | **미측정** |
| 사람이 떠난 뒤 재출발까지 | — | 측정 불가 | **미측정** |

**판정은 사람 기준도 함께 본다.** 로봇이 통과하는지가 아니라 손잡이를 잡은 사람이 따라올 수
있는 경로인지(유턴 반경·핸들 스윕)를 눈으로 확인한다. 특히 R1·R2 는 회전과 경로 이탈을
늘리는 방향이라 이 판정이 필수다.
