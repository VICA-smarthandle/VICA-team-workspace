# 개발 로그 2026-07-27 — TONY0043 (음성·웨이크워드)

## 요약

vica-wakeword 단계 1(녹음 도구)을 구현해 4화자 × 비카야 60개 = 240개 실녹음을
수집했고, 단계 0b(실제 학습) 환경을 학습 PC에 구축했다. 합성→실제 전이를 실측해
"합성은 보조" 원칙(명세 8-1절)의 근거를 확보했다.

## 작업 환경

- 학습 PC(Ryzen 7 7700X / RTX 4070 Ti 12GB / RAM 30GB / Ubuntu 24.04 / NVMe)에
  workspace 를 구성했다. `GOVERNANCE.md` 9절 절차 + `vica-wakeword` 추가 clone.
- 명세 9-3절의 "RTX 4070 Super"는 실물 기준 **4070 Ti**다. 동급이라 지장 없음.
- reSpeaker 는 USB 식별상 "ReSpeaker 4 Mic Array (UAC1.0)" (2886:0018, XVF-3000,
  6ch/16kHz/16bit)로 잡힌다. 명세의 "v3.0" 표기와 실물 라벨 대조 필요. [미검증]

## 녹음 도구 (단계 1)

- 설계 승인 후 구현: `vica-wakeword/recorder/` (FastAPI + 순수 HTML/JS, DB 없음).
  설계 문서 `vica-wakeword/docs/recording-tool-design.md`.
- **6채널 전부 저장** 결정 — 명세 15절의 채널 정본(ch0 vs raw) 결정을 녹음 후에도
  되돌릴 수 있게 하기 위해. 용량 6배는 실측 화자당 약 360MB 로 수용.
- 품질 검사 6항목은 측정값을 메타데이터에 저장하고 임계값(`recorder/config.py`,
  잠정)을 바꾸면 재계산만으로 재분류된다. 통과 클립은 자동 저장·자동 진행.
- 서버는 127.0.0.1 전용(사람 음성 데이터 보호).

## 데이터 수집 현황 (2026-07-27 저녁 기준)

| 화자 | 모델 A(비카야) | 모델 B | 함정어 |
| --- | --- | --- | --- |
| speaker_001~004 | **각 60/60 완료** | 216+7/900×4 진행 중 | 0/75×4 |

- 모델 A 240개 품질: pass 201 / check 35 / fail 4. 상황 6종 × 40개 균등.
- 백업: `~/vica-wakeword-backup/recordings-20260727-2135.tar.gz` (177MB, 파일 수
  대조 완료). **같은 디스크라 외부 사본 필요.** [남은 일]

## 단계 0 진행

- 단계 0a 이관 검증: 합성 클립 재생성(a 290 / b 530) 후 probe_embedding 수치가
  Jetson 결과(docs/stage0-findings.md)와 같은 방향으로 재현됨. 세트 b는 거의 일치
  (0.5에서 46/50, 스톱·스탑 제외 시 29/30·오탐 0). 세트 a는 소폭 약함(0.7에서
  8/10 vs 9/10) — supertonic 버전 차이로 추정. [미검증]
- **합성→실제 전이 실측** (findings 6절 "실녹음 확인" 항목): 합성음으로만 학습한
  판별기가 실녹음 비카야 240개에서 임계값 0.5 기준 125/240(52%), 0.3 기준 65% 재현.
  신호는 전이되지만 절반 수준 → 실녹음이 정본, 합성은 보조라는 명세 8-1절 원칙의
  실측 근거. `experiments/stage0/import_recordings.py` 로 재현 가능.
- 단계 0b 환경 구축: openWakeWord GitHub `368c0371` 고정, `.venv-train` 분리(R8),
  **tensorflow 불필요 확인**(tflite 변환이 지연 import 뒤로 분리됨). ACAV100M
  17.28GB + 검증셋 + MIT RIR 270개 확보. 세부는 `vica-wakeword/docs/setup-pc.md` 4절.
- **비카야 첫 학습 완료 — 한국어 openWakeWord 학습 성립 (R1 해소).** 미학습 화자
  재현율 88% @0.5 (슬라이딩 창 최대, 합성 함정어 오탐 1/48), whisper 기준선 0% 대비.
  CPU 약 20분, 산출물 `vica-wakeword/data/stage0b/vica_bikaya_v0.onnx`.
  상세·한계는 `vica-wakeword/docs/stage0b-findings.md`.

## 남은 일

1. 함정어 트랙 녹음 (전 화자 0/75) — 오탐 평가·학습에 필수, 현재 최우선
2. 모델 B 녹음 계속 (R1 해소로 3,600개 투입 근거 확보)
3. 배경음 코퍼스 선정 + 증강 실효 배수 확보 (stage0b-findings 한계 3·4항)
4. Jetson 에서 vica_bikaya_v0.onnx 실측 (RTF·지연·실환경 마이크)
5. 녹음 백업 외부 사본, 품질 임계값 확정(파일럿 분포 검토)
6. vica-wakeword 저장소를 `workspace.repos`/`.gitignore` 에 반영할지 팀 결정
