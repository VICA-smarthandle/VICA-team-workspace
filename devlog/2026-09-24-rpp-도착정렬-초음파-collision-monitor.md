# RPP 도착 정렬 · 초음파 실주행 · collision_monitor 이전 (2026-09-24)

run37 ~ run43(bag `~/vica_data/bags/route0921_run3x/4x`). 지도 `vica_map_0903_d`, 레일 GeoJSON.
브랜치 `test_Route_Server`(루트·ros2 둘 다). 앞 회차(run35·36)는
`devlog/2026-09-21-mppi-제어주기-temperature.md` 끝.
실주행 보고 페이지(비공개 아티팩트): https://claude.ai/artifact/SuZS1t9kjSRfLHZ5aLAwK9

## 0. 오늘 요약

- **도착 정렬 헌팅의 원인은 회전 넘침이었다.** 회전 0.5 rad/s·가속 2.0 이면 제동이
  목표 3.6° 앞에서야 시작되고 모터 응답 0.3~0.5 s 동안 9~14° 를 더 돈다. 넘침 ±16~21°
  가 허용 ±14.3° 를 넘어 끝없이 좌우로 흔들렸다. 네 값을 되돌리자(1daee2e) 사라졌다.
- **남은 도착 결함은 반경 경계다.** 목적지 0.26 m(허용 0.25)에서 방향을 +0.8° 로 맞추고도
  반경 밖이라 RPP 가 경로 끝점 쪽으로 +145° 까지 돌았다 온다. 파라미터로 안 풀린다
  → "반경 안에서 남은 각만큼 한 번 회전"(Spin) 설계 대기.
- **초음파는 costmap 에 두면 멈춘다.** RangeSensorLayer 마크는 같은 센서가 그 칸을 다시
  비춰야만 지워진다. 로봇이 서면 영원히 남아 RPP 가 collision ahead 로 서고, 10 s 인내 뒤
  지도를 지우자마자 출발한다. run43 완전 정지 9회가 전부 이 모양. → collision_monitor
  Range 소스로 이전(413c8dd).
- **bag 기록기가 스택을 흔든다.** 41토픽 기록기가 CPU 폭주의 방아쇠였고(run40), 기동
  직후 건 기록기를 멈춘 직후 EKF 가 완전히 멈췄다(run43 첫 시도). 공유메모리 락이 남은
  상태에선 기록기만 UDP 로 받게 하고, nav2 기동 60 s 뒤에 건다.

## 1. 회차 표

| 회차 | 바꾼 것 | 판정 | 핵심 수치 |
|---|---|---|---|
| run37 | 초음파 8채널 첫 실기 + 로봇 대장(LLM) | 초음파 무효 | 초음파 메시지 0 = 드라이버 TF 문턱 굶주림 → 2스레드 실행기(52f1ee6) |
| run38 | 초음파 8층 유효 | 후방 off 결정 | 출발 지연 2.9~13.8 s(7/8), collision ahead 143/분 → 후방 2층 off(9ad3caa) |
| run39 | `min_lookahead_dist` 0.45→0.6, 핸들 off | 유효 | 직진 손잡이 좌우속도 0.057(최고), 409호 첫 도착 −145° 실패 |
| run40 | 도착축 4개(442ff8f) | **무효** | CPU 평균 89 %, EKF TF 1.9 Hz·3.4 s 지연 → 41토픽 기록기가 방아쇠 |
| run41 | 같은 4개 재시험(CPU 정상) | 실패 | 409호 59.6 s 헌팅 취소, 테스트1 54.8 s. TF 30 Hz·14 ms |
| run42 | 4개 되돌림(1daee2e) | 유효 | 테스트1 9.4 s −9°, 409호 2.1 s −7°. 반경 경계 대회전 1건 26.7 s |
| run43 | 초음파 6층(후방 off) | 정지 과다 | 5/5 성공, 완전 정지 9회 전부 collision ahead 10 s |
| run44 | 초음파 → collision_monitor(413c8dd), 뒷바퀴 옆 off | 예정 | — |

## 2. 도착 정렬

### 2.1 도착축 4개가 헌팅을 만든 기작 (run41)

`planner tolerance 0.05 · PoseProgressChecker · rotate_to_heading_angular_vel 0.5 ·
max_angular_accel 2.0`. bag 을 0.5 s 간격으로 풀어 보면:

```
409호  d=0.18~0.30 m 에서  +18.6 → −18.1 → +18.8 → −19.2 → +16.1 → −17.6 °  반복
테스트1 d=0.18~0.20 m(반경 안)에서도 ±17~21° 반복, 54.8 s 뒤에야 성공
명령 w 와 odom w 가 0.3~0.5 s 어긋남(모터 응답 지연)
```

- RPP `rotateToHeading` 은 `sqrt(2·a·θ)` 로 제동한다. a 2.0·상한 0.5 이면 θ < 3.6° 에서야
  감속이 시작되고, 지연 0.3~0.5 s × 0.5 rad/s = 9~14° 가 더해져 허용 14.3° 를 넘는다.
  0.35/1.2 이면 넘침 ≈ 10° 로 허용 안에 선다(run42 실측 9°).
- StoppedGoalChecker 는 **멈춘 상태에서** 허용 안이어야 성공이라, 0° 를 지나칠 때는 성공이
  안 되고 넘친 뒤 되돌아온다.
- 반경 0.25 경계를 넘나들면 RPP 가 당근 **위치** 쪽으로 돌아 −110°·−154° 까지 크게 돈다.
- planner tolerance 0.05 는 목적지 근처 "no valid path" 15회 → 컨트롤러 취소 → Wait →
  14 s 정지 · "Invalid path, Path is empty" 18회를 만들었다.
- **[정정]** run40 의 ±30° 는 TF 지연 탓으로 봤으나, TF 가 정상인 run41 에서 재현됐다.
  원인은 이 넘침이다.

### 2.2 되돌린 뒤 (run42)

| 목적지 | 정렬 | 최종 오차 | 모양 |
|---|---|---|---|
| 테스트1 | 9.4 s | −9° | 174° 를 한 번에 돌고 9° 넘쳐 허용 안 착지 |
| 테스트1 | 26.7 s | −9° | d 0.26 m 에서 +0.8° 로 맞췄으나 반경 밖 → +145° 까지 돌았다 복귀 |
| 409호 | 2.1 s | −7° | — |

작은 각 반복 헌팅은 0회. 남은 것은 **반경 경계 대회전**이다(run39 테스트1 29.4 s 도 같은
설정·같은 모양). RPP 는 회전 목표 전환 거리와 도착 판정 반경이 같은 값(xy 0.25)이라
경계에서 두 모드를 오간다.

**다음 설계(선승인)**: BT 에서 FollowPath 가 xy 로 끝나면 남은 yaw 오차를 계산해
behavior_server `Spin(spin_dist)` 을 한 번 부른다. 한 방향으로 한 번 돌고 끝나 헌팅이
구조적으로 없다. 커스텀 BT 노드 1개(새 프로세스 아님). 위험: Spin 의 충돌 검사
("Collision Ahead - Exiting Spin")가 벽 옆에서 중단할 수 있다. 사용자 제안은 135° 이상에만
쓰는 것이었으나, 헌팅은 ±18° 에서 났으므로 허용치를 넘는 모든 오차에 쓰는 편을 권한다.

## 3. 초음파

### 3.1 설정 정리

| 항목 | 값 | 어디서 |
|---|---|---|
| 물리 빔(듣는 폭) | 50°(레벨 3) | 펌웨어 `US_ANGLE_LEVEL 0x03` |
| 칠하는 폭 | 30° | 드라이버 `ultrasonic_fov_rad 0.524` → 메시지 fov → RViz·costmap·collision_monitor |
| 최대 거리 | 1.5 m | `ultrasonic_max_range_m`, 에코 없음 = 1.5 |
| 발사 | 등진 둘씩 4라운드, 채널당 2.19 Hz | 앞왼·후오 / 앞오·후왼 / 바퀴옆 둘 / 뒷바퀴옆 둘 |
| stamp 지연 | 0.11~0.42 s(라운드별 보정) | `ultrasonic_measurement_delay_ms` |

RViz 부채꼴은 메시지 fov 를 그리므로 30° 다. 30° 는 09-02 A/B 에서 50° 대비 부분 감속
1/8 로 확정한 값.

### 3.2 run38·run43 — 왜 서는가

```
                 초음파     collision ahead/분   완전정지(≥1s)/분
run38            8층        143                  1.7
run43            6층        110                  1.5
run39 · run42    off        2 · 0                0.2 · 0
```

run43 완전 정지 9회, 전부 같은 순서다(14:48:34 사례).

```
14:48:33.99  RegulatedPurePursuitController detected collision ahead!   (라이다 전방 1.26 m)
   … 10 s 동안 cmd 0 …
14:48:44.19  Controller patience exceeded → Aborting → local_costmap 전체 지우기
14:48:44.39  cmd +0.25 → 14:48:45 +0.50 m/s   (지우자마자 출발 = 실제로는 안 막혀 있었다)
```

- RangeSensorLayer 는 새 측정이 **그 센서의 지금 부채꼴 안**만 다시 쓴다. 로봇이 움직이거나
  서면 옛 마크가 부채꼴 밖에 남아 영원히 안 지워진다. 라이다는 자기 층만 지우므로 못 지운다.
  지도는 층 중 최댓값을 쓰니 RPP 가 선다. Humble RangeSensorLayer 에는 시간 소멸 파라미터가
  없다(설치 헤더 확인: phi·inflate_cone·clear/mark_threshold·clear_on_max_reading·
  no_readings_timeout 뿐).
- 원인 분류(사건 12개): 유턴·회전 중 뒤 사용자(후방 0.2~0.3 m) + 옆/뒷바퀴 옆 0.35~0.64 m
  **4** · 사람 앞 0.6~0.7 m(라이다) + 앞/옆 초음파 **4** · 근처 없음 **1**(run38 은 14).
- 10 s 는 `failure_tolerance 10.0` — DWB 시절 "앞 사람을 지도 안 지우고 조용히 기다린다"는
  값이다. 줄이면 정지 시간은 줄지만 정지 횟수는 그대로이고 복구 진입이 잦아진다.

### 3.3 라이다 대조와 혼선

초음파 에코(<1.5 m)마다 같은 원뿔(±25°) 안 라이다 최근접과 비교(run43):

| 센서 | 에코 | 라이다와 일치 | 초음파만 |
|---|---|---|---|
| 앞 왼·오 | 95 · 88 | 75 · 60 % | 12 · 11 % |
| 바퀴 옆 왼·오 | 1,831 · 770 | 92 · 90 % | 1 · 2 % |
| 뒷바퀴 옆 오·왼 | 718 · 1,838 | 88 · 94 % | 4 · 3 % |
| 후방 오·왼 | 612 · 634 | 62 · 67 % | 28 · 20 % (= 뒤에 선 사용자) |

- **혼선 증거 없음.** '초음파만' 사건에서 동시발사 짝이 같은 거리를 본 것은 0~4건.
  후방 둘이 자주 같은 값을 낸 것은 같은 사람을 본 것이다.
- 옆·뒷바퀴 옆은 라이다와 거의 같은 것을 본다(중복). 초음파만의 몫은 앞 센서와
  낮은 물체·유리.
- 라이브 프로브로 1.3 Hz 를 잰 것은 프로브 쪽 오류였다. bag 기준 2.19 Hz 정상.

### 3.4 결정 — collision_monitor 로 이전 (413c8dd)

검토한 셋:

| 방법 | 옛 마크 | 피해 가기 | 정지 | 작업 |
|---|---|---|---|---|
| failure_tolerance 10→3 | 그대로 | 가능 | 3~4 s 로 짧아짐 | 1줄 |
| 같은 2D 층(라이다+초음파 LaserScan) | 0.5 s 안 소멸 | 가능 | 1 s 안쪽 | 드라이버 수정+설정 |
| collision_monitor Range 소스 | 생기지 않음 | 초음파만 보는 것은 불가 | 감속 위주 | 설정만 |

- 같은 2D 층 방식: Nav2 장애물 층은 매 갱신 "지우기 → 표시하기" 순이라, 초음파가 계속
  보는 물체는 라이다가 지워도 다시 칠해지고 사라진 것은 0.5 s 안에 지워진다. 단 지금 라이다
  층은 3D voxel(5 cm 층)이고 라이다 0.382 m · 초음파 0.091 m 라 다른 층에 적혀 라이다가 못
  지운다 → 별도 2D 층과 드라이버의 LaserScan 발행이 필요.
- **사용자 결정: 짧게라도 서는 것 자체가 주행을 불안정하게 한다 → collision_monitor 먼저.**

적용 내용:
- local costmap 초음파 층 6개 `enabled: False`(블록은 되돌리기용으로 남김).
- collision_monitor `observation_sources` 에 `us_front_left/right`, `us_left/right_wheel`
  (`type: range`, `obstacles_angle 0.05`). 뒷바퀴 옆·후방은 넣지 않는다 — Humble 폴리곤은
  진행 방향을 가리지 않아 뒤 사용자를 찍으면 선다.
- Humble 1.1.20 `range.cpp`: range 가 [min,max] 밖이면 버리고, 안이면 fov 호 위에 점을 찍는다.
  에코 없음(1.5 m)도 점이지만 x 1.65 m 라 폴리곤 밖. `source_timeout 1.0` 보다 오래된 소스는
  무시(fail-open, WARN).
- **폴리곤이 앞(x 0.206~0.946, 폭 ±0.28/±0.32)에만 있어 실제로 발동하는 것은 앞 두 개다.**
  센서 x 0.151 → 거리 0.055~0.295 m 정지, ~0.795 m 감속(40 %). 바퀴 옆 둘은 연결만 됐고
  옆 폴리곤을 만들기 전엔 반응하지 않는다.

run44 판정(미리 정한 숫자):
- 주행 중 collision ahead(분당) ≤ 10, 완전 정지(≥1 s) ≤ 0.3/분.
- collision_monitor 감속 로그 수와 사람 사건의 최근접 거리(≥ 0.5 m).
- 초음파 소스 "Ignoring the source" 경고 수(주기 지연 확인).
- CPU ≤ 60 %.

## 4. 스택·기록기 사건

- **run40**: nav2 재기동 직후 41토픽 기록기를 걸자 8코어 평균 89 %. 기록기를 멈추자 즉시
  35~40 %. EKF TF 1.9 Hz·3.4 s 지연.
- **run40b**: 22토픽 기록기가 fastrtps SHM "Failed init_port 9165/9183" 을 내고 /tf 0건.
  `/dev/shm` 에 09:33 부터의 락 파일이 남아 있음(도커 공유라 손대지 않는다, 리부트로 정리).
- **run41~**: 기록기에만 UDP 전용 프로필(`~/vica_data/dds/udp_only.xml`,
  `FASTRTPS_DEFAULT_PROFILES_FILE`)을 준다. SHM 오류 없이 /tf·/odom 정상 기록.
- **run43 첫 시도**: nav2 기동 직후(14:37:57) 초음파 포함 30토픽 기록기 → load 25, TF 지연
  최대 4 s. 기록기를 멈춘 직후 EKF 가 **완전 정지**(3 s 동안 CPU 0, 스레드 12개 전부 futex
  대기, /odom·odom→base_footprint 발행 0) → AMCL 이 초기위치를 못 받음 → nav2 재기동.
  기록기와 EKF 정지의 인과는 미확정. 이후 nav2 기동 60 s 뒤에 기록 시작 → CPU 51 %·TF 정상.

## 5. 음성 — 청취 창 안의 "멈춰" 는 로봇을 안 세운다 (미수리, run42)

| 시각 | 한 말 | 결과 |
|---|---|---|
| 14:21:48 | 비카야 → "잠깐 멈춰" | 안 섬. 6.6 s 뒤 다시 외친 "멈춰!" 로 정지 |
| 14:23:53 | 비카야 → "잠깐 정지해" | 안 섬. 7.1 s 뒤 "정지" 로 정지 |
| 14:24:44 | 비카야 → "잠깐 멈춰." | 안 서고 5 s 뒤 409호 도착 |

- 청취 창 안 발화는 `/vica/user_text` → `vica_llm_intent_node` 긴급 분기
  (voice `src/ros_node.py`)가 `intent=unknown · safety_flag=emergency` 만 발행한다.
  미션은 이 표시를 navigate 관문(`GateReason.SAFETY_FLAG`)에만 쓰고 정지하지 않는다.
- 모터 정지는 `/vica/emergency`(하드 키워드 `_verify_emergency`) → `emergency_estop_bridge` →
  `/voice_emergency_stop` 경로뿐이다.
- 수리 후보: 텍스트 경로 긴급도 `/vica/emergency` 를 발행. 음성 담당(사용자) 결정 대기.

## 6. 커밋

| 저장소 | 커밋 | 내용 |
|---|---|---|
| ros2 | 52f1ee6 | 핸들 드라이버 2스레드 실행기(TF 게이트 굶주림) |
| ros2 | 9ad3caa | 후방 초음파 층 2개 off |
| ros2 | 852c3db | RPP `min_lookahead_dist` 0.45→0.6 |
| ros2 | 442ff8f | 도착축 4개(tolerance 0.05·Pose·0.5·2.0) |
| ros2 | 1daee2e | 위 4개 되돌림 |
| ros2 | 413c8dd | 초음파 → collision_monitor, costmap 초음파 층 전부 off |

## 7. 다음

1. run44 — collision_monitor 초음파 판정(3.4 의 숫자).
2. 도착 반경 경계 → "남은 각만큼 Spin 한 번" 설계·승인.
3. 옆 감속이 필요하면 옆 폴리곤 설계(Humble 은 방향 무관 — 뒤 사용자 주의).
4. 초음파만 보는 물체를 피해 가야 하면 같은 2D 층 방식(드라이버 LaserScan).
5. 청취 창 긴급어 정지 수리(음성).
6. 보류: 레일 위를 가로지르는 사람을 따라갔다 되돌아오는 현상(판단 지연 1~2.5 s·움직임 예측
   없음, 기다렸다 우회·2 Hz 는 사용자 보류), `rotate_to_heading_min_angle` 2.0, 미션 복귀 시계.

## 8. run44 — collision_monitor 초음파 시험 결과와 원복 (15:50~16:07)

"이렇게도 시험해 봤다" 기록. 413c8dd 를 ccda871 로 되돌렸다(뒷바퀴 옆 2층은 off 유지).

| 지표 | run43(초음파 costmap) | run44(초음파 CM) |
|---|---|---|
| 완주 | 5/5 | 5/5 |
| collision ahead / 분 | 110 | 9 |
| 완전 정지(≥1 s) / 분 | 1.5 | 0.6 |

- **30 cm 꼬깔콘(레일 위)**: 라이다(0.382 m)는 못 봄(전방 1.7~2.4 m 비어 있음). 앞 초음파
  0.13~0.52 m → CM 감속 → 정지 11 s. 지도에 없어 planner·RPP 가 돌아가지 못하고 사용자가 치웠다.
  초음파만 보는 물체를 못 피하는 대가가 그대로 나왔다.
- 초음파 소스 `Ignoring the source`(stamp 가 source_timeout 1.0 s 초과) **660회** — 드라이버
  stamp 지연 0.11~0.42 s + 채널 주기 0.45 s. CM 에서는 자주 무시(fail-open)됐다.
- 복도 사람 회피가 괜찮았던 것은 라이다·카메라 덕(초음파 무관).
- 화장실·407호 구간 직진 흔들림 w 표준편차 0.22(run43 0.06)는 CM 과 무관 — 그 구간 CM 발동은
  라이다 1회. 화장실 구간은 97 % 가 당근(planner) 모드였다. 원인 미조사.
- 사용자 판정: CM 에 초음파는 의미 없음 → 원복.

## 9. 초음파 간섭 — 논문 검토와 과거 이력

### 9.1 검토한 자료

| 자료 | 방법 | 우리(DYP-A22 I2C)에 |
|---|---|---|
| 임형철·이성수, 초음파 센서 간 신호 간섭 제거 방법, 전기전자학회논문지 25(4) 2021 | 이전 값과 비교, 벗어나면 버리되 허용치를 (n+1)ε 로 키움. 초기에만 발사 주기 70~120 ms 무작위 | 가능하나 이미 펌웨어 3점 중앙값이 같은 역할. 한 바퀴 420 ms 라 사람 접근 시 바퀴당 76 cm 변해 ε 잡기 어렵고, 거부마다 0.42 s 지연. 동시 발사 짝의 혼선은 매 주기 같은 값이라 못 잡음 |
| 임형철·이성수, KNN 알고리즘을 활용한 초음파 센서 간 간섭 제거 기법, 26(2) 2022 (`~/Downloads/초음파 간섭.pdf`) | 같은 목표를 보는 센서 3개 값을 KNN 으로 분류(93.9 %) | 사실상 불가. 같은 곳을 보는 3개가 없고, "최댓값이 참" 규칙은 한쪽만 본 진짜 장애물을 지운다 |
| 박상호 외, 초음파 혼선 저감을 위한 위상 기반 온오프 복조기, 한국정보기술학회논문지 20(7) 2022 | 원파형 STFT 위상으로 코드 분할 신호 분리 | 불가. A22 는 거리값·오류코드만 주고 원파형·파형 선택이 없다 |
| DYP-A22 데이터시트·출력 인터페이스(dypcn.com) | 내장 필터, 동주파수 간섭 감지 시 `0xFFFE` 출력. 다중 센서 발사 간격·코드 파형 규정 없음. 혼선 맞춤 설정은 제조사 FAE | — |

### 9.2 우리 방식의 위치

논문 C 의 분류로 우리는 이미 **시간 분할**이다. 등진 짝만 동시에 쏘고(A 앞왼+후오 · B 앞오+후왼 ·
C 바퀴옆 둘 · D 뒷바퀴옆 둘), 감지 범위가 겹치는 센서(앞 둘, 옆 앞·뒤)는 105 ms 떨어져 쏜다.
150 cm 왕복은 8.7 ms 라 잔향 여지가 작다. RViz 부채꼴 겹침은 같은 물체를 두 센서가 보는 중복
감지이지 간섭이 아니다. run43 대조에서 짝과 같은 값 0~4건 → 간섭 증거 없음.

**주의**: 펌웨어는 `0xFFFE`(센서가 스스로 알린 간섭)를 `0xFFFF`·I2C 실패와 함께 `usFail` 로
뭉뚱그린다(smart_handle_firmware.ino 466~478행). 3회 연속이면 값이 0(무효)이 되므로 간섭은
"유령"보다 **"구멍(감지 누락)"**으로 나타난다. 지금은 이를 셀 방법이 없다.

### 9.3 과거 이력

| 시도 | 날짜 | 커밋 | 결과 |
|---|---|---|---|
| 2채널 순차 발사(GAP 5 + WAIT 100) | 09-01 | ee9f72d | 4.9 Hz, 누락 0 |
| 센서별 레이어 분리(겹침 구역 거짓 clear 가 이웃 마크를 지움) | 08-31→09-01 | 77c8bbe | 깜빡임 해소 |
| 지향각·거리 실험 30~60°, 0.3~1.5 m | 08-31~09-01 | e3e72a5·9048bbd | 50°·1.5 m 확정 |
| 칠하는 폭 50°→30° | 09-02 | 3ec7887·1ddca19 | 부분 감속 1/8 |
| 8채널 등진 짝 4라운드 | 09-23 | 2cdfae2 | 2.19 Hz |
| 간접 혼선 검사 | 09-24 | (run43 분석) | 증거 없음 |

**한 번도 안 한 것**: `0xFFFE` 계수, 라운드 순서 변경·무작위 지연, 노이즈 저감 레지스터 0x06,
완료 폴링(0xFFFF 일 때만 다시 읽어 8~12 Hz, 세 번 제안·미구현), **초음파를 라이다와 같은 층
(LaserScan/PointCloud2 변환)에 넣기**. collision_monitor 초음파는 09-01 계획·보류 뒤 오늘
413c8dd 가 처음이다.

### 9.4 제안 (현실성 순)

1. **`0xFFFE` 채널별 계수**(펌웨어 + 파서, 1~2시간) — 간섭이 있는지 센서가 직접 답한다. 먼저 잰다.
2. 필요하면 **짝 내 교대 지연**(EERUF 발상, Borenstein & Koren 1995 — 원문 미열람, 초록 기준): 짝 중
   하나를 매 주기 0/약 3 ms 번갈아 늦게 쏘면 동기 혼선이 주기마다 흔들려 기존 중앙값이 걸러낸다.
   주기 약 2.4→2.3 Hz. 반나절 + 실기 1회.
3. 판정용 **완전 순차 8라운드 A/B**(1.19 Hz, 상시용 아님).
4. 논문 B 적응 허용치는 1번에서 튀는 값이 확인될 때만. KNN·위상 복조는 하드웨어상 부적합.

## 10. run45~47 — 뒷바퀴 옆 off 효과, 바퀴 옆 60°, 0630 지도

같은 잣대(도구 scratchpad `slow_attr.py`): **회전·도착(1.2 m 안)이 아닌데 v < 0.3 인 시간**을
0.1 s 단위로 뽑아, 그 순간 가까이 있던 센서로 원인을 가른다(초음파 앞 ≤ 1.0 m·옆 ≤ 0.5 m,
라이다 앞 ≤ 1.0 m·옆 ≤ 0.45 m, collision_monitor 로그, RPP collision ahead 로그).

| 회차 | 지도 | 초음파 | collision ahead/분 | 완전정지/분 | **초음파만 원인 느려짐/정지** | 라이다 원인(CM 포함) |
|---|---|---|---|---|---|---|
| run42 | 0903_d | 없음 | 0 | 0 | 0 % | 0.5 % |
| run43 | 0903_d | 6층(뒷바퀴 옆 on) | 110 | 1.5 | — | — |
| run45 | 0903_d | 4층(뒷바퀴 옆 off)·옆 50° | 12 | 0.2 | 1.2 % | 3.5 % |
| run46 | 0903_d | 옆 60°(빔+칠하는 폭) | 72 | 1.2 | 8.6 %(꼬깔콘 시험 16 s 빼면 ≈1.8 %) | 7.2 % |
| run47 | **0630** | 옆 60° | 56 | 0.6 | **6.4 %** | 5.9 % |

- **run45**: run43 과 달라진 것은 뒷바퀴 옆 2층 off 하나 → collision ahead 110→12/분, 완전정지 1.5→0.2/분.
  통계 프레임 첫 회차: 7.5분 8,630 판독 중 0xFFFE 9회(전부 costmap 에서 꺼 둔 후방 오른쪽), ffff·i2c 0.
- **run46 꼬깔콘(영상 `~/Downloads/초음파 주행.mp4` = 18:04:02~18:05:42, 45 s 손으로 치움 = bag 18:04:47)**:
  - 노란 콘: 1.29 m 첫 포착 → 0.67 m 에서 정지 → **1.3 s 뒤 재출발, 보고 거리는 0.67 그대로**
    (펌웨어 3점 중앙값이 최대 0.84 s 늦음) → 0.27 m 전진 → 0.06 m(닿음).
  - 18:02:11 콘: 0.94 m 뒤 1.2 s 에코 소실 → 0.15 m 재포착 → 0.03 m 로 **0.58 m 밀고 감**(사용자 "안 섰다").
  - 원인 넷: 콘 모양(구멍·경사) 간헐 에코 · 중앙값이 간헐 에코를 지우고 늦춤 · RPP 는 정지 뒤 저속에서
    투영(0.2 + v×1.2 s)이 짧아 다시 출발해 정적 장애물에 기어 붙음 · CM 은 라이다만(0.38 m 높이라 30 cm 콘 못 봄).
- **run47(0630, 영상 `~/Downloads/초음파 주행2.mp4` = 18:24:43~18:27:51, 앱 목적지 전송 장면으로 동기)**:
  초음파만 원인 9.5 s 중 **8.2 s 가 한 곳** — 화장실 가는 좁은 통로 (-2.6, 5.7). 라이다 앞 1.2 m 가 비었는데
  좌 바퀴 옆(60°) 0.14~0.41 m·앞왼 0.62 m → collision ahead 13 s → patience 10 s → costmap 지우고 출발
  (run43 과 같은 잔상 모양, 넓은 60° 호가 좁은 곳에서 번짐). '저속·근처 없음' 11.8 % 는 미션 접근 감속
  (/speed_limit 80→60 %) 구간과 겹친다(초음파 무관).
- **옆 물체를 비켜 가지 않는다(방2→안내소)**: 박스를 초음파 좌 0.38 m 로 봤지만(라이다 좌 1.2 m, 박스가
  라이다 높이보다 낮음) 0.5 m/s 로 그대로 통과, 18:25:47 엔 라이다 좌 0.34 m(몸통 옆 ≈10 cm)까지 붙음.
  원인은 구조 — **경로를 짜는 planner 는 전역 지도(라이다만)를 보고, RPP 는 선을 따라가며 닿을 때만 선다.**
  DWB 시절 초음파로 코너를 부드럽게 돈 것(09-01 사용자 "낮은 물체와 좁은 폭에서 훨씬 잘한다")은 DWB 가
  근처 지도의 초음파 마크로 궤적 점수를 매겨 **컨트롤러가 스스로 비켜 갔기** 때문 — RPP 에는 그 기능이 없다.
- **결정(사용자)**: 초음파는 라이다와 같은 층에 두더라도 **근처(local) 지도에만**. 전역에는 넣지 않는다
  (방향 부정확 + 전역은 오래 기억). → 옛 마크 멈춤은 고치지만 비켜 가기는 못 고친다는 한계를 알고 진행.

## 11. 초음파 간섭·노이즈 저감 정지 시험 (17:0x~17:4x)

주행과 분리한 정지 시험이다. 도구 `src/vica_user_guidance/firmware/usonic_register_bench.py`
(핸들 드라이버를 끈 채 시리얼 직접), 결과 `~/vica_data/usonic_bench/*.json`.

### 11.1 준비한 것 (ros2 36641d1 · aae0c2f · c0b2e0e)

- **통계 프레임 AA 58**: 약 5 s 마다 채널별 ok / clr(0xFFFD) / ffff / fffe / oth / i2c 횟수.
  드라이버가 `[US 통계 #n] … fffe 합 N` INFO 로 남긴다. 판정 로직과 거리 프레임은 그대로.
- **시험 명령(하향 1바이트, 드라이버는 안 보냄)**: 0x30 기본값 · 0x31~35 노이즈 1~5(8채널) ·
  0x41~44 지향각 1~4(바퀴 옆 ch0·ch3). 적용 뒤 센서에서 되읽어 **AA 59** 로 알린다.
- **발견**: 노이즈 값을 **바꿀 때만** 센서가 수십 ms 무응답(내부 저장 추정) → 직후 0x07 되읽기 전 채널 실패.
  되읽기에 30 ms × 5 재시도를 넣어 해결.

### 11.2 간섭

| 장면 | 결과 |
|---|---|
| run45 주행 7.5분 | 0xFFFE 9 / 8,630 — 전부 costmap 에서 꺼 둔 후방 오른쪽(라운드 A 짝 = 앞 왼쪽) |
| 정지 시험 전 판(약 12판 × 25~30 s) | 0xFFFE **0** |
| 앞 두 센서 빔이 겹치는 곳에 물체(0.5 m 비스듬·1 m 가운데) | 한 센서가 상대 쪽 물체 거리를 읽은 적 0 → 순차 발사가 정상 작동 |

- **결론: 간섭은 사실상 없다.** 발사 순서 변경·지연 교대 같은 대책은 필요 없다.
- 꺼 둔(costmap off) 센서도 펌웨어는 계속 쏜다. 필요하면 펌웨어에서 발사 자체를 뺄 수 있다(지금은 불필요).
- 비스듬한 판은 소리를 거울처럼 튕겨 뒤 물체를 가린다(앞 오른쪽 1 m 가운데 물체 4 %).

### 11.3 바퀴 옆 지향각 50° 대 60°

| 장면 | 50° | 60° |
|---|---|---|
| 빈 곳(바닥 오인식) | 헛값 0 | **헛값 0** |
| 박스 30 / 60 / ≈100 cm | 28~94 cm, 놓침 0/71 | 차이 ≤ 0.6 cm, 놓침 0/71, 표준편차 ≤ 0.5 cm |
| 빔 가장자리 꼬깔콘(≈0.5 m) | 27~40 % | **≈100 %** |
| 빔 가장자리 필통(피해야 함) | 못 잡음(139 cm 배경) | **≈42 cm 로 잡음** |
| 펜·충전 선(밟고 가도 됨) | 안 잡힘 | 안 잡힘 |

→ **바퀴 옆 60° 채택**(펌웨어 `US_ANGLE_LEVEL_CH={4,3,3,4,3,3,3,3}` + 드라이버
`ultrasonic_fov_rad_per_channel` 바퀴 옆 1.047, 사용자 결정으로 칠하는 폭도 60°). 단 run47 에서 좁은 통로
멈춤의 원인이 되어, 칠하는 폭만 50°/30° 로 되돌리는 안이 후보로 남았다.

### 11.4 노이즈 저감(0x06) 1 / 3 / 4 / 5

| 물체(사진 `~/Downloads/노이즈 테스트.jpg`) | 원하는 동작 | 결과 |
|---|---|---|
| 마우스(앞 0.3 m) | 서야 함 | 레벨 1~5 모두 100 % |
| 충전 선(같은 자리) | 안 서도 됨 | 레벨 1~5 모두 0 % |
| 꼬깔콘(앞 0.6 m) | 서야 함 | 75~100 %(구멍), 레벨 무관 |
| 작은 회색 물체(옆 0.41 m) | 안 서도 됨 | 레벨 5 에서도 100 % — 다만 몸통 옆 0.64 m 라 직진엔 무관 |

→ **노이즈 1 유지.** 무엇이 잡히는지는 레벨이 아니라 물체 높이·모양·빔 안 위치가 정한다. 데이터시트
(출력 인터페이스 14쪽)의 공식 이름은 "전원 노이즈 저감"이고 "높을수록 작은 신호 물체를 놓칠 수 있다".
공식 문서의 다른 손잡이: 지향각 0x07(낮을수록 덜 민감), 내장 동주파수 필터(항상 켜짐·0xFFFE),
'가공값' 출력은 UART 자동 모델만(IIC 는 실시간값), 맞춤 튜닝은 제조사 FAE.

## 12. LLM·미션 — 사용자 메모(`~/Downloads/llm수정.jpg`)와 bag 대조

| # | 사용자 메모 | bag 확인 | 근거 |
|---|---|---|---|
| ① | "3층으로 가줘" → 네 → 410호로 이동 | **맞음** | run37 10:09:09 "3층으로 가자"→"응"→navigate 410호(6a71929f) 실제 주행 |
| ② | "응답이 없어 … 돌아가겠습니다" → 비카야·정지 → 복귀 중으로 남아 대답 안 하고 15 s 뒤 재출발 | **맞음** | run37 홈 복귀 6회(RETURN_RESUME_SEC 15, '복귀 대기 중' 거절 11회), run47 18:24:23 "비카야 다녀오셔야죠"(heard '비카') 에 즉시 홈 복귀 |
| ③ | 응답을 못 들음 — 복도? 핫스팟? 하다 보니 잘 되기도 | **부분** | run37 barge-in 실패 42/61, Realtime 끊김 2회(keepalive, 각 30 s 텍스트 경로). 핫스팟 영향은 작음 |
| ④ | 뒷말 씹힘·오디오 겹침, 자기 말이 사용자 말에 섞임 | **부분** | run37 확인 질문과 고정 문구 겹침, run47 "화장실로 안내해드릴까요?"+"지금은 다른 응대 중" 동시 발화. 로봇 음성이 사용자 녹음에 섞이는 것은 오늘 bag 으로 직접 확인 못 함 |
| ⑤ | 이동 중·비상정지 중 다른 목적지 → "○○로 갈까요?" 뒤 "지금은 이동 중/비상정지라 불가" | 미확인 | 오늘 bag 에 해당 장면 없음. 되묻기 전에 상태로 걸러야 할 항목 |
| ⑥ | 주행 중 "일시정지·잠깐 멈춰·기다려" 안 됨, 가끔 비상정지 | **맞음·원인 확인** | run42 청취 창(비카야 뒤) 안 "잠깐 멈춰/정지해" 3회 모두 무정지 — LLM 이 safety_flag=emergency 만 달고 `/vica/emergency` 미발행. 하드 키워드("멈춰!"·"정지")만 비상정지 |
| ⑦ | "○○ 가줘" → 바로 출발(되묻기 생략될 때 있음) | **맞음** | Realtime 이 need_confirm 을 발화마다 정함: run42 14:18:30 "테스트1로 가자" nc=False 즉시 출발 / run47 "화장실 가자" nc=True 되묻기 |
| ⑧ | 복도 소음 속 5~7 m 밖에서도 대답 잘함 | 긍정 | — |
| ⑨ | "비카야" → "안녕하세요? 시각장애인…" → "비카야" → 지금 다른 응대 중 | **맞음** | run47 18:21:59~18:23:05: 핸들 쪽(doa 166~178°) 호출 → awaiting_user 긴 인사 → "화장실 가자" 가 "다른 응대 중"+"실례했습니다" 로 2회 버려짐, 3번째에 성공 |
| ⑩ | 한번 꼬이면 비카야 불러도 가끔 되거나 엉뚱한 말 | **맞음** | ②·⑨ 의 상태(복귀 대기 중·awaiting_user)에 갇히는 것과 같은 뿌리 |

**메모에 없던 것(bag 에서 추가 확인)**:
- A. 초기위치 전 목적지 → Nav2 거부 3회 → "이동에 실패했습니다"(run42 14:18:30, run47 18:21:24). 원인을 말해 주거나 기다려야 함.
- B. wakeword 계측 로그 시간 계산 오류("대기 -1790241748 s"). 동작 무관.
- C. run37: "3층 세미나실로 안내해드릴까요?"(할 수 없는 일 제안), "세미나실 쪽으로 이동 중"(환각), '407호으로' 조사.
- D. run42 도착 대기시간 질문에 "안녕."(whisper) / '아니'(소리) → deny → "잘 듣지 못했습니다" 로 어긋난 응답.
- E. 청취 창에서 바퀴 방향 찾기 Spin 이 nav 미준비로 거부(run47 18:21:11) — 무해.
