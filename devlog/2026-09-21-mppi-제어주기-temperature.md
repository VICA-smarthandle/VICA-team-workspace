# MPPI 3회차 — 네 값 동시 · 10 Hz 전환 · 0630 시험 (2026-09-21)

run27 · run28 · run29. 커밋 ros2 `7363470` `5ee7ad9`.
앞 회차(run24~26)는 `devlog/2026-09-18-controller-mppi-전환.md`.

## 0. 오늘 요약

- **잔떨림은 이겼다.** 로봇 실제 각속도 표준편차 DWB 0.210 → run28 **0.186**.
  코너 조향 버스트·반전 2.8·2.0 → **2.1·2.0**. 네 회차 중 가장 매끄럽다.
- **대신 벽을 잃었다.** 완주 8/8 → **7/8**, 벽 여유 최소 0.70 → **0.43 m**,
  `start_lethal` 0 → **12**, 일반 코너 제자리 8 % → **36 %**.
- **두 가지는 같은 원인이다** — `temperature` 를 올리면 평균이 너그러워져 부드러워지는
  동시에 **"저기 벽이 있다"는 의견도 묻힌다.** 부드러움과 경계심이 같은 손잡이다.
- **`inflation 0.55 < 외접 0.620` 이 이제 1순위다.** 그 띠는 비용이 0 이라 MPPI 가
  공짜로 본다. 이걸 두고 다른 값을 만지면 같은 일이 반복된다.

## 1. run27 — 네 값 동시, 두 축은 독립이 아니었다

`batch 2000` · `wz_std 0.35` · `time_steps 56` · `prune_distance 3.0` (`7363470`).

**[새 사실] `temperature` 는 궤적 비용 규모 대비 값이라 `time_steps` 가 바꾼다.**
지평이 길어지면 점수 차가 벌어져 같은 temperature 가 더 까다로워진다.

```
실효 표본  run26(36 step) 986/1000 → run27(56 step) 578/2000
검산  batch x0.707 · wz_std x0.70 · 실효표본 악화 x1.84 = x0.91   (실측 x0.914)
```

**시야를 넓힌 것이 잡음 대책을 통째로 되돌렸다.** 두 축이 독립이라고 보고 네 값을
같이 민 것이 이 회차의 교훈이다.

제어주기 놓침 **225회**(그 전 네 회차는 0). 95 %(213/225)가 **사람 회피 ±12초 안**에
몰렸다 — `consider_footprint: true` × 112,000 점이 그 순간만 터진다. 다만 같은 회차
안에서 갈라 보면 놓침 0 구간도 36.5 회/분으로 run26(34.4)보다 나빴다.
**CPU 는 증폭기이지 원인이 아니다.**

`wz_std` 0.5 → 0.35 로 U턴 제자리 51 % → **65 %**. **이 값은 잔떨림과 유턴을 반대로
움직이는 단일 노브다.** 둘 다 만족하는 자리를 네 회차로 못 찾았다.

얻은 것: U턴 벽여유 0.75 → 0.85 m · 일반코너 0.85 → 0.95 m · 레일이탈 7.0 → 3.8 %.

## 2. run28 — 10 Hz 전환 (`5ee7ad9`)

`controller_frequency 10.0` · `model_dt 0.1` · `time_steps 28` · `temperature 1.2`.

**`model_dt` 는 선택이 아니다.** `optimizer.cpp setOffset()`:

```
controller_period >  model_dt  ->  throw runtime_error   (노드가 안 뜬다)
controller_period == model_dt  ->  shift_control_sequence = ON (warm start)
controller_period <  model_dt  ->  WARN, shifting OFF
```

1/10 = 0.1 이 유일한 값이다. 0.05 로 두면 컨트롤러가 기동에서 죽는다.
지평은 28 x 0.1 = 2.8 s 로 그대로. 초당 궤적 점 2,240,000 → **560,000 (x1/4)**.
공식 README 확인: 지평 x vx_max = 1.4 m < local costmap 반경 3.0 m.
낮춘 또 다른 근거: local costmap 이 애초에 초당 5회만 갱신된다 — 20 Hz 는 같은 지도를
네 번 다시 보고 있었다.

### 2.1 [측정 정정] 명령 스트림의 '조향 반전/분' 은 주기가 바뀌면 못 쓴다

세는 기회가 주기에 비례해 줄어든다. **정본 잣대는 `/odom` 각속도를 20 Hz 로 리샘플한
값**(주기 무관).

| 물리 흔들림 (/odom 20 Hz 리샘플) | DWB | run26 | run27 | run28 |
|---|---|---|---|---|
| 각속도 표준편차 | 0.210 | 0.238 | 0.250 | **0.186** |
| 부호 반전/분 | **26.3** | 32.0 | 41.3 | 31.7 |
| 코너 버스트·반전 | 2.8·2.0 | 3.5·2.6 | 3.4·3.3 | **2.1·2.0** |

잡음 축은 예측대로: 주기당 |Δw| 0.0150 → **0.0093**, 실효 표본 545 → **1412/2000**
(전날 "1400 대" 로 예측). 제어주기 놓침 225 → **1**. CPU 49 %.
사람 첫 포착 **2.03 m** · 사건당 정지 **0.58 s** — 둘 다 최고 기록.

### 2.2 대가 — 벽에 붙고 갇힌다

| | DWB | run26 | run27 | run28 |
|---|---|---|---|---|
| 완주 | 12/12 | 8/8 | 8/8 | **7/8** |
| 전체 벽여유 최소 | 0.56 | 0.75 | 0.70 | **0.43 m** |
| 벽여유 <0.45 비율 | 0 | 0 | 0 | **0.11 %** |
| abort · planner 실패 | 6·6 | 9·9 | 6·4 | **24·21** |
| `start_lethal` | 0 | 5 | 0 | **12** |
| 일반코너 제자리 | 7 % | 4 % | 8 % | **36 %** |
| 제자리 최장 | 8.0 s | 8.8 s | 7.0 s | **16.7 s** |

벽 0.55 m 미만 접근 **8회**, 최소 0.40 / 0.36 / 0.35 / 0.25 / **0.11** / **0.00** m.
**여덟 번 모두 v=0.50 · w≈0** — 꺾다가 붙은 게 아니라 **벽과 나란히 최고속 직진** 중이었다.
15:46 화장실 건은 사진으로 확인(소화기 쪽 오른쪽 벽):

```
15:46:46  Failed to make progress
15:46:46  [follow_path] Aborting handle
15:46:48  Starting point in lethal space! Cannot create plan     (24초 갇힘)
```

**사슬**: temperature 1.2 로 평균이 너그러움 → 비용을 거의 안 봄 + inflation 사각지대
(벽 0.55~0.62 m 띠는 비용 0) → 벽과 나란히 전속 직진 → 몸통 접촉 → lethal → abort →
복구 제자리 회전. **일반 코너 제자리 36 % 는 코너를 그렇게 돌아서가 아니라 갇혀서 돈 것이다.**
10 Hz(주기당 4.4 cm)는 붙고 나서 고칠 틈을 절반으로 줄인 보조 요인이다.

## 3. run29 — 0630(좁은 지도) 시험. **측정은 오염됐다**

같은 설정, 지도만 0630. 사용자가 화장실→안내소 코너에서 손으로 옮겨 줬다.

**CPU 가 주행 내내 80 %였다**(0903_d 는 같은 설정으로 49 %). 갇히기 전·중·후가 모두
79~81 % 로, 갇힘의 결과가 아니라 이 회차의 조건이다. 지도 탓도 아니다 — 0630 은
253,396 셀로 0903_d(1,004,751)의 **4분의 1** 이다.

```
상위   ekf_node 31~36 % · amcl 27~33 % · bt_navigator 16~26 % · controller 15~32 %
굶주림 징후   /scan 버퍼 미갱신 10회 · AMCL "cannot publish pose" 28회
              제어주기 놓침 22회 · planner loop 5 Hz -> 1.37 Hz
              "Timed out while waiting for action server ... follow_path" 16회
```

원인 미확정. **이 회차의 주행 품질 수치는 판정 근거로 쓰지 않는다.**

다만 벽 관련은 run28 과 같은 방향이다: 벽여유 <0.45 **1.2 %**(0903_d 0.11 %) · 최소
0.40 m · 갇힌 구간 59초 동안 **벽 0.55 m 미만이 20 %**, 정지 29 %, |w|>0.1 이 59 %.
`Starting point in lethal space` 5회. **넓은 데서 0.11 m 였던 것이 좁은 데서 갇힘으로 나왔다.**

## 4. DWB vs MPPI — 여섯 회차 종합

공식 README: MPPI 는 "가우시안 분포에서 무작위로 흔든 제어를 앞으로 굴려 궤적 묶음을
만들고 critic 으로 점수를 매겨 **소프트맥스로 섞는다**". DWB 는 속도 격자에서
**하나를 고른다**. 고르기 vs 섞기 — 성격 차이가 전부 여기서 나온다.
(DWB 는 76.4 % 의 주기에서 명령이 한 치도 안 바뀐다. MPPI 는 5 % 안팎.)

| | DWB | MPPI 최선 조합 |
|---|---|---|
| 흔들림 크기 | 0.210 | **0.186** |
| 코너 조향 | 2.8·2.0 | **2.1·2.0** |
| 사람 첫 포착 · 사건당 정지 | 1.24 m · 0.64 s | **2.03 m · 0.58 s** |
| 벽 여유 | **0.56 m** | 0.43~0.85 (설정 따라 요동) |
| U턴 제자리 | **38 %** | 51~65 % |
| 일반코너 속도 | **0.464** | 0.398~0.417 |
| 완주 | **12/12** | 7/8 ~ 8/8 |
| 궤적 전멸 | 5회 | **0회** |
| controller CPU | 19.1 % | **17.6 %**(10 Hz 면 더 적음) |

**MPPI 가 이기는 것**: 동적 장애물(더 멀리서 더 짧게), 코너 조향 매끄러움, 궤적 전멸 없음.
**DWB 가 이기는 것**: 유턴, 코너 속도, **결과의 안정성**.

**MPPI 의 진짜 비용은 CPU 가 아니라 값들의 얽힘이다.** 공식 README 는 "구형 i5 에서
50 Hz 이상" 이라 하고 우리 젯슨에서도 DWB 보다 적게 썼다. 대신 나흘간 드러난 얽힘:

- `temperature` 는 `time_steps` 가 바꾼다 (§1)
- `temperature` 는 부드러움과 경계심을 **같이** 돌린다 (§2.2)
- `wz_std` 는 잔떨림과 유턴을 **반대로** 돌린다 (§1)
- `model_dt` 는 제어 주기와 정확히 같아야 하고 아니면 노드가 죽는다 (§2)
- `offset_from_furthest` 는 미터가 아니라 점 개수, 기준도 로봇이 아니다 (09-18 §5.2)

**비카에 성격은 맞다** — 사람이 손잡이를 잡고 따라오므로 섞어서 내는 쪽이 손에 낫고,
사람을 더 멀리서 알아보고 덜 멈추는 것도 안내에 맞다.
**다만 지금 상태로는 못 쓴다** — 완주 7/8 에 벽 0.11 m 다.

## 5. 다음 순서 (적용 안 함)

1. **`inflation_radius` 0.55 → 0.65.** 미뤄 둔 숙제이자 지금 1순위. 이걸 안 하면
   다른 값을 아무리 만져도 벽 붙음이 반복된다.
2. `temperature` 1.2 → 0.9 로 부드러움과 경계심의 접점 찾기.
3. 그 뒤에 유턴 호 — 벽을 제대로 보는 상태라야 "가까워져도 호를 그려라" 가 안전한 요구가 된다.
4. run29 의 CPU 80 % 원인 규명(ekf_node 가 상위). 좁은 지도 판정은 그 뒤에 다시.

bag `~/vica_data/bags/route0921_run27~29` · 분석 `~/vica_data/analysis/run27~29/`
사진 `mppi주행.jpg`(팀 드라이브) · CPU 비교 도구 `~/vica_data/analysis/tools/compare_cpu.py`
