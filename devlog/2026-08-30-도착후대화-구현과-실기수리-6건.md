# 2026-08-30 도착 후 대화 구현과 실기 수리 6건

## 발단

목적지 도착 후 "대기/종료/다음"을 대화로 정하는 시나리오(arrival-dialog-flow,
8/29 설계 확정)를 하루 만에 구현·머지·실기하고, 실기에서 터진 문제들을 당일
수리했다. 덤으로 TTS 목소리 교체(F2 + CosyVoice 굽기)와 whisper turbo 판정까지.
**dev 최종: voice `0ffa3ca` · ros2 `1a192e6`** (둘 다 push 완료).

---

## 1. TTS 목소리 확정 — F2 + CosyVoice 굽기 (오전)

- **시청회 아티팩트**로 여성 5종(F1~F5)×대본 5문장 비교 → **F2 확정**
  (voice `f740444`). 안내 톤은 여성만 쓴다(사용자 원칙, 메모리 등재).
- **CosyVoice3 를 젯슨에 설치**(지뢰 7개 — 메모리 `voice-batch-2026-08-30`에
  전부 기록: 휠 저장소 .dev→.io 이전, libcudss, openai-whisper 빌드,
  torchcodec, cuBLAS→torch 2.8.0 다운그레이드, `<|endofprompt|>` 규칙 등).
  RTF 2.4~5.4 로 **실시간 불가** → 굽기 전용 판정.
- **고정 멘트 71개를 CosyVoice 가 F2 를 클로닝한 목소리로 구움**
  (voice `cd04143`, assets/baked/ + manifest). 실시간(supertonic F2)과
  굽기(CosyVoice F2 클로닝)가 같은 결이라 이중인격 없음. CLOVA 는
  유료(월 기본료형)라 탈락. 팀원 목소리는 supertonic.embed 로 추후.
- **whisper turbo 벤치**: large-v3-turbo 가 medium 보다 빠르고(1.59s vs
  1.91s) RAM 동일 — 교체는 .env 두 줄, 사용자 결정 대기.

## 2. 도착 후 대화 구현 A~E (오후)

설계 정본(아티팩트 "도착 후 대화 설계") 그대로:

| 작업 | 내용 | 커밋 |
| --- | --- | --- |
| A 계약 | VicaIntent 에 wait/finish + wait_minutes | ros2 `ed52f65` |
| B 파서 | wait/finish 는 **LLM 판단**(지름길 없음 — 맥락 의존), 시간 숫자는 코드 | voice `24772e9`~`b206146` |
| C 상태기계 | ASKING_NEXT(질문 태그)·ASKING_WAIT_TIME·WAITING + 유형별 질문 + 무응답 사다리 | ros2 `c9c55da` |
| C 노드 | 라우팅("네"의 뜻은 상태가 정함)·home.yaml 로더·arrival_dialog 파라미터 | ros2 `6e9ede2` |
| D/E | 빈손은 unknown 사다리로, RETURNING 중 "비카야" 브레이크 | (C 노드에 포함) |

- STT 귀띔(CONFIRM_HINT)에 대기·시간·종료 어휘 추가 (voice `3397685`).
- 임시 지도(vica_map_0829_2)에 **홈=입구** (home.yaml) + 테스트 category
  배정(방=restroom, 휴게실=entrance). 앱팀 홈 저장 기능이 나오면 같은
  형식으로 대체.
- dev 1차 머지: 미션 merge `ac93655`(충돌은 test 파일 "양쪽 추가" 2곳뿐),
  음성 fast-forward. 실기 워크스페이스는 주행팀 자이로 브랜치 위에 dev 를
  얹고(fast-forward) colcon 재빌드.

## 3. 실기 1차 → 수리 6건 (저녁)

실기에서 터진 것들을 당일 원인 분석·수리했다. **전부 시험으로 못박음**
(미션 287·음성 380 통과).

| # | 증상 | 원인 | 수리 |
| --- | --- | --- | --- |
| 1 | 도착 멘트보다 대기 질문이 먼저 나옴 | 도착=narration, 질문=response — TTS 큐 우선순위로 역전 | 한 발화로 합침 ("…도착했습니다. 기다릴까요?") `46bb0db` |
| 2 | 앱 주행에도 홈 복귀 | 앱/음성 출처 미구분 | **보류** — 앱팀 상의 (견적 15줄) |
| 3 | 확인 질문 전에 출발 + "이동 중" 거절 | navigate 제안(need_confirm)을 무시하고 즉시 Navigate | 제안은 확인 흐름(CONFIRMING) 합류 `656a001` |
| 4 | "십 분에서 십오 분" → 13분 | LLM 이 상단×1.5 대신 **평균** — 산수를 LLM 에 맡긴 설계 실수 | 산수 전부 코드로(범위=최댓값×1.5, 합성수사 "십오"=15 지원) voice `8697dfa` |
| 5 | barge-in(끼어들기) 전멸 | DOA 잠금(비카야 ±15°) 만료 후 폴백 부채꼴 미설정 — 방향 증거 부족 | 방향 관문 **해제**(장착 실측 전까지, 스위치화) `ec586bf` + 합집합 `841c5da` + doa_probe 도구 |
| 6 | 응답했는데 "응답 없음"→홈, 세울 방법 없음 | 8초 시계가 STT(1.5s)·LLM(2s) 처리를 모름 + 늦은 답 폐기 | **귀 홀드**(/vica/listen_state 중계, closed+6초 유예, 20초 상한) voice `0ffa3ca`·ros2 `1a192e6` + **복귀 그물**(RETURNING 중 wait/navigate 답 → 복귀 취소·즉시 처리) |

기타: arrival_dialog 기본 on(`94cbdbc`, 사용자 결정), 실기 체크리스트
아티팩트, 원격 GUI 는 노트북 FreeRDP → xrdp(:10) 경로 확인.

## 4. 남은 것

- **실기 2차** (수리 6건 검증): 미션·웨이크워드·⑫ 재기동 후 체크리스트
  아티팩트("실기 체크리스트 0830")대로. 특히 — 합쳐진 도착 질문, 확인 후
  출발, 시간 15분/23분, 끼어들기 부활, 늦은 답의 복귀 취소.
- 앱 주행 홈 복귀 제외 (앱팀 상의 후 15줄).
- **마이크 DOA 점검** — 방향 관문 재활성 절차는 메모리에. 해제 중 옆사람
  말에 로봇이 끊기면 그게 신호.
- 조사 오류 "입구**으**로"(자동 confirm_prompt 받침 처리) — 소소 건.
- whisper turbo 교체 결정, 온보딩 재녹음, TTS 도감 갱신.
