# 2026-09-01 미션↔TTS 소통 확장과 멘트 대청소

8/31 야간 실기의 로그를 재료로, 로봇이 "고장난 것처럼 말이 많던" 문제를
구조부터 손봤다. 미션 8커밋(feat/app-ux-mode-split)·음성 7커밋(dev),
시험 미션 341·음성 397 통과. **미션·웨이크워드·LLM·TTS 재기동 필요.**

## 1. 아침 — 재굽기와 접수 신호 교체

- `bake_one_cv` 첫 실기동에서 venv 분리 결함 발견·수리(`37c3314`) 후
  "비상멈춤이 해제되었습니다" 구움(2.2초, `25c7f4e`). 원격이라 청취 확인은
  실기 체크리스트로.
- **"확인할게요" 풀 폐지 → "생각 중" 운율 루프**(`4cf2c64`). 접수 신호는
  LLM 을 타는 모든 발화 앞에 붙어 야간 49분에 28회 나갔다. 대신 낮은
  음량의 아르페지오(~2.1초)를 응답이 나올 때까지 반복한다. 스피커 단일
  출구 원칙대로 TTS 노드 안에서 0.2초 조각으로 틀어, 응답·긴급이 오면
  한 조각 안에 자리를 내준다. 20초 시한·finally 소등.

## 2. 재청취 연쇄 폭주 — 3겹 차단 (`d586672`)

질문 창이 잡담·자기 목소리를 삼키면 그 대답이 또 창을 열어 문장이 와다다
쏟아졌다. ① **에코 제거**: 로봇이 방금 한 말(tts_done, 12초)을 전사에서
문장 단위로 걷어냄 — '질문+어.'는 '어.'만 남는다. ② **무의미 침묵**: 자동
창(40초 문맥)에서 온 unknown·clarify 는 대꾸 없이 로그만. ③ **기록 보호**:
기각 발화는 대화 히스토리에 안 남음 — "그래"가 엉뚱한 목적지에 붙던 뿌리.

## 3. 발신 실태 조사 → 멘트 대청소

tts_request 발신 집계(야간 49분, 229건): 미션 55%·LLM 30%·"네?" 14%.
"못 알아들음 뒤처리"가 22건. 42곳 멘트를 분류(질문 11·필수 12·낡으면
무의미 19)하고 **6건 삭제**:

- 회전 완료·재시도 안내·"홈으로 복귀중" 2곳·엇갈린 confirm·취소 재확인
  타임아웃(`d7a8c31`)
- **각성 질문 "다시 안내를 시작할까요?"**(`ef64e4d`, A안): 도착-대화
  문법을 빌린 오배선이라 "네"가 "몇 분쯤?"으로 새고, 야간 21:42엔
  혼란→무응답 판정→사용자를 두고 홈행까지 갔다. WAITING·복귀 중
  "비카야"도 이제 질문 없이 접고 새 대화로 받는다.

## 4. 큐 청소 2종 — "상태가 바뀌면 입부터 닫는다"

- **취소·앱 선점**(`1010770`): `_force_clear_all` 맨 앞에 StopSpeech —
  하던 말 중단 + 대기 비긴급 폐기(/vica/tts_stop). 음성 취소·앱 취소·앱
  선점 세 경로 공통. 낡은 "방2 도착" 멘트가 새 주행 뒤에 나오던 헛소리 제거.
- **"비카야" = 리셋**(`70d51b8` + `01ee2d9`): 호출이면 무조건 큐 청소
  (문장 틈 호출 구멍 폐쇄) + 미션의 답-대기 상태(확인·도착·접근 질문)를
  조용히 접음 — 옛 질문의 답 자리가 새 대화 첫 마디를 삼키지 못하게.
  판정을 LLM 히스토리에 맡기지 않는 이유: 짧은 답은 LLM 을 안 타고,
  히스토리는 오염되며, 판정 권한은 미션이라는 원칙.

response 유통기한 꼬리표("선언은 발신자, 판정은 큐")는 **보류** — 위
청소들이 발생원을 막았으니 실기 2차에서 밀림이 남는지 보고 결정.

## 5. 확인(CONFIRMING) 두 겹 — "응 화장실로 가자"

야간 로그: 같은 목적지 confirm=True 가 반복되다 3수째에야 출발(48회 관측
군). 원인은 복합 답("응 ○○로 가자")이 순수-단어 지름길을 빗나가 LLM 으로
가고, LLM 이 재제안으로 되돌리는 흔들림. ① **첫 단어 지름길**(`4b08273`):
첫 단어가 긍/부정이면 0초 확정·거절 — 전체 일치라 "어디로 가?" 오폭 없음.
② **미션 그물**(`bd8bf55`): 확인 중인 그 목적지의 재제안이 오면 답으로
간주, on_confirm_answer 통로(게이트 포함)로 출발.

## 6. 짧은 답 구제 — "네" 0.08초 (`3dde8a6`)

유령 문턱(0.16초)이 한 글자 답을 잘랐다(야간 실측 0.08초·rms 0.0289 기각).
**질문 창 한정**(사용자 결정)으로 짧아도 또렷하면(rms≥0.02) STT 재판을
받되, 통과는 정답 어휘(긍/부정)로만 — 유령 전사 부활 방지. 자유 창은
기존 문턱 그대로.

## 미뤄둔 것 (재개 시 이 목록부터)

1. **response 유통기한 꼬리표** — 실기 2차에서 큐 밀림 관찰 후 (4절)
2. **tts_done ID 태그** — 문구 문자열 대조의 취약성 (소통 확장 후보 ②)
3. **미션의 tts_state 구독** — 필요해질 때 (후보 ③)
4. **DOA 방향 관문 복원** — doa_probe 실측 후 (.env GATE=0 유지 중)
5. 구운 해제 멘트 **청취 확인** — 체크리스트 항목 (원격이라 미청취)
6. "생각 중" 운율의 **소리 결** — 1차 합성안, 실기에서 듣고 조정
7. 확인 그물 경로의 **이중 발화**(질문+시작 멘트 연달아) — 관찰
8. 신뢰도 필터(no_speech·logprob)의 작은 소리 오살 — 기각 로그 관찰 지속
9. "약 N미터" 마일스톤 — 배선만 남김, 되살릴지 결정
10. 미사용 구운 wav 정리(MSG_WAIT_RESUME_ASK 등) — 여유 있을 때
